# Ui_Settings — Hydration (2026-09-07 14:36)

## State
Settings page UI, persistence (SharedPreferences), settings widgets, and UX. Latest work (2026-09-07): the **header-normalization** plan was implemented on `feature/header-norm` (build green, no commit). The Settings overlay header was normalized from an inline Row (48dp back `Button` + 24sp title) to the shared `DrawerHeader` composable (32dp back + 17sp title, no `actions`). Design ruling: `DrawerHeader` already auto-centers the title vertically against the tallest control via `CenterVertically`, so no title-sizing change was needed — a pure call-site swap. Removed now-unused `ArrowBack` + `CircleShape` imports; left orphaned `settings_back` string (label now `cd_close` "Close/Fermer"). **Follow-up 1 (2026-09-07): removed the leftover 16dp `Spacer` between the header and the tab bar.** **Follow-up 2 (2026-09-07): post-header spacing normalized to a single 6dp gap across all drawers** — added `ui.padding.header.vertical=6dp` token + `AppConfig.uiPaddingHeaderVertical`; `DrawerHeader`/`DrawerScaffold` verticalPadding defaults now read the token; removed Menu's 20dp spacer, Marker's contentPadding top + internal 8dp spacer, and Track's contentPadding top at both landscape and portrait sites. **Follow-up 3 (2026-09-07, debug): fixed the detail-drawer header layout** — root cause: the Material3 `IconButton` back button enforced a 48dp minimum interactive size, overflowing the 48dp header row and clipping the button's top; replaced it with a plain clickable 32dp circle in `DrawerHeader`. Removed the rounded top corners on the Marker + Track detail drawers (square top edge, per user). Set the portrait Track drawer height to header(48)+card+footer so the card sits ~6dp below the title (aligned the `MeasureHeight` probe to the actual content). **Follow-up 4 (2026-09-07, debug): fixed the portrait Track drawer's tiny vertical scroll** — the Prev/Next footer was hardcoded at 60dp but renders ~64dp, leaving a ~4dp scroll. Applied render-measure-resize: added a `MeasureHeight` probe for the footer (wrapped in a single `Column`, since `MeasureHeight` only measures the first child) and sized the drawer to header(48)+card+measured-footer. `docs/ui-drawer-guidelines.md` updated (§6 token + uniform post-header gap note + portrait drawer height floor rule, §12 Consumers row + footnote, §12 Not Migrated). Ask review verdict: CORRECT — build green, clean minimal diff. Prior work (properties-normalization + cleanup, transparency normalization + low-depth 2-depth redesign, Card/Expander/NestedCard refactor, guideline consolidation, marker "Belongs to track") remains implemented.

## Target Files
- `app/src/main/java/ykws/android/maro/ui/map/MapScreen.kt` — Settings header → shared `DrawerHeader` (SettingsOverlay ~3319)
- `app/src/main/java/ykws/android/maro/ui/components/DrawerScaffold.kt` — canonical `DrawerHeader` (back button → 32dp clickable circle)
- `app/src/main/java/ykws/android/maro/ui/map/OverlayLayer.kt` — portrait Track drawer height (render-measure-resize footer)
- `app/src/main/java/ykws/android/maro/ui/map/MarkerDrawer.kt` — square top corners
- `docs/ui-drawer-guidelines.md` — §6 note, §12 Consumers + Not Migrated, portrait drawer height floor
- `app/src/main/assets/ui.properties` / `AppConfig.kt` — UI tokens (prior work)

## Next Step
`render-tweaks` todo and deferred `settings apply on close` section remain open. BUILD SUCCESSFUL.
