# Global Context — Routing Table

> State only — routing map, feature summaries, focus history, global todos, doc index. Rules and instructions live in `AGENTS.md`.

## Focus History
- [2026-09-16 12:32 UTC] Tracks + ColorManagement — one session on `feature/no-black-casing` that first unified the map's surfaces and then answered a device report: `ui.map.surface.*` became the single home for the fill, corner, padding, border and both alphas, with `MapSurface` and `MapToggleSquare` the one painting path for the nine surfaces, one colour accessor surviving (a name that had no reader before and now has every one), the content-only fade replacing the two whole-box `.alpha()` calls that made the tracking and lock off boxes land twice as faint, the recenter button joining on `ui.accent`, and the zone card gaining the outline it never had; the Ask hop sent back the tracking dot, which sat at `22 − glyphBox/2` instead of 6 dp and drifted with the emoji's metrics, plus nine claim drifts across the file, `AppConfig` and both docs; the device report then showed the scale's labels breaking **inside the digit pair** rather than wrapping — `softWrap = false` is the fix — with their column widened to the whole bar-to-border space, and every alpha key in all three palettes now states `1.0 = fully opaque, 0.0 = fully invisible` with the depth ramp's 0–255 range flagged as the exception; `apk-build.bat` SUCCESS throughout with the scoped run steady at 126, and the label's 12 dp ceiling, the owed device judgement and two unreferenced strings stay open → xTrack/Tracks/FEAT_HYD_Tracks.md
- [2026-09-16 10:05 UTC] Tracks — one session on `feature/no-black-casing` that made the speed scale interactive: it keeps its display rule untouched — `legendVisibleFor`, `legendVisibleForState` and `bandedStrokeOnMap` carry no new parameter and no body change — while one persisted `trackLegendExpanded` (`track_legend_expanded`, default true, so an untouched install shows the card) decides which of two faces it wears, the card collapsing into a 44 dp square on the row's own recipe carrying the ⏱ stopwatch written `\u23F1\uFE0F` with no active state of its own, both faces on one hoisted `legendAnchor` value the card alone widens; the `#implement` pipeline then closed the Ask hop's findings in two Code hops, the first correcting the shipped entry rather than extending it — the two-face predicate deleted outright because its `legendAllowed` arm was dead behind the branch's own guard, which puts the scoped run back at 126 with `TrackRenderModePathTest` at its original 15, plus one shared anchor, a KDoc naming the branch it follows, and every comment and doc row that still counted four toggle boxes now naming five readers — `apk-build.bat` SUCCESS throughout, and the device run, F2's accepted gesture cost and F5's fifth hand-copied square stay open → xTrack/Tracks/FEAT_HYD_Tracks.md
- [2026-09-16 06:31 UTC] Tracks + ZoneTile + ColorManagement — one session on `feature/no-black-casing` that closed the selection-rendering work and normalised the map's chrome: the outlines plan's three items shipped (the arrowhead rim at the line's width, the legend gate keyed on banded strokes through one shared entry point, the strip's chrome), the speed scale then became one toggle button wide on the row's own gutter with 8 dp corners, bold `uiTextSecondary` labels, asymmetric padding and its own background, and the map's toggle row and two overlay cards finally moved onto two families — `ui.map.toggle.*` and `ui.map.overlay.*` over one shared `ui.map.surface.inactive` (`#A8FFFFFF`) — with eighteen keys and their accessors in the palette and the retired `ui.button.disabled.*` trio and three `status.*.alpha.*` keys gone; `apk-build.bat` SUCCESS with 126 scoped `ui.map` + `config` tests green throughout, and the open items are device-tuned values, readerless keys and one dead `EarthWaterIcon` arm → xTrack/Tracks/FEAT_HYD_Tracks.md
- [2026-09-15 19:57 UTC] Tracks — one session on `feature/no-black-casing`: the selection rendering shipped (per-type widths read from `maro.properties`, the selection opaque, cased and drawn above every other trace, chevrons in proportion with an outside-only dark rim, and the controls separated — arrows to the mode, the eye to the selection's fill and persisted on the first tap, the legend given the eye as a trigger, the spacing ceiling at 400 dp); the outlines plan was then rewritten implementation-only and reviewed twice, leaving three items open in its §2 — the arrowhead rim at the line's weight, the legend gate on banded strokes, and the strip's chrome; the walk's level of 2026-09-15 is open with items 6 and 7, which are §2.2 and §2.3 of that plan → xTrack/Tracks/FEAT_HYD_Tracks.md
- [2026-09-12 12:30 UTC] WorkflowImprovement — **command-flow shipped end to end on feature/wrKFl** (after `5558f3b`, `cd2f635`, `37e238a`): the Output Contract gained a countable unit, the containment rule and the objection bullet; rule 2 scopes ambiguity to MODE LOCK/QUESTIONS and rule 3 makes a gate name its action; `#go`, `#review` (cascade + two guard lines), `#walk` (facets, `## Walk` template, the §7a/C12 single statement) and `#brief`/`#full` are registered with pages; the first real `#archive` pass retired seven files into xxArchive with INDEX rows, leaving docs-integrity and archive-lifecycle for a second gate; the walk closed with the push still open → xTrack/WorkflowImprovement/FEAT_HYD_WorkflowImprovement.md
- [2026-09-12 11:52 UTC] WorkflowImprovement — **session closed, restart-ready** on feature/wrKFl (`5558f3b` integrity, `cd2f635` archive + two plans, `37e238a` command delta on open): `#archive` shipped with the xxArchive tier and `#doctor` a–r; the command-flow design was walked to closure with its six resolutions applied but unshipped; eight retirement candidates logged as a feature todo; this is a transient 11th entry — the next `#bake` prunes the oldest → xTrack/WorkflowImprovement/FEAT_HYD_WorkflowImprovement.md
- [2026-09-12 10:57 UTC] Performance — **power-management Phase 1 COMPLETE, reviewed, cleaned and pushed** on feature/staying-alive: `82cee3f` (feature) + `ba4c187` (freshness fix), then a cleanup pass run through `#implement` — the grace KDoc corrected to **1–15**, the grace ticker gated on a live hold (so its KDoc finally matches the code), dead imports removed, and the battery-exemption question consolidated into `data/power/BatteryExemption`: one `shouldPrompt` predicate at all four trigger sites, so an already-exempt user is no longer prompted from the MapScreen path. A follow-up pass applied the review findings (the keeper's KDoc now states it delegates the query and keeps the method as the phase-3 service-lifecycle seam; `MapScreen` imports the predicate; plan §4.1/§4.2 describe the split in the same words). 23 power tests green, `apk-build.bat` SUCCESS; **device verification still unrun** — grace release at the 1-minute minimum plus the master-off check for an external screen-awake source; **background process management untouched by design** — the service stays unconditional until phase 3 → xTrack/Performance/FEAT_HYD_Performance.md
- [2026-09-12 09:00 UTC] WorkflowImprovement — **docs & rulebook integrity pass COMPLETE on feature/wrKFl** (baseline commit `2b74f09` + this pass): command registry single-sourced (AGENTS.md §7b normative, `cmd_help.md` stamped *derived*, GIT_WORKFLOW demoted to git detail), `#checkout` de-registered, `#bake` explicit-only and `#commit` offers a bake on stale state with a `Last Bake` stamp added to the FEAT_HYD template, tiered git confirm policy, `plans/` and `docs/oZer/` deleted (8 renames, 2 deletions, **Tasker feature created**), README pointer-ised with GDAL moved to SETUP, MARO_ARCHITECTURE plus seven docs audited, `#doctor` extended to checks a–p; no build (docs-only, plan C5) → xTrack/WorkflowImprovement/FEAT_HYD_WorkflowImprovement.md
- [2026-09-12 07:42 UTC] Ui_Settings — focused on keep-screen-on ("Keep phone on" toggle, System tab ▸ Screen section) on feature/staying-alive: existing window-flag path in MainActivity audited; extension intent open for discussion → xTrack/Ui_Settings/FEAT_HYD_Ui_Settings.md
- [2026-09-11 20:39 UTC] Mergitur — **three-branch integration COMPLETE, baked and doctor-clean** on feature/mergitur (TR → TI → GL, one merge commit each; `pre-mergitur` tag at `025e1bc`): TR's ConfirmDialog normalization + resume-with-backup re-seated into TI's derived map-visibility / `MapSelectionPolicy` / `MapTrackSegments` restructure; GL's state-only `GLOBAL_CONTEXT.md` + AGENTS.md rule consolidation landed; build SUCCESS every hop, scoped tests green, survival gate proven (`MapTrackSegments.kt` identical to TI), both Ask findings closed (B1 accepted as a documented behaviour change); push + PR and device smoke test pending → xTrack/Mergitur/FEAT_HYD_Mergitur.md

## Routing Map
| Keyword | Feature File |
|---|---|
| documentation, docs, readme, faq, setup, git_workflow, maro_architecture | xTrack/Documentation/FEAT_DSC_Documentation.md |
| workflow, clinerules, xtrack, commands, memory, #doc, doccommands | xTrack/WorkflowImprovement/FEAT_DSC_WorkflowImprovement.md |
| zone300, 300, bande 300m, water-only | xTrack/Coastline/FEAT_DSC_Coastline.md |
| depth, bathymetry, depthmapping, baro, seafloor, soundings, litto3d, shom, emodnet | xTrack/DepthMapping/FEAT_DSC_DepthMapping.md |
| coastline, trait de côte, fourmigue, hazard, obstruction, balisage, danger_isole, aton, seamark, lighthouse, reef | xTrack/Coastline/FEAT_DSC_Coastline.md |
| dashboard, ui, layout, hud, display, screen | xTrack/Ui_Dashboard/FEAT_DSC_Ui_Dashboard.md |
| gps, gpsplugin, gps mode, demo mode, heading, course, compass, location, geolocation | xTrack/GPS/FEAT_DSC_GPS.md |
| mapdisplay, map display, map, layer, depth layer, color depth, orientation | xTrack/UI_Map/FEAT_DSC_UI_Map.md |
| performance, battery, gps-tune, adaptive, compass-gate, map-refresh, battery-drain, power management, keep screen on, keep awake, screen-on, wakelock, screen lock, lock delay, movement gate | xTrack/Performance/FEAT_DSC_Performance.md |
| bake, baking, bake-script, bake-bat, apk-bake, apk-build, apk-deploy, deploy, prebake-pipeline, bake-env | xTrack/BakeNormalization/FEAT_DSC_BakeNormalization.md |
| depthsafety, depth-safety, danger-depth, shallow, grounding, isobar precision, isobath precision, depth alert, depth overlay, water-only | xTrack/DepthSafety/FEAT_DSC_DepthSafety.md |
| app-bak-flow, app-back-flow, back, back button, back handler, exit, double-back, press back, touch lock, splash guard | xTrack/Ui_General/FEAT_DSC_Ui_General.md |
| filters-link, filter link, linked filters, decouple list map filter, map filter referential | xTrack/Ui_General/FEAT_DSC_Ui_General.md |
| settings, preferences, config, scroll, options | xTrack/Ui_Settings/FEAT_DSC_Ui_Settings.md |
| regulation, regulated zones, regulatedzone, regulation zone, speed zone, speed limit, anchoring, SHOM regulation, shom reg, maritime regulation, regulatory zone, réglementation maritime, zone réglementée, arrêté maritime, DIRM, cap d'antibes, lérins | xTrack/RegulatedZones/FEAT_DSC_RegulatedZones.md |
| arclayout, arc, arc-menu, layer-toggle, multi-btn, layer, toggle, fan-out | xTrack/ArcLayout/FEAT_DSC_ArcLayout.md |
| color, colour, color management, color-scheme, colors.properties, colour palette, colour scheme, colours, theme | xTrack/ColorManagement/FEAT_DSC_ColorManagement.md |
| zonetile, zone tile, zone info, zone ahead, zone cone, speed zone, speed zone display | xTrack/ZoneTile/FEAT_DSC_ZoneTile.md |
| boat, trace, trip, boat-trace, boat-tracing, track, tracks, recording, port-salis, journey | xTrack/Tracks/FEAT_DSC_Tracks.md |
| markers, pin, circle, corridor, usermarker, user marker, where am i | xTrack/Markers/FEAT_DSC_Markers.md |
| menu, menu drawer, hamburger, track drawer, position source, menu overlay | xTrack/Ui_Menu/FEAT_DSC_Ui_Menu.md |
| navigation, nav, heading arrow, direction arrow, speed arrow, cap arrow, course arrow, auto-show, auto show, redisplay on approach, zone reveal, demo auto-show | xTrack/Navigation/FEAT_DSC_Navigation.md |
| checkdev, dev branch, branch health, ahead behind, workflow hygiene | xTrack/CheckDev/FEAT_DSC_CheckDev.md |
| health, diagnostics, crash reporting, telemetry, memory monitoring | xTrack/Health/FEAT_DSC_Health.md |
| mergitur, merge, three-branch integration | xTrack/Mergitur/FEAT_DSC_Mergitur.md |
| tracks-import, tracksimport, gpx import cleanup, map track visibility, selection policy, visibleonmap | xTrack/TracksImport/FEAT_DSC_TracksImport.md |
| tasker, water state, intent broadcast, query receiver, automation | xTrack/Tasker/FEAT_DSC_Tasker.md |

## Feature Summaries

| Feature | One-Liner | Created | Modified | Status |
|---------|-----------|---------|----------|--------|
| **ColorManagement** | **Centralised colour palette — all tokens in colors.properties with alias interpolation, documented in color-scheme.md; shared UI colour tokens de-settings-ified (uiSettings* → ui*, e.g. uiBackground/uiTextPrimary/uiAccent/uiDividerColor); the map's chrome tokenised into two families, `ui.map.toggle.*` and `ui.map.overlay.*`, over one shared `ui.map.surface.inactive`; the map's surfaces then collapsed onto one `ui.map.surface.*` block with `ui/map/MapSurface.kt` as the only painting path — nine surfaces, one colour accessor, a content-only fade, one corner and one outline — and every alpha key in all three palette files now states `1.0 = fully opaque, 0.0 = fully invisible`; the bottom-left zone stack then joined that family on its active face, and the one-home-per-fact rule was applied — the family's caller and exception lists live in `MapSurface.kt` alone, `colors.properties` is the source of truth for every value with `AppConfig`'s defaults and every doc moved onto it** | **2026-06-16 14:05** | **2026-09-16 15:00** | **active** |
| **Documentation** | **README, FAQs, setup guides, architecture docs, and plans cleanup** | **2026-06-11 06:42** | **2026-09-04 22:36** | **active** |
| WorkflowImprovement | xTrack #command system and rule-book governance. 2026-09-12 pass (`feature/wrKFl`): command registry single-sourced (AGENTS.md §7b normative, `cmd_help.md` derived, GIT_WORKFLOW demoted to detail, `## OwnedFiles` retired), `#checkout` de-registered, `#bake` explicit-only with `#commit` offering a stale bake, tiered git confirm policy, `plans/` and `docs/oZer/` removed (Tasker feature created), README pointer-ised with GDAL moved to SETUP, `#doctor` extended to a–p; archive lifecycle + `#archive` implemented (xxArchive tier, digest floor, checks q–r, committed `cd2f635`); command delta on feature open shipped (newest three command-touching `## Implemented` entries, stated once in §7a); command-flow shipped end to end — the Output Contract gained a countable unit, the containment rule and the objection bullet, rules 2–3 were added, and `#go` / `#review` / `#walk` / `#brief`·`#full` are registered with pages; the first real `#archive` pass retired seven files into xxArchive, with docs-integrity + archive-lifecycle held for a second gate. Absorbed WorkflowAmbiguityFix 2026-06-28. 2026-09-13 (`feature/wrKFl`): the command-flow review was remediated (rule 3's undefined clause deleted, the walk-reporting rule given a Turn 1 home, the plan lifecycle written into §7a), a depth-1 walk stack shipped with `#done` retired and both gates reading any open level, and the adapter clean-up moved the file templates to `docs/xtrack-templates.md` with `.claude/skills/xtrack/references/` deleted. | 2026-06-03 00:00 | 2026-09-13 19:02 | active |
| DepthMapping | Bathymetry / depth mapping from Litto3D, SHOM, EMODnet sources | 2026-05-10 00:00 | 2026-09-04 22:36 | active |
| Coastline | Coastline extraction, spatial indexing, isOnWater, hazard rings, unified data store; eastern bound extended to Menton (7.55°E), single region ID via BuildConfig | 2026-05-10 00:00 | 2026-06-23 07:08 | active |
| Ui_Dashboard | Main dashboard UI layout and HUD information display | 2026-05-15 00:00 | 2026-09-04 22:36 | active |
| GPS | GPS plugin with demo mode, heading/COG compass, geolocation, auto-follow spring-back hold | 2026-05-10 00:00 | 2026-09-04 22:36 | active |
| UI_Map | Map rendering, depth color layer, orientation overlay, boat marker offset; deterministic overlay z-order (tile→base→tracks→markers) via OverlayZOrder.reorder; marker filter drives map overlay (map renders from filtered list, panel auto-closes when filtered out); menu/fan open closes marker+track dashboard; list-context stacking removed (close returns to map, Prev/Next navigates) | 2026-05-10 00:00 | 2026-09-04 22:36 | active |
| Performance | Battery optimization, adaptive GPS tuning, compass gate, map refresh; **power management** (screen-hold policy + keeper in `data/power/`, movement gate + interaction grace, lock-phrased toggle, battery-prompt consolidation into `AppSettings` with a shared `BatteryExemption` predicate, sourced reading freshness via `SpeedFreshness`, demo mode grace-governed) | 2026-05-20 00:00 | 2026-09-12 10:57 | active |
| BakeNormalization | APK bake/build/deploy pipeline and prebake data processing | 2026-06-01 00:00 | 2026-09-04 22:36 | active |
| DepthSafety | Danger depth alerts, shallow water grounding prevention, isobath precision | 2026-06-03 00:00 | 2026-09-04 22:36 | active |
| Ui_General | App-lifecycle UX: back-to-exit guard, edge-to-edge rendering, WindowInsets, list normalization (ListOverlayScaffold, sort, swipe-to-delete, per-type custom sort fields, localized EN+FR), list-detail navigation (scroll preservation, prev/next, exit conditions), drawer delete undo (snackbar + undo + reopen), notification lifecycle (foreground notification follows recording state, 3-choice exit dialog, service-owned recorder), landscape menu drawer (scroll-when-overflow, overscroll suppressed when content fits), top-left icons (GPS→tracking→land/water, GPS click-to-toggle, 🐾, idle dot red), menu drawer rows (Tracks/Markers captions, chevron opens first filtered/sorted item, disabled when none), touch-lock splash guard (📵 toggle, double-tap zoom, status.lock.* tokens; renamed from screen-lock 2026-09-12 so `screen lock` belongs to the Performance power feature); compact track/marker list cards (desc/comment lineHeight 14sp, reduced v-padding, header→title separator removed); drawer footer pinning + dynamic animated height (card-probe); drawer vertical rhythm (12dp card padding, header vertical padding 12dp, footer 10dp rhythm); landscape drawer/settings sizing (portrait width × ui.landscape.panel.widthScale 1.2, menu 75%); scrim matrix (0.50 menu/settings/lists, none detail+wizard, transparent fan/lock); dashboard close (back/menu/create close, fan keeps open); list-count-display (Track History title "· N" + menu Tracks/Markers counts left of chevron, live track excluded); marker-filter-remove-geometry (markers filter Geometry axis removed; icon/pinned/origin remain, origin ungated); track-filter-date-range (Track Date Range filter extended+reordered to 7 options Last week→Last 6 month with All last + default, THIS_YEAR dropped) | 2026-06-08 16:43 | 2026-09-12 09:05 | active |
| Ui_Settings | Settings page UI — 4 tabs; row-family normalization R1–R8 (container-owned horizontal inset; SectionHeader/SectionDivider/Card/NestedCard/CardDescription; ToggleRow/SliderRow/RangeSliderRow/SegmentedRow/ColorRow/ColorPairRow; CategoryToggleGroup) plus section-title colour promotion and tab-finalization phase 1 (18sp sentence-case titles, M3 SecondaryScrollableTabRow cells, single 14dp section rhythm); properties/header/drawer-measurement normalization (ui.properties → typed AppConfig tokens, shared DrawerHeader, wrap-content drawers, marker belongs-to-track row); C12 OverlayLayer parameter-object collapse (six @Immutable bundles, 89→60 params); transparency paradigm (0=opaque / 100=invisible, two-thumb Border·Fill, two-depth low-depth warning); confirm-dialog normalization COMPLETE (one ConfirmDialog on the overlay ladder, dialog-owned scrim, in-drawer merge + batch delete hoisted, ladder dialog plumbing deleted; P4 scrims are hard on/off toggles — the ladder scrim yields while a dialog is visible, so dims never stack) | 2026-06-09 15:28 | 2026-09-11 20:00 | active |
| **Navigation** | **Navigation aids — heading/speed arrow and direction line on map overlay; auto-show / re-display of regulated zones + 300 m band on approach (demo + GPS)** | **2026-06-10 08:40** | **2026-09-08 16:49** | **active** |
| **RegulatedZones** | **Maritime regulatory zones — multi-source normalization (SHOM INSPIRE + IGN Natura 2000), sealed classification, 8-category icon mapping, keyword-driven display logic; the bottom-left icon stack now paints through the map surface family, one weight per tag** | **2026-06-11 18:00** | **2026-09-16 15:00** | **active** |
| **ArcLayout** | **Layer toggle arc menu — pure-Compose semicircle fan-out with layer toggles, FanLayout framework, fixed child centering using effectiveTheta=180/currentCount; control-stack button swap (Add Zone above fan)** | **2026-06-13 07:34** | **2026-09-06 21:19** | **active** |
| **ZoneTile** | **Zone information tiles and map overlay rendering — zone-ahead cone/line, speed zone display, ETA, zone state management; the zone-info text card now reads the map overlay family (`ui.map.overlay.*`) over the shared `ui.map.surface.inactive`** | **2026-06-17 09:45** | **2026-09-16 06:31** | **active** |
| **Tracks** | **Boat movement tracking: record from GPS fixes, persist as protobuf, render on the map under one three-way render mode (Simple / Dir & Speed / Colours), GPX export, track history UI, idle auto-markers, direction chevrons; finalize durability + data-derived end/idle/nav (schema 4); marker-track single reference and auto-marker lifecycle; export naming + import modes; live-track paint regression; resume-confirm-backup; selected-track speed heatmap and its render-mode generalization shipped — every stored track banded from the ramp under per-type widths read from `maro.properties`, the selection opaque, cased and drawn above every other trace, the menu's Tracks rendering switch, the drawer eye scoped to the selection's fill and persisted; the outlines plan's three items shipped on 2026-09-15 (the arrowhead rim at the line's weight, the legend gate on banded strokes, the strip's chrome), the speed scale then went one toggle button wide on the row's own gutter, on 2026-09-16 the toggle row and both overlay cards moved onto the `ui.map.toggle.*` and `ui.map.overlay.*` families over a shared `ui.map.surface.inactive`, and the speed scale itself became one control with two faces on one anchor — the card collapses to a 44 dp square on the row's recipe carrying the ⏱ stopwatch, its state persisted as `trackLegendExpanded` with the display gate untouched — and its labels then stopped breaking inside the digit pair, centred in the full bar-to-border space → xTrack/Tracks/FEAT_DSC_Tracks.md** | **2026-06-15 00:00** | **2026-09-16 12:32** | **active** |
| **CheckDev** | **Dev-branch health monitoring — remote branch state, ahead/behind analysis, workflow hygiene validation** | **2026-06-20 11:42** | **2026-06-20 11:42** | **active** |
| **Health** | **Application health monitoring — diagnostics, crash reporting, memory/performance telemetry** | **2026-06-20 11:42** | **2026-06-20 11:42** | **active** |
| **Markers** | **User-defined map markers (Pin, Circle, Corridor) with sea-distance-gated proximity matching, percentage-based sort scoring, and on-demand "where am I?" query — icon/pin decoupling implemented (icon pure POI, pin real persisted flag mirroring tracks); pin-halo-rendering implemented (static halo ring pinned white/unpinned light-blue, corridor always-on line + under-line halo, selected-marker gold via selectedMarkerId, focus zoom-to-fit, MarkerAppearance/MarkerHalo split)** | **2026-06-22 11:52** | **2026-09-05 10:42** | **active** |
| **Ui_Menu** | **Hamburger menu drawer — position source, track recording, marker management sections; right-side sliding panel via OverlayLayer/DrawerSlot; track/marker action normalization (shared list/detail card, double-click inline edit, chevron tappable gutter, delete = swipe/header-trash); menu render migrated to the Settings model (shared SectionHeader/SectionDivider/CardArea/ToggleRow in ui/components, 14dp section rhythm, sentence-case titles reusing settings_section_*)** | **2026-07-05 06:57** | **2026-09-11 15:23** | **active** |
| Mergitur | Three-branch integration COMPLETE into feature/mergitur (TR `735cd29` → TI `e4d56f6` → GL `431d165` on base `025e1bc`, tag `pre-mergitur`): TR's ConfirmDialog + resume-backup re-seated onto TI's derived visibility / `MapSelectionPolicy` / `MapTrackSegments` restructure, GL's state-only `GLOBAL_CONTEXT.md` + AGENTS.md consolidation landed; build green every hop, scoped tests green, survival gate proven, both Ask findings closed (B1's twin tie now deterministic via a 1 ms `startTimeMs` nudge in `duplicateTrack`); push + PR and device smoke test pending | 2026-09-11 20:04 | 2026-09-11 20:57 | active |
| TracksImport | Derived map track visibility (drops the persisted `visibleOnMap` flag), shared `MapSelectionPolicy` (tracks + markers), `MapTrackSegments` rendering extraction, GPX off-route cleanup harness + 4 test classes | 2026-09-11 20:28 | 2026-09-11 20:28 | active |
| Tasker | External automation bridge — publishes the boat's water state (push broadcast on toggle + query/response pair) with `TrackRecordingService` as the state holder; architecture approved, implementation not started | 2026-09-12 08:50 | 2026-09-12 08:50 | active |

## Global Todos

- [ ] Validate the intermittent Overpass-outage theory — confirm the coastline OSM fetch failures are transient (succeeded 13:52, failing ~16:52 on 2026-06-08), not a persistent network / cert / IPv6 block. Quick checks: retry `bake-coastline` later; `curl -sk https://overpass-api.de/api/status`; race other mirrors.
- [ ] **Change direction arrow color by speed compliance** — arrow in heading-ahead display (↑/↗→/→) should reflect speed-vs-limit ratio: green ≤ limit, orange ≤ limit×1.4, red > limit×1.4
- [ ] **Three pre-existing unit-test failures block the full-suite gate** — `MarkerFilterMigrationTest` (2 cases, v7 pinned→icon axis migration) and `RegulationAggregatorTest` (1 case, keeps multiple SHOM zones regardless of type) fail at HEAD; verified 2026-09-12 by reproducing them with unrelated working changes stashed. Until someone owns them, `gradlew testDebugUnitTest` exits non-zero regardless of what a branch touches, so it cannot be used as a clean pre-merge gate.
- [ ] **Tasker water-state bridge — must be resolved before the power service becomes conditional (note for the Tasker feature revisit)** — `TrackRecordingService` answers `ACTION_QUERY_WATER_STATE` from a runtime-registered receiver and holds `lastKnownOnWater`; the Performance power work plans to run the service only while a keep-alive reason holds, at which point the query goes unanswered while the app is idle. Re-homing it to a manifest receiver reading the persisted `isWater` setting works but returns a **stale** value, since nothing updates that setting while backgrounded. Detail and options in `xTrack/Performance/260912_FEAT_PLN_Performance_power-management-centralization.md` §6 and `xTrack/Tasker/260628_FEAT_PLN_Tasker_tasker-water-state-integration.md`.

## Cross-Reference Docs
Docs available via `#doc read [name]` from any feature. Fuzzy-resolve searches this table.

| Doc | Owner Feature | One-Liner |
|-----|---------------|-----------|
| `color-scheme.md` | ColorManagement | Color tokens, palette, alias chains |
| `material-icons-standalone-guide.md` | Ui_General | How to add Material Symbols icons as standalone ImageVector .kt files |
