# Global Context — Routing Table

> State only — routing map, feature summaries, focus history, global todos, doc index. Rules and instructions live in `AGENTS.md`.

## Focus History
- [2026-09-19 09:54 UTC] Ui_Settings — one session that made the map's own drawing values into settings and then gave the heading aids their own controls. The first pass turned the three zone and shoreline strokes and the shoreline's two colours into `maro.properties` keys and settings (widths 10, 6 and 3 px, shoreline transparency 50 %, colours `#1545C0` / `#08805C`), moved eleven colour keys out of `colors.properties` on the rule that a colour parameterising a rendering behaviour is functional data rather than a palette token, gave the three draw functions their widths, colours and transparency as parameters with the baked `6f`, `3f` and half-strength alpha deleted, and put a width row in both zone cards plus a collapsible Appearance expander on the coastline; it shipped as `82b2b1d`, was rebased onto `origin/develop` and pushed as `217a405`, with a pull request open. The second pass gave the head arrow and the heading line thickness, colour and transparency as seven settings — `map.navigation.arrow.widthDp` 2.25, `.transparencyPct` 0 and `.followSpeedColour`, `map.navigation.line.widthDp` 1 and `.transparencyPct` 70, plus the two colours, the line's key having lost its packed `#4D` to its transparency — with an **Appearance expander under each item's own toggle**, the arrow's **Speed Colour** mode reading the track ramp through `quantiseKn` and `colorAt` and turning to the ramp's neutral tint while `gpsStale` is set, a never-disabled **Default colour** row, a dash proportional to the line's width, and a shaft inset meant to hide its round cap inside the head; four device-review corrections landed on it, and a colour-row tidy removed the Pick text from every `ColorRow` and wrote the rule into the guidelines. The arrow and line change built SUCCESS with the scoped run at 233 tests holding the six pre-existing properties-versus-`AppConfig` drift reds; the colour-row tidy has never compiled, three builds having died on a lock over `R.jar` held outside Gradle; and open are the cap-arrow repairs the review named — the inset's `sin` divisor, the head capped against a short arrow, and the inset extracted as a pure tested function — beside the device pass over both changes and the six drift reds themselves → xTrack/Ui_Settings/FEAT_HYD_Ui_Settings.md
- [2026-09-19 08:55 UTC] Tracks — one session on `feature/someMui` that re-cut the speed ramp's draw steps for a jitter-free low range and a smooth high one, then designed the limit colour language and left it on paper: the nine steps moved to 1.0 across families 1 to 8 and 2.0 for family 9, so 0–13 kn holds 7 shades where it had 19 and 13–70 kn paints 38 where it had 17, while `HEATMAP_MAX_FAMILIES` rose 8 → 9 — a bound the file had already outgrown, which had left the ninth family unread and everything above 32 kn painting family 8's saturated top, the block header beside the keys still saying eight; `AppConfig`'s default ramp and both heatmap test classes were reconciled with the shipped grid, rewriting the properties test onto the file's ceilings, steps and colours and moving the cap contract to family 9, which closed the two properties reds and left the known set at four, and the pass is committed as `624d1d9`; a second plan in design carries the 5 and 10 kn enforcement language — a 25 % tolerance band above each limit with the ceiling rounded up to the whole knot, so 7 and 13 kn, green below 5 and blue between, the warm chain above 15 untouched, and the legend rows that would move onto the limits — decided in discussion, captured in full and not applied → xTrack/Tracks/FEAT_HYD_Tracks.md
- [2026-09-19 06:50 UTC] UI_Map — one session on `feature/someMui` that made the marker's scale a configuration surface and settled the map's zoom range at 11–20: the range moved after a 30.0 test value, the sprite's growth exponent left the code for `map.marker.size.zoomExponent` (0.45 → 0.35, which takes the boat from 388 dp to 223 dp offshore at level 20 while leaving the mid levels all but untouched), and the base pair followed as `map.marker.size.boatBaseDp` (36.8) and `map.marker.size.dotBaseDp` (9.2) — a 15 % raise on 32 / 8, the dot still a quarter of the boat, both clamped 1–128 on parse — so `ZOOM_EXPONENT`, `BOAT_BASE_DP` and `DOT_BASE_DP` are deleted and the sprite, the wizard's crosshair and the cap arrow read the settings while the reference zoom, the coast-shrink pair and the arrow's speed clamps stay code; both passes ran the `#implement` pipeline with `apk-build.bat` SUCCESS and no warnings and the scoped `ui.map` + `config` run steady at 213 tests with only the six properties-versus-default reds, and the marker-sizing doc — which had a wrong reference zoom, wrong base sizes and a pointer to a line holding none of them — was rewritten to the real curve with its tunables pointing at the keys; the device pass and the review's findings (the crosshair now 13 % under the sprite it replaces, the 4 : 1 ratio a comment rather than a guard, and no shipped-token test for the three keys) stay open → xTrack/UI_Map/FEAT_HYD_UI_Map.md
- [2026-09-18 20:58 UTC] Markers — one session on `feature/mrkrs` (`35c66c0` off `origin/develop`) that gave a selected marker's dashboard open its own framing: the corridor fits 50% and the circle zone 30% of the smaller displayed map dimension, both centred on the map, and a pin frames the default zone's 200 m footprint at the zone's share and never below the current zoom, the three numbers living as `marker.focus.*` in `maro.properties` behind `AppConfig` and the fit being a pure `MarkerFocus` helper with eight JVM tests; the navigate flow kept its single camera ownership while one remembered-id-guarded effect frames the map tap and the dashboard's Prev/Next, so `MarkerDrawer.kt` and `OverlayLayer.kt` stayed untouched, the 8–18 zoom bounds gained one home and `UserMarker.bboxOf` replaced the private metres-to-degrees conversion; shipped through the `#implement` pipeline with `apk-build.bat` SUCCESS, `MarkerFocusTest` 8/8 and the scoped `ui.map` + `config` run at 197 tests with only the five pre-existing properties-versus-code-default reds; the device pass and the Ask pass's properties-parse finding stay open → xTrack/Markers/FEAT_HYD_Markers.md
- [2026-09-18 19:27 UTC] Ui_Settings — one session that gave the regulated zone polygons a user-owned transparency pair: a **Regulated Zones Appearance** expander now leads the Regulated zones card of the Layers tab with one two-thumb **Transparency** row (outline left / fill right, 5% snap, commit on release) whose pair mirrors the 300 m band's 20/80, persisted as `regulatedZoneFillTransparencyPct` / `regulatedZoneBoundaryTransparencyPct`, and `drawRegulatedZones()` derives both alphas through the new shared `transparencyPctToAlpha` (which `drawZone300` now uses too) instead of its baked `0x30000000` fill and literal `alpha = 200`, the per-type `RegulationZoneColor` pair being deleted with it; the subject-free `settings_transparency_border_fill_label` / `settings_transparency_border_fill_value_fmt` replaced the 300 m and marker-halo duplicates while the tracks row's own differently-worded key stayed put, and the plan review caught the key collision, the missing `last*` seeds and the guard comparison the setting needed to survive a slider commit. `apk-build.bat` SUCCESS with no new warnings, the scoped `ui.map` + `config` run at 189 with only the five pre-existing properties-versus-`AppConfig` reds and five new green tests in `MapOverlayRendererTest`; the branch was created from `origin/develop` at the end of the session, and the device pass and the hydration stay open → xTrack/Ui_Settings/FEAT_HYD_Ui_Settings.md
- [2026-09-17 20:28 UTC] Ui_Settings — one session on `feature/hide-law` that shipped the Layers tab's "Show Land/Water Icon" section: a persisted `showLandWaterIcon` (default shown, an absent key meaning shown) hides the map row's 🌊/🏔️ square, which the row's own `Arrangement.spacedBy` reflows, and the locked-screen mirror's hard-coded three-square offset became `lockMirrorStartOffset()` — 156 dp with the square, 106 dp without, its KDoc naming the row's order as the count's dependency; `apk-build.bat` SUCCESSFUL on both hops of the `#implement` pipeline with the Ask hop's five prose findings closed in a second Code hop, the feature summary, hydration and front-matter date baked, and `ab3e0fa` committed and pushed; the device judgement of the mirror stays open → xTrack/Ui_Settings/FEAT_HYD_Ui_Settings.md
- [2026-09-17 19:45 UTC] WorkflowImprovement — one session on `feature/some-wflw` that rewrote the rulebook's governance: a five-tier legend (🛑 ⛔ 💬 🧹 🟢) landed and every rule was re-marked, ending at 20 with the delegation rule that makes a user-invisible implementation choice the agent's, `#rule` was rebuilt as bare legend / single-tier reload / `all` staleness verdict with the registry of ids declined, the report bullet now demands the problem and the fix in plain words with an ELIJP and its twenty-word form, the five action classes carrying a verdict line were named in the legend zone and the `Directive trace` sentence joined the hydration shape, and the halves were made live with `#doctor` (s), the `#review` sweep and the walk page's documented `Closed` semantics plus one rendering rule; `GIT_WORKFLOW.md` completes a refused command after the move and bare `#move` picks from a branch list; four plans sit in design — the lifecycle, the walk, the trim parked at 2.4%, and the reply-shape plan — with six shipped plans plus two Documentation ones named as retirement candidates; the delegation rule is blocked on rescoping `ASK, DON'T GUESS`'s first clause → xTrack/WorkflowImprovement/FEAT_HYD_WorkflowImprovement.md
- [2026-09-17 14:27 UTC] Ui_General — one session on `feature/twks-props` that re-assessed the selected-item dashboard's autoclose and replaced it with two rules: a surface wanting the dashboard's own slot (the wizard, the other selected-item dashboard) or a change of the world the open item's Prev/Next walks — the menu, settings and both lists keep the selection, `panelOwnsRegion` standing the four detail slots down while a panel is open, the one-item guard sitting inside `openTrackDetail`/`openMarkerDetail`, and the ten referential callbacks closing through two named helpers over the pure `scopeClosed()` guard with four tests; shipped through `#implement` (`apk-build.bat` SUCCESS on both hops, the scoped `ui.map` + `config` run at 134 with only the five pre-existing `maro.properties`-versus-`AppConfig` reds), the Ask hop's findings folded as F1–F4 and closed in a second Code hop, and the plan's outcome answering a device report whose cause was the menu button one slot above the fan anchor, the fan itself never having closed it → xTrack/Ui_General/FEAT_HYD_Ui_General.md
- [2026-09-17 10:03 UTC] Tracks — one session on `feature/twks-props` that split the menu's render control back into two axes and then answered two follow-up requests: the three-way switch became the twin box — chips **Arrows** and **Colours**, each on or off by itself under "Display Tracks with:", so four combinations where the triple gave three — with every fork arbitrated rather than assumed (writer per chip, both controls moved to `ui/components` with `SegmentedRow`, the retired triple migrated once and wiped in the same edit, a fresh install opening on Colours without chevrons, all four states valid, the caption and label keys redrawn, the enum and both test names deleted outright); the store is now `track_arrows` (off) and `track_colours` (on) with `trackRenderPlan`, `selectionBandedAfterTap` and the rebuild keys reading the flags, and the plan was written, reviewed twice and shipped through the `#implement` pipeline with the Ask findings folded into its §10 and its pointers landed; the second pass then swapped the drawer header's eye for the standalone **Speed** vector, which keeps the tint readout the legend's emoji could not, and narrowed the legend gate to follow the open track's fill, so flipping a selection gold hides the scale while other banded tracks stay on the map; `apk-build.bat` SUCCESS on both passes with the scoped `ui.map` + `config` run at 130 tests and only the five pre-existing `maro.properties`-versus-`AppConfig` reds, the device pass owed over the four combinations, the new glyph, the new gate and the migrated keys, and the migration's pure mapping the one unpinned decision → xTrack/Tracks/FEAT_HYD_Tracks.md
- [2026-09-16 17:10 UTC] RegulatedZones + Navigation — one session on `feature/tracking-more` that fixed two position-source faults reported on device: the bottom-left tag stack stopped riding the layer's visibility — its set is derived from the Zone categories settings, the stack is fed the marker point (`MapContent`'s `boatPosition` parameter deleted), and the band sign was given its own band result at that point (`markerInZone300`), so the dashboard keeps the boat while the stack answers what the user is looking at — and the dashboard's shore pipeline moved onto `dashboardPositionFor(marker, boat, gpsMode)`, the boat in GPS mode and the marker in demo, while `syncCenterFromBoat` made the user own the map centre whenever auto-follow is suppressed, with `recenterNow()` and the resume timer handing it back in-frame; two plans written and reviewed, a nine-item walk closed with four decisions, `DashboardPositionTest` green and `apk-build.bat` SUCCESS, device pass open → xTrack/RegulatedZones/FEAT_HYD_RegulatedZones.md
## Routing Map
| Keyword | Feature File |
|---|---|
| documentation, docs, readme, faq, setup, git_workflow, maro_architecture | xTrack/Documentation/FEAT_DSC_Documentation.md |
| workflow, clinerules, xtrack, commands, memory, #doc, doccommands | xTrack/WorkflowImprovement/FEAT_DSC_WorkflowImprovement.md |
| zone300, 300, bande 300m, water-only | xTrack/Coastline/FEAT_DSC_Coastline.md |
| depth, bathymetry, depthmapping, baro, seafloor, soundings, litto3d, shom, emodnet | xTrack/DepthMapping/FEAT_DSC_DepthMapping.md |
| coastline, trait de côte, fourmigue, hazard, obstruction, balisage, danger_isole, aton, seamark, lighthouse, reef | xTrack/Coastline/FEAT_DSC_Coastline.md |
| dashboard, ui, layout, hud, display, screen | xTrack/Ui_Dashboard/FEAT_DSC_Ui_Dashboard.md |
| gps, gpsplugin, gps mode, demo mode, heading, course, compass, location, geolocation | xTrack/GPS/FEAT_DSC_GPS.md |
| mapdisplay, map display, map, layer, depth layer, color depth, orientation | xTrack/UI_Map/FEAT_DSC_UI_Map.md |
| performance, battery, gps-tune, adaptive, compass-gate, map-refresh, battery-drain, power management, keep screen on, keep awake, screen-on, wakelock, screen lock, lock delay, movement gate | xTrack/Performance/FEAT_DSC_Performance.md |
| bake, baking, bake-script, bake-bat, apk-bake, apk-build, apk-deploy, deploy, prebake-pipeline, bake-env | xTrack/BakeNormalization/FEAT_DSC_BakeNormalization.md |
| depthsafety, depth-safety, danger-depth, shallow, grounding, isobar precision, isobath precision, depth alert, depth overlay, water-only | xTrack/DepthSafety/FEAT_DSC_DepthSafety.md |
| app-bak-flow, app-back-flow, back, back button, back handler, exit, double-back, press back, touch lock, splash guard | xTrack/Ui_General/FEAT_DSC_Ui_General.md |
| filters-link, filter link, linked filters, decouple list map filter, map filter referential | xTrack/Ui_General/FEAT_DSC_Ui_General.md |
| selected-item dashboard, selected item dashboard, dashboard close, close conditions, marker detail drawer | xTrack/Ui_General/FEAT_DSC_Ui_General.md |
| settings, preferences, config, scroll, options, land/water icon, show land/water icon, earth/water, zones transparency, regulated zone transparency, zone appearance | xTrack/Ui_Settings/FEAT_DSC_Ui_Settings.md |
| regulation, regulated zones, regulatedzone, regulation zone, speed zone, speed limit, anchoring, SHOM regulation, shom reg, maritime regulation, regulatory zone, réglementation maritime, zone réglementée, arrêté maritime, DIRM, cap d'antibes, lérins | xTrack/RegulatedZones/FEAT_DSC_RegulatedZones.md |
| arclayout, arc, arc-menu, layer-toggle, multi-btn, layer, toggle, fan-out | xTrack/ArcLayout/FEAT_DSC_ArcLayout.md |
| color, colour, color management, color-scheme, colors.properties, colour palette, colour scheme, colours, theme | xTrack/ColorManagement/FEAT_DSC_ColorManagement.md |
| zonetile, zone tile, zone info, zone ahead, zone cone, speed zone, speed zone display | xTrack/ZoneTile/FEAT_DSC_ZoneTile.md |
| boat, trace, trip, boat-trace, boat-tracing, track, tracks, recording, port-salis, journey | xTrack/Tracks/FEAT_DSC_Tracks.md |
| markers, pin, circle, corridor, usermarker, user marker, where am i | xTrack/Markers/FEAT_DSC_Markers.md |
| menu, menu drawer, hamburger, track drawer, position source, menu overlay | xTrack/Ui_Menu/FEAT_DSC_Ui_Menu.md |
| navigation, nav, heading arrow, direction arrow, speed arrow, cap arrow, course arrow, auto-show, auto show, redisplay on approach, zone reveal, demo auto-show | xTrack/Navigation/FEAT_DSC_Navigation.md |
| checkdev, dev branch, branch health, ahead behind, workflow hygiene | xTrack/CheckDev/FEAT_DSC_CheckDev.md |
| health, diagnostics, crash reporting, telemetry, memory monitoring | xTrack/Health/FEAT_DSC_Health.md |
| mergitur, merge, three-branch integration | xTrack/Mergitur/FEAT_DSC_Mergitur.md |
| tracks-import, tracksimport, gpx import cleanup, map track visibility, selection policy, visibleonmap | xTrack/TracksImport/FEAT_DSC_TracksImport.md |
| tasker, water state, intent broadcast, query receiver, automation | xTrack/Tasker/FEAT_DSC_Tasker.md |

## Feature Summaries

| Feature | One-Liner | Created | Modified | Status |
|---------|-----------|---------|----------|--------|
| **ColorManagement** | **Centralised colour palette — all tokens in colors.properties with alias interpolation, documented in color-scheme.md; shared UI colour tokens de-settings-ified (uiSettings* → ui*, e.g. uiBackground/uiTextPrimary/uiAccent/uiDividerColor); the map's chrome tokenised into two families, `ui.map.toggle.*` and `ui.map.overlay.*`, over one shared `ui.map.surface.inactive`; the map's surfaces then collapsed onto one `ui.map.surface.*` block with `ui/map/MapSurface.kt` as the only painting path — nine surfaces, one colour accessor, a content-only fade, one corner and one outline — and every alpha key in all three palette files now states `1.0 = fully opaque, 0.0 = fully invisible`; the bottom-left zone stack then joined that family on its active face, and the one-home-per-fact rule was applied — the family's caller and exception lists live in `MapSurface.kt` alone, `colors.properties` is the source of truth for every value with `AppConfig`'s defaults and every doc moved onto it** | **2026-06-16 14:05** | **2026-09-16 15:00** | **active** |
| **Documentation** | **README, FAQs, setup guides, architecture docs, and plans cleanup** | **2026-06-11 06:42** | **2026-09-04 22:36** | **active** |
| WorkflowImprovement | xTrack #command system and rule-book governance. Earlier passes, 2026-06 to 2026-09-13 (`feature/wrKFl`): the command registry single-sourced with `cmd_help.md` derived and GIT_WORKFLOW demoted to detail, `#checkout` de-registered, `#bake` explicit-only with `#commit` offering a stale bake, `#archive` shipped with the xxArchive tier and `#doctor` extended through checks p, q–r, command-flow and its four commands shipped with pages, the walk stack shipped with `#done` retired and both gates reading any open level, and the adapter clean-up moved the file templates to `docs/xtrack-templates.md`. 2026-09-17 (`feature/some-wflw`): the rulebook gained a five-tier legend and every rule was re-marked, ending at 20 with the delegation rule that makes a user-invisible implementation choice the agent's; `#rule` was rebuilt as bare legend / tier reload / `all` staleness verdict, the registry of ids declined; the report bullet now demands the problem and the fix in plain words, with an ELIJP and its twenty-word form; enforcement became three tiers — command refusals, a verdict line over five named action classes, and the `#review` sweep — with one `Directive trace` sentence written into the hydration at each bake, and `#doctor` gained check (s) that every rule bullet wears a tier glyph; the walk page gained documented `Closed` semantics and one rendering rule, the git exit completes a refused command after the move, and bare `#move` picks from a branch list. Since 2026-09-19 (`feature/archive`) a plan in design specifies **`#archive sweep`** — enumerate a feature's plans with their associated docs, print a description, relevance and status per row, recommend an action, and dispose by leave, follow-up or archive — decisions D1–D13 settled, nothing built yet; the feature's first real archive pass then retired six shipped plans into `xxArchive/` with Outcomes and index rows, dropped eleven pointers, detached six `## Docs` entries and attached the eleven plans that had been on disk unlisted. | 2026-06-03 00:00 | 2026-09-19 09:31 | active |
| DepthMapping | Bathymetry / depth mapping from Litto3D, SHOM, EMODnet sources | 2026-05-10 00:00 | 2026-09-04 22:36 | active |
| Coastline | Coastline extraction, spatial indexing, isOnWater, hazard rings, unified data store; eastern bound extended to Menton (7.55°E), single region ID via BuildConfig | 2026-05-10 00:00 | 2026-06-23 07:08 | active |
| Ui_Dashboard | Main dashboard UI layout and HUD information display | 2026-05-15 00:00 | 2026-09-04 22:36 | active |
| GPS | GPS plugin with demo mode, heading/COG compass, geolocation, auto-follow spring-back hold | 2026-05-10 00:00 | 2026-09-04 22:36 | active |
| UI_Map | Map rendering, depth color layer, orientation overlay, boat marker offset; deterministic overlay z-order (tile→base→tracks→markers) via OverlayZOrder.reorder; marker filter drives map overlay (map renders from filtered list, panel auto-closes when filtered out); menu open keeps the marker+track dashboard (a panel over the map, not a slot occupant) and the fan keeps it too — the close conditions live in Ui_General; list-context stacking removed (close returns to map, Prev/Next navigates); **inspect mode (drag-to-select by proximity from the marker point) designed and in design, not implemented — ⊕ toggle gated on having something inspectable, one radius derivation feeding both the ring and the pick gate, the layer-visible map-filtered candidate set, a candidate overlay the mode owns, a quiet-clock pick that never moves the camera, and a frozen distance ladder walked by one inspect cursor that can cross between marker and track cards → xTrack/UI_Map/260917_FEAT_PLN_UI_Map_inspect-mode.md**; the zoom range then settled at 11–20 and the marker's scale became configuration — `map.marker.size.zoomExponent` (0.35) plus the base pair `map.marker.size.boatBaseDp` (36.8) and `map.marker.size.dotBaseDp` (9.2), a 15 % raise clamped 1–128 on parse replacing the deleted `ZOOM_EXPONENT`, `BOAT_BASE_DP` and `DOT_BASE_DP` constants, so the sprite, the wizard's crosshair and the cap arrow share the settings | 2026-05-10 00:00 | 2026-09-19 07:35 | active |
| Performance | Battery optimization, adaptive GPS tuning, compass gate, map refresh; **power management** (screen-hold policy + keeper in `data/power/`, movement gate + interaction grace, lock-phrased toggle, battery-prompt consolidation into `AppSettings` with a shared `BatteryExemption` predicate, sourced reading freshness via `SpeedFreshness`, demo mode grace-governed) | 2026-05-20 00:00 | 2026-09-12 10:57 | active |
| BakeNormalization | APK bake/build/deploy pipeline and prebake data processing | 2026-06-01 00:00 | 2026-09-04 22:36 | active |
| DepthSafety | Danger depth alerts, shallow water grounding prevention, isobath precision | 2026-06-03 00:00 | 2026-09-04 22:36 | active |
| Ui_General | App-lifecycle UX: back-to-exit guard, edge-to-edge rendering, WindowInsets, list normalization (ListOverlayScaffold, sort, swipe-to-delete, per-type custom sort fields, localized EN+FR), list-detail navigation (scroll preservation, prev/next, exit conditions), drawer delete undo (snackbar + undo + reopen), notification lifecycle (foreground notification follows recording state, 3-choice exit dialog, service-owned recorder), landscape menu drawer (scroll-when-overflow, overscroll suppressed when content fits), top-left icons (GPS→tracking→land/water, GPS click-to-toggle, 🐾, idle dot red), menu drawer rows (Tracks/Markers captions, chevron opens first filtered/sorted item, disabled when none), touch-lock splash guard (📵 toggle, double-tap zoom, status.lock.* tokens; renamed from screen-lock 2026-09-12 so `screen lock` belongs to the Performance power feature); compact track/marker list cards (desc/comment lineHeight 14sp, reduced v-padding, header→title separator removed); drawer footer pinning + dynamic animated height (card-probe); drawer vertical rhythm (12dp card padding, header vertical padding 12dp, footer 10dp rhythm); landscape drawer/settings sizing (portrait width × ui.landscape.panel.widthScale 1.2, menu 75%); scrim matrix (0.50 menu/settings/lists, none detail+wizard, transparent fan/lock); dashboard close (the selected-item dashboard closes only when the wizard or the other dashboard wants its slot, or when the world its Prev/Next walks changes — the menu, settings and both lists keep the selection, the four detail slots standing down while a panel is open); list-count-display (Track History title "· N" + menu Tracks/Markers counts left of chevron, live track excluded); marker-filter-remove-geometry (markers filter Geometry axis removed; icon/pinned/origin remain, origin ungated); track-filter-date-range (Track Date Range filter extended+reordered to 7 options Last week→Last 6 month with All last + default, THIS_YEAR dropped); dashboard-close-conditions (the two close rules shipped 2026-09-17 with `panelOwnsRegion`, the one-item guard in both openers and `scopeClosed()` under test) | 2026-06-08 16:43 | 2026-09-17 14:27 | active |
| Ui_Settings | Settings page UI — 4 tabs; row-family normalization R1–R8 (container-owned horizontal inset; SectionHeader/SectionDivider/Card/NestedCard/CardDescription; ToggleRow/SliderRow/RangeSliderRow/SegmentedRow/ColorRow/ColorPairRow; CategoryToggleGroup) plus section-title colour promotion and tab-finalization phase 1 (18sp sentence-case titles, M3 SecondaryScrollableTabRow cells, single 14dp section rhythm); properties/header/drawer-measurement normalization (ui.properties → typed AppConfig tokens, shared DrawerHeader, wrap-content drawers, marker belongs-to-track row); C12 OverlayLayer parameter-object collapse (six @Immutable bundles, 89→60 params); transparency paradigm (0=opaque / 100=invisible, two-thumb Border·Fill, two-depth low-depth warning) now extended to the regulated-zone polygons — the Regulated Zones Appearance row leading the Regulated zones card, the persisted `regulatedZoneFillTransparencyPct` / `regulatedZoneBoundaryTransparencyPct` pair defaulting to the 300 m band's 20/80, and one shared `transparencyPctToAlpha` behind both renderers; confirm-dialog normalization COMPLETE (one ConfirmDialog on the overlay ladder, dialog-owned scrim, in-drawer merge + batch delete hoisted, ladder dialog plumbing deleted; P4 scrims are hard on/off toggles — the ladder scrim yields while a dialog is visible, so dims never stack); show-land-water-icon (the Layers tab's "Show Land/Water Icon" section and its persisted `showLandWaterIcon`, default shown, hides the map row's 🌊/🏔️ square — the row reflows itself and the locked-screen mirror's square count moved into one helper); stroke-widths-and-shoreline-colours (the map's own drawing values became settings and changed file: six values as both a `maro.properties` key and a settings control — `map.coastline.widthPx`, `map.coastline.transparencyPct`, the two coastline colours, `map.zone300.boundary.widthPx` and `map.regulatedZone.outline.widthPx` — seeded from `AppConfig` and clamped once where the setting is read; eleven colour keys re-homed from `colors.properties` to `maro.properties` with names unchanged, the isobath and regulated-zone families staying palette keys; the three draw functions given their widths, colours and transparency as parameters with their baked `6f`, `3f` and half-strength alpha deleted, all six values seeded into the overlay tracker and its three redraw guards; a width row in both zone Appearance cards and a collapsible Appearance expander on the coastline holding width, Transparency and one mainland/island colour pair; the coastline width pair collapsed to one key; and `color-scheme.md` trimmed to key names plus a pointer) | 2026-06-09 15:28 | 2026-09-19 11:03 | active |
| **Navigation** | **Navigation aids — heading/speed arrow and direction line on map overlay; auto-show / re-display of regulated zones + 300 m band on approach (demo + GPS); the dashboard's position source is now mode-aware (`dashboardPositionFor` — the boat in GPS, the marker in demo) and the map centre is owned by the user whenever auto-follow is suppressed** | **2026-06-10 08:40** | **2026-09-16 17:10** | **active** |
| **RegulatedZones** | **Maritime regulatory zones — multi-source normalization (SHOM INSPIRE + IGN Natura 2000), sealed classification, 8-category icon mapping, keyword-driven display logic; the bottom-left icon stack paints through the map surface family, one weight per tag, and since 2026-09-16 it answers for the marker — its tags come from the Zone categories settings rather than the layer's visibility, and the 300 m sign carries its own band result** | **2026-06-11 18:00** | **2026-09-16 17:10** | **active** |
| **ArcLayout** | **Layer toggle arc menu — pure-Compose semicircle fan-out with layer toggles, FanLayout framework, fixed child centering using effectiveTheta=180/currentCount; control-stack button swap (Add Zone above fan)** | **2026-06-13 07:34** | **2026-09-06 21:19** | **active** |
| **ZoneTile** | **Zone information tiles and map overlay rendering — zone-ahead cone/line, speed zone display, ETA, zone state management; the zone-info text card now reads the map overlay family (`ui.map.overlay.*`) over the shared `ui.map.surface.inactive`** | **2026-06-17 09:45** | **2026-09-16 06:31** | **active** |
| **Tracks** | **Boat movement tracking: record from GPS fixes, persist as protobuf, render on the map under two independent axes (Arrows, Colours — four combinations, neither of them implying the other), GPX export, track history UI, idle auto-markers, direction chevrons; finalize durability + data-derived end/idle/nav (schema 4); marker-track single reference and auto-marker lifecycle; export naming + import modes; live-track paint regression; resume-confirm-backup; selected-track speed heatmap and its render-mode generalization shipped — every stored track banded from the ramp under per-type widths read from `maro.properties`, the selection opaque, cased and drawn above every other trace, the drawer eye scoped to the selection's fill and persisted; the outlines plan's three items shipped on 2026-09-15 (the arrowhead rim at the line's weight, the legend gate on banded strokes, the strip's chrome), the speed scale then went one toggle button wide on the row's own gutter, on 2026-09-16 the toggle row and both overlay cards moved onto the `ui.map.toggle.*` and `ui.map.overlay.*` families over a shared `ui.map.surface.inactive`, the speed scale itself became one control with two faces on one anchor — the card collapses to a 44 dp square on the row's recipe carrying the ⏱ stopwatch, its state persisted as `trackLegendExpanded` with the display gate untouched — and its labels then stopped breaking inside the digit pair, centred in the full bar-to-border space; since 2026-09-17 the axes are the menu's own two chips under "Display Tracks with:" — the retired three-way switch, its enum, key, strings and both test names are gone, the store is `track_arrows` (off) and `track_colours` (on) with the old value migrated once and wiped, the header toggle wears the standalone `Speed` vector, and the scale follows the open track's fill → xTrack/Tracks/260917_FEAT_PLN_Tracks_render-axes-split.md** | **2026-06-15 00:00** | **2026-09-17 10:03** | **active** |
| **CheckDev** | **Dev-branch health monitoring — remote branch state, ahead/behind analysis, workflow hygiene validation** | **2026-06-20 11:42** | **2026-06-20 11:42** | **active** |
| **Health** | **Application health monitoring — diagnostics, crash reporting, memory/performance telemetry** | **2026-06-20 11:42** | **2026-06-20 11:42** | **active** |
| **Markers** | **User-defined map markers (Pin, Circle, Corridor) with sea-distance-gated proximity matching, percentage-based sort scoring, and on-demand "where am I?" query — icon/pin decoupling implemented (icon pure POI, pin real persisted flag mirroring tracks); pin-halo-rendering implemented (static halo ring pinned white/unpinned light-blue, corridor always-on line + under-line halo, selected-marker gold via selectedMarkerId, MarkerAppearance/MarkerHalo split); the select framing then left its single 64 px border fit for a stated share of the smaller displayed map dimension — corridor 0.50, circle zone 0.30, and a pin through the default zone's 200 m footprint at the zone's share, never below the current zoom — tuned by `marker.focus.*` in maro.properties, computed by the pure `MarkerFocus` helper and applied by one camera owner for the list open, the map tap and the dashboard's Prev/Next; the Where-Am-I pipeline then became one query per tap whose recording note survives a close, the debug rays leaving with the drawer or the switch, the debugger passed per call, one resolve body with the two empty cases kept apart, and the build-time `marker.debug.rays.enabled` hook retired; the boat's tap zone followed as a fixed radial circle with a gold flash beating beneath the sprite, all six values read from `map.marker.tap.*`, and the card's close rule gained a one-finger pan gated so a pinch cannot trip it — the device pass is owed** | **2026-06-22 11:52** | **2026-09-18 22:00** | **active** |
| **Ui_Menu** | **Hamburger menu drawer — position source, track recording, marker management sections; right-side sliding panel via OverlayLayer/DrawerSlot; track/marker action normalization (shared list/detail card, double-click inline edit, chevron tappable gutter, delete = swipe/header-trash); menu render migrated to the Settings model (shared SectionHeader/SectionDivider/CardArea/ToggleRow in ui/components, 14dp section rhythm, sentence-case titles reusing settings_section_*)** | **2026-07-05 06:57** | **2026-09-11 15:23** | **active** |
| Mergitur | Three-branch integration COMPLETE into feature/mergitur (TR `735cd29` → TI `e4d56f6` → GL `431d165` on base `025e1bc`, tag `pre-mergitur`): TR's ConfirmDialog + resume-backup re-seated onto TI's derived visibility / `MapSelectionPolicy` / `MapTrackSegments` restructure, GL's state-only `GLOBAL_CONTEXT.md` + AGENTS.md consolidation landed; build green every hop, scoped tests green, survival gate proven, both Ask findings closed (B1's twin tie now deterministic via a 1 ms `startTimeMs` nudge in `duplicateTrack`); push + PR and device smoke test pending | 2026-09-11 20:04 | 2026-09-11 20:57 | active |
| TracksImport | Derived map track visibility (drops the persisted `visibleOnMap` flag), shared `MapSelectionPolicy` (tracks + markers), `MapTrackSegments` rendering extraction, GPX off-route cleanup harness + 4 test classes | 2026-09-11 20:28 | 2026-09-11 20:28 | active |
| Tasker | External automation bridge — publishes the boat's water state (push broadcast on toggle + query/response pair) with `TrackRecordingService` as the state holder; architecture approved, implementation not started | 2026-09-12 08:50 | 2026-09-12 08:50 | active |

## Global Todos

- [ ] Validate the intermittent Overpass-outage theory — confirm the coastline OSM fetch failures are transient (succeeded 13:52, failing ~16:52 on 2026-06-08), not a persistent network / cert / IPv6 block. Quick checks: retry `bake-coastline` later; `curl -sk https://overpass-api.de/api/status`; race other mirrors.
- [ ] **Change direction arrow color by speed compliance** — arrow in heading-ahead display (↑/↗→/→) should reflect speed-vs-limit ratio: green ≤ limit, orange ≤ limit×1.4, red > limit×1.4
- [ ] **Three pre-existing unit-test failures block the full-suite gate** — `MarkerFilterMigrationTest` (2 cases, v7 pinned→icon axis migration) and `RegulationAggregatorTest` (1 case, keeps multiple SHOM zones regardless of type) fail at HEAD; verified 2026-09-12 by reproducing them with unrelated working changes stashed. Until someone owns them, `gradlew testDebugUnitTest` exits non-zero regardless of what a branch touches, so it cannot be used as a clean pre-merge gate.
- [ ] **Tasker water-state bridge — must be resolved before the power service becomes conditional (note for the Tasker feature revisit)** — `TrackRecordingService` answers `ACTION_QUERY_WATER_STATE` from a runtime-registered receiver and holds `lastKnownOnWater`; the Performance power work plans to run the service only while a keep-alive reason holds, at which point the query goes unanswered while the app is idle. Re-homing it to a manifest receiver reading the persisted `isWater` setting works but returns a **stale** value, since nothing updates that setting while backgrounded. Detail and options in `xTrack/Performance/260912_FEAT_PLN_Performance_power-management-centralization.md` §6 and `xTrack/Tasker/260628_FEAT_PLN_Tasker_tasker-water-state-integration.md`.
- [ ] **`#new` should warn before switching and offer to overwrite the local branch** (raised 2026-09-19, after a refused `#new extra-settings` blocked by a dirty state file): pin what the warning says and what overwrite means — a forced checkout discards working-tree changes, while `#move new` stashes instead — then land it in `AGENTS.md` §7b and `docs/cmd_help_git.md`.
- [ ] **Convert the map's paint code from device px to dp, as one complete pass** (agreed 2026-09-19, held out of the Ui_Settings stroke-width change so the two stay tellable apart): every width, offset, padding and dash length, with the converted defaults reproducing the current 3× look exactly and a visual check at a different density, since that is the only check that can show the conversion is right. The hazards a partial conversion runs into — `MarkerHalo`'s ring-versus-padding composition, the isobath base-plus-bonus arithmetic, and the dash patterns — are written up in `xTrack/Ui_Settings/260919_FEAT_PLN_Ui_Settings_stroke-widths-and-shoreline-colours.md` §7.

## Cross-Reference Docs
Docs available via `#doc read [name]` from any feature. Fuzzy-resolve searches this table.

| Doc | Owner Feature | One-Liner |
|-----|---------------|-----------|
| `color-scheme.md` | ColorManagement | Color tokens, palette, alias chains |
| `material-icons-standalone-guide.md` | Ui_General | How to add Material Symbols icons as standalone ImageVector .kt files |
