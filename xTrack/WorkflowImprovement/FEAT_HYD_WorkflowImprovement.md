# Context Hydration — WorkflowImprovement — 2026-09-19

**Last Bake:** 2026-09-19 09:31 UTC

**Directive trace:** no covered action stopped since the last bake: no dependency was added, no machine-shaped data file was opened, no device was touched and no work started unordered, the retirements having run on an explicit selection; the one bend recorded is a scripted substitution used for the `## Implemented` pointer drops because those lines are too long to patch reliably, and the earlier unsourced claim about list ordering was named by the session's own review sweep rather than left standing.

## State

The `#archive` command's designed extension — `#archive sweep` — is specified and unbuilt: the plan carries decisions D1–D13, separate state sets for plans and docs, the workflow and the doc chain, and its walk level closed by exhaustion on three answers — a leave writes nothing, the one nudge lives in `#doctor`, and the index gains `dropped` as a fourth status word. The feature's first real archive pass then ran: six shipped plans — command-flow, adapter-cleanup, walk-stack, boattrace-rename, core-directive-lifecycle and reply-shape-and-delegated-choices — were retired into `xxArchive/` with their Outcomes written, six index rows added, eleven `## Implemented` pointers dropped while every bare one-liner stayed, and six `## Docs` entries detached. The eleven plans that sat on disk unlisted are now attached, which makes the older shipped ones visible to the bake's retirement report for the first time, and the two collateral drifts the moves created — a live todo naming an archived plan and a live plan linking one — were re-pointed rather than left dangling.

## Target Files

- `xTrack/WorkflowImprovement/260919_FEAT_PLN_WorkflowImprovement_archive-sweep.md` — the plan in design, its walk closed
- `AGENTS.md` — §7b `#archive` row: the sweep forms and the reserved word
- `docs/cmd_help_archive.md`, `docs/cmd_help.md`, `docs/cmd_help_bake.md`, `docs/cmd_help_doctor.md`, `docs/xtrack-templates.md`
- `xTrack/WorkflowImprovement/FEAT_DSC_WorkflowImprovement.md` — the `### archive-sweep` section still owed

## Next Step

Implement Stage B in the plan's §7 order, beginning with the §7b row and the reserved word. Open residues: `ASK, DON'T GUESS`'s first-clause rescope, the ⛔ two-force reading, check (r)'s path-versus-name scope, and the Coastline-topic plan flagged inside the feature's `## Docs`.
