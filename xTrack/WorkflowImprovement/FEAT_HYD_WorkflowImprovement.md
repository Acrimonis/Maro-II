# Context Hydration — WorkflowImprovement — 2026-09-04

## State
Process simplification done and merged to develop; #bake sweep planned and sample-folded on WorkflowImprovement (6 done sections folded, gitting-it trimmed per live-trim rule). Fold rules baked into cmd_help_bake.md C8 + C13: done sections fold to one-liner + pointer (drop done todos, promote valid rules/key-files/docs); open sections keep a detailed summary + open todos + valid refs.

## Target Files
- `AGENTS.md` — Core Directives (MODE LOCK, WRITE-ONCE), §3, §7a, §7b
- `xTrack/GLOBAL_CONTEXT.md` — Focus History stack
- `docs/cmd_help_bake.md` — C8 + C13 (canonical fold criteria)
- `xTrack/*/FEAT_DSC_*.md` — migrated to sections + pointer-index

## Next Step
Run the full #bake sweep across the remaining FEAT_DSC files per C8/C13, then commit.
