**Last Bake:** 2026-06-13 13:36 (UTC+2)
**State:** All todos complete. Feature functional.

**Session summary:**
- Regulated zones icon changed: horizontal line → diagonal slash (45° `/`)
- All white button backgrounds changed from `0xCCFFFFFF` (80% opacity) to `ComposeColor.White` (fully opaque): anchor, arc toggles, settings, zoom
- Badge text centering fixed with `PlatformTextStyle(includeFontPadding = false)` to remove uneven font padding

**Target files:**
- `ArcLayoutToggle.kt` — reg icon, white backgrounds, badge centering
- `MapScreen.kt` — settings/zoom white backgrounds

**Next step:** None — feature complete.
