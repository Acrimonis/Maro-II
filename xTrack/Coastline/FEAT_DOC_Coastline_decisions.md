<!-- scope: feature -->
# Coastline — Architectural Decisions

> Auto-generated by `#doc update` 2026-06-23 07:08 UTC.
> Source: `FEAT_PLN_*` files + verified against [commit `635ca5a`](app/build.gradle.kts:30).

## Data Model

| Decision | Rationale | Source |
|----------|-----------|--------|
| Single region ID via `BuildConfig.REGION_ID` | Eliminates ~10 hardcoded `"nice-frejus"` strings; region change = one `gradle.properties` edit | `app/build.gradle.kts:30`, `gradle.properties:22` |
| Region ID `nice-menton` replaces `nice-frejus` | Eastern bound extended 7.31→7.55°E; cache namespaces auto-update | `gradle.properties:22` |
| `DepthSerializer.deserialize(InputStream)` | OOM fix: avoids double-memory (byte array + grid) during APK asset load | `DepthSerializer.kt` |
| `CoastlineGenerator.generate(regionId)` default = `BuildConfig.REGION_ID` | No caller needs to pass region explicitly | `CoastlineGenerator.kt:109` |

## Data Storage

| Decision | Rationale | Source |
|----------|-----------|--------|
| Downloads → `D:\.src\.data\` | Cross-project sharing (Maro-II and siblings); gitignored `/data/` stays clean | `tools/bake-litto3d.bat`, `tools/bake-emodnet.bat` |
| `apk-deploy.bat` syncs `D:\.src\.data\` → `data/app-assets\` | Baked assets must be in-project for Gradle `asset srcDir` packaging | `apk-deploy.bat` |
| Litto3D tiles moved out of `tools/litto3d_tiles/` | Tiles no longer tracked in git; `.gitignore` entry removed | `.gitignore:45` |

## Baking & Scripts

| Decision | Rationale | Source |
|----------|-----------|--------|
| LF→CRLF fix for all 17 `.bat` files | Batch files were corrupted with LF line endings (`'M'`, `'tlocal'`, `'ll'` syntax errors) | `tools/bake-*.bat`, `apk-bake.bat` |
| `%` → `%%` in `bake-litto3d.bat` | `for` loops require doubled percent signs in batch files | `tools/bake-litto3d.bat` |
| `apk-bake.bat` if/else dispatch with `if defined FRESH` | Guard prevents literal `%FRESH%` passthrough when variable undefined | `apk-bake.bat` |

## UI

| Decision | Rationale | Source |
|----------|-----------|--------|
| "Côte" / "Bande" button labels | Short labels fit the compact map overlay; tooltips explain full action | `MapScreen.kt` |

## Rendering

| Decision | Rationale | Source |
|----------|-----------|--------|
| Coastline segment index unchanged | Grid-based spatial index auto-includes new Menton segments; no code changes needed | `CoastlineSpatialIndex.kt` |
| Zone300 band regenerates from updated coastline | Band depends on coastline segments; "Bande" button triggers full rebuild | `Zone300Builder.kt` |
