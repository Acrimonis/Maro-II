---
name: Route
status: active
created: 2026-08-16 10:44
modified: 2026-09-21 20:34
---

# Feature: Route

**Description:**
Set a destination point on the map and have the app compute and trace the fastest water-constrained route from the boat's current GPS position. The route always stays on water and **never enters a priced speed zone while a way around exists** — that is an invariant of the search, not a price to tune — and where no way around exists, or an end already lies inside such a zone, the crossing is priced, taken and **reported as forced** with the zone's own name. The route's ETA is read off the line that is drawn, each leg at the limit in force over it.

**Engine — the tracer is the shipped engine, 2026-09-21.** The behaviours above belong to the feature, not to
an engine. **The obstacle-vertex tracer is both the design of record and what ships**: the corridor's own
shapes and corners as the graph, land and water below the gate as geometry no price can trade, zones and the
300 m band as prices, and a line taut by construction — its first cut producing the drawn line, built in
`spatial/taut/` and switched in at **one expression** in `MapScreen`. **The prebaked mesh and its A\*
(`MeshRouteEngine`) are deactivated and wired to nothing**, kept in the tree with their bake as the baseline,
and every rule below carrying a retirement marker is that engine's own. Its first `#implement` run returned
**revise** — six blockers, their corrections ordered as the design of record's §16 — so this is a first cut
and not the finished engine.

## Sections

### navigation-mesh

**Retired as the design of record (2026-09-20)** — the rules below govern `MeshRouteEngine` and its bake alone. The tracer's world is the corridor's own geometry and needs neither a mesh nor a bake, so nothing here binds it, and the sections above and beside this one hold the tracer's own requirements.

**Focus:** Prebake an adaptive navigation mesh (constrained Delaunay triangulation) for the corridor and serialize it as a bundled protobuf `.bin`; the app only loads and searches it. All builder code lives in `src/test` (prebake) so it can use `testImplementation` geometry libraries.

#### Rules
- App is a pure consumer: all mesh construction at build time (JUnit gated by -Dmaro.prebake), on-device search only
- Region = BuildConfig.REGION_LON_WEST/EAST + REGION_ID (`maro.region.id`, `nice-menton` today), the gradle props being the single source; the N/S bound and the 6 NM seaward band follow the depth envelope's own derivation (`DepthZoneMask.envelopeOf`)
- Reserve a forbidden/infinite-cost sentinel for future prohibited zones
- Bake-time geometry is file-based: RouteGeometry's prebake implementation reads coastline/regulated-zone `.bin` files directly via serializers (runnable in a JVM test)
- Refinement is adaptive but bounded: finer triangles near the coastline and the zone boundaries, coarser offshore, and the connectivity pass holds a **30 m minimum channel width** (agreed 2026-09-19, plan §14 C4)
- Depth gate, agreed 2026-09-19 (plan §18): water shallower than **2.5 m** is closed to routing — the mesh is simply not built there — so a passage no boat of this size can use is absent from the map entirely; the number is a depth value owned by the depth layer, `bake-depth` runs before `bake-route`, and a boat's own draught would replace it once such a value exists
- The route bake refuses by name when an input is missing — coastline, zone or depth `.bin` — rather than writing a partial mesh, and it runs after any coastline, depth or zone rebake: a mesh carries nothing that would detect a stale input (agreed 2026-09-19)

#### Mesh schema (protobuf, packed — decided 2026-09-19, walk item 1)
- Packed primitive arrays with CSR offsets, hand-built like `CoastlineSerializer` and `DepthSerializer`: `pointsLat[]`, `pointsLon[]`, `edgeFrom[]`, `edgeTo[]`, `edgeLengthM[]`, `edgeInBand[]`, `nodeOffsets[]`, `nodeEdges[]`, plus `regionId` and the box
- Edges store **length and the in-band mark, never time and never a limit**: `time = lengthM ÷ min(cruiseSpeed, zoneLimit)` is recomputed at search time, so neither the 3–40 kn slider nor a regulation change requires a re-bake
- The loading side fills the same flat arrays the search reads, so one representation serves file, memory and search; the object-shaped `RoutePoint`, `RouteNode`, `RouteEdge` and `RouteMesh` stay in the builder and the serializer

### route-search

**Retired as the design of record (2026-09-20)** — a stretch is a mesh concept, so this section's rules are the mesh engine's own and bind nothing the tracer does.

**Focus:** On-device fastest-path search over the prebaked mesh.

#### Todos
- [ ] Phase 2: replace the 2.5 m closing depth with the boat's own draught once such a value exists to replace it with

#### Rules
- Kotlin coroutines/Flow only
- Both ends of a route sit in one stretch of connected water: when either resolves into a different stretch, that end is re-resolved to the closest point inside the same stretch, and a destination resolving to land takes the same path (agreed 2026-09-19)
- The boat can leave the covered water while a route is active: the line stays, a recompute is skipped rather than failing, and the trip figure keeps its last value marked stale the way the app already marks a stale fix (review 2026-09-19)
- The heuristic is admissible by construction — `min(cruise, limit)` can never exceed the cruise speed, so a distance-over-cruise estimate never overstates the remaining time — and it is stated here so it is not "optimised" away (review 2026-09-19)
- Mesh load = stream-deserialize on Dispatchers.Default (matches depth/coastline precedent; not memory-mapped)

### destination-ui

**Focus:** Destination input, route preview, and overlay.

#### Rules
- One-shot compute for MVP; live re-planning deferred
- Mode-entry is the route toggle in the map control stack, gated the way the inspect toggle is; the free-water pace control sits in Settings (accepts extra shared-file edits, the dashboard's distance-to-shore cell among them)
- The trip's time uses a set free-water pace (3–40 kn, default 28) until evidence exists, then the boat's observed pace outside the regulated zones and the band, reduced by a high quantile over a sliding window with a fallback to the set pace on thin samples (agreed 2026-09-19, plan §16)
- The dashboard's distance-to-shore cell shows the trip figure while a route is confirmed, the zone cell keeps its compliance duty, and no separate route panel exists (agreed 2026-09-19, plan §16)
- Pin/polyline/zoom-to-fit are geo-native osmdroid overlays — no manual screen↔geo projection; the `MapEventsOverlay.longPressHelper` entry this rule once named left with the long-press, the aim being a screen-centred target owned by `RouteOverlay` (2026-09-20)
- Ordering and start, agreed 2026-09-19: the line sits above the tracks and below the markers in the existing order, one file owns the map objects, and the start is the position the dashboard reads — the GPS fix in GPS mode, the same seam in demo mode — never the map centre — and it is captured **once, at the instant the toggle is turned on**, then held for the whole draft and the confirmed route, so no preview ever re-reads it (revised 2026-09-20 on device: in demo mode the dashboard's seam resolves to the map centre, so a re-read start tracked the aim and drew a degenerate route between the last centre and the new one)
- The toggle is the single route control, agreed 2026-09-19 (plan §17) and revised on device 2026-09-20: **on** freezes the boat's position at that instant as the route's start, arms the aim from it, and brings up the confirmation **in the dashboard's own slot** at once — that panel replaces the indicator grid for as long as a route is aimed and its details track the aim, so nothing floats over the map the aim is being dragged across — while **off** ends the mode, an unconfirmed draft included. Confirm locks the route, and Cancel, the back key and the toggle all end the mode, so plan §17's "OK confirms" with no named control and plan §20's "Cancel changes nothing" are both superseded. It replaces the long-press todo entirely, and the mode is gated the way the inspect toggle is
- The route toggle and the inspect toggle are mutually exclusive: entering one leaves the other, per the app's single-mode habit (review 2026-09-19)
- The pin is drawn at the resolved destination, never at the raw aim: an aim landing on land or in another stretch moves to the closest point of the boat's own stretch, and the pin must show where the route actually ends (review 2026-09-19)
- The line's appearance lives as `maro.properties` keys behind `AppConfig` — following the rule that every drawing value has one home — and gains no Settings row until one is asked for (review 2026-09-19)
- Arrival carries no state and no cue: reaching the destination is the trip cell reading zero while the line stays drawn, and only the toggle ends a route (agreed 2026-09-19)

### route-saving

**Focus:** Persist a confirmed route as an ordinary track, pinned or not, so it can be listed, exported and redrawn like any other journey.

#### Rules
- A saved route is a normal track in every respect — list, stats, GPX export, heatmap, replay — so nothing downstream gains a special case and only the flag records that the speeds were planned rather than measured
- The write goes through `data/track/`'s own repository and factory, which is the fourth shared surface the isolation design accepts, after the MapScreen hook, the menu entry and the Settings row
- The pin is the track's existing field: the save offers its initial value while the track list keeps its own control, so the field has one home and two ways to reach it
- The four outcomes, agreed 2026-09-19 (plan §20) and amended on device 2026-09-20: `Route` follows without saving and locks the route, `Save & route` writes the track and then follows, `Save only` writes it and ends the mode with the camera returning to the start, and `Cancel` exits the mode — the panel rising with the mode, in the dashboard slot, rather than from a control of its own. That panel is the app's **one exception** to the dashboard's read-only habit: it carries action buttons because the outcomes have to sit where the details are, and the alternative — a floating dialog — cannot be aimed under
- The save is an outcome and the pin is a state, so the buttons state outcomes and the dialog's one checkbox is the pin — which is also why the save itself gets no checkbox
- The saved track takes the standard auto-name the Tracks feature already uses, with `plannedCourse` distinguishing it from a recorded journey (review 2026-09-19)

## Isolation Design

**Principle (revised 2026-09-19):** the route is placed in the app's own tree rather than in a package of its own — the app already slices `data/` by subject (`depth`, `coastline`, `regulation`, `track`, `markers`, …) and keeps each prebake builder beside the subject it bakes for, so a parallel `route/` root would re-invent that taxonomy and hide the feature from the code map. The isolation principle survives as the three seams below, not as a folder.

1. `MapScreen.kt` — a single `RouteHost(mapView, boatPosition)` call.
2. Menu drawer (`Ui_Menu`/`OverlayLayer`) — one entry to open the destination picker.
3. Settings (`Ui_Settings`/`SettingsOverlay`) — one cruise-speed row.

**Collision surfaces:**

| Shared file | Risk | Strategy |
|---|---|---|
| `MapScreen.kt` (a ~2.5k-line orchestration shell) | highest | single `RouteHost()` call |
| `OverlayLayer.kt` / menu drawer | mode-entry | one menu item wired to a Route callback |
| `SettingsOverlay` | cruise speed | one row from the shared row family, its value held by `SettingsManager` — the home is still open (plan §6) |
| `MapOverlayRenderer.kt` / `OverlayZOrder` | overlay ordering | the route's own osmdroid polyline and pin, its slot still to be decided (plan §14 C10) |
| `spatial/` + `data/regulation` | read coupling | read-only `RouteSpatialAdapter`, the single sanctioned importer |
| `data/model/` | shared types | `LatLng` and `BoundingBox` reused, never re-declared |

**Placement — main (ships in the APK)** (paths as the seam left them, 2026-09-20):
```
data/model/   RoutePoint.kt, RouteMesh.kt, RouteNode.kt, RouteEdge.kt, RouteResult.kt, RouteEngineDetails.kt
data/route/   RouteMeshSerializer.kt, RouteSpatialAdapter.kt, RoutePace.kt
spatial/      RouteEngine.kt, RoutePointQueries.kt, RoutePlanTiming.kt, RouteFillet.kt, RouteTurnGeometry.kt, Units.kt
spatial/mesh/ MeshRouteEngine.kt, RouteSearch.kt, RouteMeshRepository.kt, RouteMeshContainment.kt, RouteShortcut.kt, RouteMeshDetails.kt
ui/map/       RouteViewModel.kt, RouteOverlay.kt, RouteHost.kt, RouteConfirmPanel.kt
```

**Placement — prebake (src/test, never in the APK), beside the domain it bakes for:**
```
app/src/test/java/ykws/android/maro/data/route/
  RouteMeshBuilder.kt, RouteGeometry.kt, RouteMeshPrebakeTest.kt
```

**Naming:** the `Route` prefix only where a name would be ambiguous outside its package — `RouteMesh`, `RouteSearch`, `RouteOverlay` — and plain names inside `data/route/`. `RouteConfig.kt` is dropped: conversions come from the shared units home and the settings from `SettingsManager` with their keys in `maro.properties`.

**Decoupling rules:**
1. Route depends only on `data/model`'s `LatLng` and `BoundingBox` plus its own `RoutePointQueries` interface and the test-only `RouteGeometry` — with `RouteSpatialAdapter` as the single sanctioned importer of spatial and regulation code.
2. `RouteSpatialAdapter` is the only file importing `CoastlineSpatialIndex` / `SpeedZoneIndex` / `RegulatedZonesRepository`; read-only, Context-based, runtime-only.
3. Route declares no constant and keeps no preference of its own: conversions come from the shared units file, and the cruise-speed home remains the open recommendation in the plan's §6.
4. All Route runtime state lives in `RouteViewModel` (StateFlow).
5. Tap capture via osmdroid `MapEventsOverlay`, never `setOnTouchListener`.

**Interaction flow (state):**
```
Idle → route toggle on → Draft (start frozen here, aim target + preview + confirmation panel in the dashboard slot)
Draft → Route / Save and route → Confirmed (the route is locked)
Draft → Save only → Idle (the track is written, the camera back to the start)
Draft → Cancel / back / toggle off → Idle
Confirmed → Recompute → Confirmed (from the current position)
Confirmed → toggle off → Idle
```

**Loading + serialization:**
- Mesh load: stream-deserialize on `Dispatchers.Default` (depth/coastline precedent; not memory-mapped).
- Serialization: protobuf-javalite; output `data/app-assets/route/<region>.bin` (region id from BuildConfig).
- Prebake: JUnit gated by `-Dmaro.prebake=true`; `tools/bake-route.bat`; JTS + poly2tri as `testImplementation` (prebake-only, never in the APK).

## Rules
- Out of scope MVP: draft-aware routing beyond the depth gate's reference floor, prohibited zones (ACCESS_PROHIBITED etc.), live re-planning
- Placement, decided 2026-09-19 (walk item 8): the route joins the app's own slicing instead of taking a package of its own — model types in `data/model/`, repository, serializer and spatial adapter in `data/route/`, point queries and search in `spatial/`, view model, overlay and host in `ui/map/`, the prebake builder in `app/src/test/.../data/route/`; shared-file edits stay limited to the MapScreen hook, the menu drawer entry and the Settings cruise-speed row
- The mesh carries geometry and connectivity only, never a limit: the effective limit is resolved while a route is asked for, from the 300 m band's value and the regulated zones' own limits, cached per search. The mechanics — a lookup per edge crossing rather than stored zone ids — are the agent's call (2026-09-19)
- Prerequisite, decided 2026-09-19: the band's value and the zone-to-effective-limit mapping each need **one home** before Route can consume them — today the mapping is written three times (`SpeedZoneBuilder.kt`, `RegulatedZoneComponents.kt` twice) and the band's 5 kn is a literal in `NavigationViewModel.kt`; that consolidation belongs to RegulatedZones, not here
- The 300 m band's shape is baked — a boundary the triangulation respects plus an in-band mark per edge — while its limit stays live and is resolved at search time; agreed 2026-09-19, and only a benchmark showing the mark costs more than the query it replaces would drop it
- Conversions come from one home: a pure-Kotlin units file beside the spatial helpers owns knots-to-metres and miles-to-metres, that fold lands **before** this feature so no fifth spelling appears, and Route declares no conversion constant of its own (agreed 2026-09-19)
- Prebake dependencies, approved 2026-09-19: JTS and poly2tri ride the test classpath only — a version entry and an alias each in `gradle/libs.versions.toml` and two lines in the existing `testImplementation` block — so the app cannot import either and the APK's size and runtime are untouched

## Key Files

**The incumbent baseline, kept for measurement (retired as the design of record 2026-09-20):** `RouteMeshRepository.kt`, `RouteMeshSerializer.kt`, `RouteSearch.kt`, `RouteMeshContainment.kt`, the prebake `RouteMeshBuilder.kt` and `tools/bake-route.bat` are the mesh engine's alone; the seam (`RouteEngine`, `MeshRouteEngine`) and everything above it stay engine-neutral, and the tracer's files are the ones the design of record's §13 names.
- `app/src/main/java/ykws/android/maro/data/model/RoutePoint.kt`, `RouteMesh.kt`, `RouteNode.kt`, `RouteEdge.kt`, `RouteResult.kt` — NEW. The route's pure domain types, beside `LatLng` and `BoundingBox`
- `app/src/main/java/ykws/android/maro/spatial/mesh/RouteMeshRepository.kt` — NEW. Stream-deserialize the mesh `.bin` on `Dispatchers.Default`, hold it as flat arrays, and decode **single-flight** (a `Mutex` with a re-read under it, so the initial preparation and the readiness retry cannot decode twice)
- `app/src/main/java/ykws/android/maro/data/route/RouteMeshSerializer.kt` — NEW. javalite with hand-built packed arrays, schema beside `coastline.proto` and `depth.proto`
- `app/src/main/java/ykws/android/maro/data/route/RouteSpatialAdapter.kt` — NEW. Context-based read-only adapter over `CoastlineSpatialIndex` / `SpeedZoneIndex` / `RegulatedZonesRepository`
- `app/src/main/java/ykws/android/maro/spatial/RoutePointQueries.kt` — NEW. Runtime interface: isWater, pricedZoneAt (a zone's identity and its limit in one answer), distanceToZoneM, the band's live limit
- `app/src/main/java/ykws/android/maro/spatial/mesh/RouteSearch.kt` — NEW. On-device A* over the flat mesh, house-prefixed like `CoastlineSpatialIndex`; the mesh engine's own search, reached through `MeshRouteEngine` and nothing else
- `app/src/main/java/ykws/android/maro/spatial/RouteEngine.kt` — NEW. **The seam**: readiness (`NotReady` · `Ready` · `Unavailable(reason)`, the last a closed set carrying a `@StringRes` id) and one cancellable `route(start, aim, cruiseSpeedKn)`. What `Ready` promises — and what it does not, the permissive default of a live-reading engine — is stated here, because it is a property of the contract
- `app/src/main/java/ykws/android/maro/spatial/mesh/MeshRouteEngine.kt` — NEW. **The incumbent, and the only door onto the mesh**: the repository, the search and the spatial adapter behind one class, built by `overBundle(...)` in the app or `overMesh(...)` in the readings. One search is *started* at a time, never one *running* — the cancel is not a join
- `app/src/main/java/ykws/android/maro/spatial/mesh/RouteMeshDetails.kt` — NEW. The mesh engine's own dossier, off `RouteResult.Success` and off every reader of the plan
- `app/src/main/java/ykws/android/maro/spatial/mesh/RouteShortcut.kt`, `RouteMeshContainment.kt` — NEW. The shortcut walk over the chain and the bake's kept triangles as an exact containment test
- `app/src/main/java/ykws/android/maro/spatial/RoutePlanTiming.kt`, `RouteFillet.kt`, `RouteTurnGeometry.kt` — NEW. The drawn clock (each leg at the limit its chain edge carried), the fillet, and the one home for a turn's geometry
- `app/src/test/java/ykws/android/maro/ui/map/RouteEngineSeamTest.kt` — NEW. **The seam read through the feature**: a straight-line engine that knows no mesh is handed to a real `RouteViewModel`, and the plan, the trip figure and the save are asserted off what the feature made of its answer
- `app/src/test/java/ykws/android/maro/spatial/mesh/RouteMeshDetailsReadings.kt` — NEW, test-only. The one home for "a route with no dossier reads as all zeros", beside the strict `meshDetailsRead` a counting test uses instead
- `app/src/test/java/ykws/android/maro/spatial/CoastlinePointWalkTest.kt` — NEW. The point walk's own guard: its nearest distance against a **brute-force minimum over every segment the index holds**, and its water answer against the mainland side test with the real-island override counted where it fires — a reading of the answer rather than of the mechanism
- `app/src/main/java/ykws/android/maro/ui/map/RouteViewModel.kt` — NEW. StateFlow: destination draft, preview route, confirmed route
- `app/src/main/java/ykws/android/maro/ui/map/RouteHost.kt` — NEW. The one file touching osmdroid for the feature: the polyline, the pin and their add and remove, the frozen start's single read and the aim, reached by the single `RouteHost(...)` call in MapScreen; it raises no panel of its own, and the back key ends the mode while it is armed
- `app/src/main/java/ykws/android/maro/ui/map/RouteOverlay.kt` — NEW. The route toggle, the screen-centred aim target, the pin row and the feature's pure rules — the aim threshold, the preview floor and the trip figure; the trip figure and its recompute live in the dashboard cell, not here
- `app/src/main/java/ykws/android/maro/ui/map/RouteConfirmPanel.kt` — NEW. The confirmation, composed by the shell **into the dashboard slot** while a route is aimed: the details, the pin and the four outcomes, built from `ConfirmActionButton` and `RoutePinOption` rather than re-drawn
- `app/src/main/java/ykws/android/maro/spatial/Units.kt` — NEW (shared home). The knot and nautical-mile conversions the prerequisite fold landed, so Route declares no conversion of its own
- `app/src/main/java/ykws/android/maro/data/route/RoutePace.kt` — NEW. The observed-pace reduction: samples taken outside the zones and the band, a ten-minute window, a high quantile, and the set pace below its sample floor
- `app/src/test/java/ykws/android/maro/data/route/RouteMeshBuilder.kt` — NEW. Prebake: planar overlay + CDT + edge costing (JTS + poly2tri), in the home `Zone300AssetBaker` already uses
- `app/src/test/java/ykws/android/maro/data/route/RouteGeometry.kt` — NEW, test-only. The bake interface — coastline segments, zone rings, band rings — so no bake-only interface ships in the APK
- `app/src/test/java/ykws/android/maro/data/route/RouteMeshPrebakeTest.kt` — NEW. Prebake test gated by `-Dmaro.prebake=true`, carrying the JVM A* benchmark assertion
- `tools/bake-route.bat` — NEW. Bake script, wired into `apk-bake.bat`

## Docs
- `xTrack/Route/260919_FEAT_PLN_Route_registration-and-drift.md` — the settled design: what the corpus gained, the register of every decision taken on 2026-09-19, what a route costs and how re-planning works, the bake wiring with its approved test-only libraries, the open points, and the implementation sequence (shipped)
- `xTrack/Route/260920_FEAT_PLN_Route_trajectory-quality.md` — the trajectory-quality study: a proposal per reported point — the missing bay, the jagged line, the comfortable-turn fillet, the shortest sensible path and the 25 m zone berth — with the instrument, the round plan, the acceptance numbers and the decisions D1–D5 (shipped: rounds 0–4 as run, §18–§19)
- `xTrack/Route/260920_FEAT_PLN_Route_engine-seam-and-parking.md` — **the engine seam**: the contract, the couplings that remain (§3 — including couplings 5–6, the band mark and the forced-crossing report, still derived inside the mesh path), the eight steps, the probe-as-instrument payoff, and the parked alternative with its three un-parking triggers (**steps 1–7 shipped; step 8 open**)
- `xTrack/Route/260920_FEAT_PLN_Route_taunt_algorithm-design.md` — **the design of record and what ships since 2026-09-20**: §10–§12 the algorithm, §13 its Stage 0 entry point, §15 the resolutions taken on review, §16–§17 the two revision passes built, §18 the band's crossing closed in closed form, **§19 the drag's pipeline and the search's cost with the levers priced** (§19.1 the plan, §19.2 the ten levers, §19.3 the re-pricing after items 1–5, §19.4 the terrain kept for a box, §19.5 the review that sent that step back); it is the whole tracer's record, and the meshes of §14 are parked with the retired engine

## Walk
**Level 1 — Date:** 2026-09-19 · **Source:** the ten challenge items of `260919_FEAT_PLN_Route_registration-and-drift.md` §14, plus the depth question tagged as item 11 · **Closed:** 2026-09-19 — exhausted, ten items resolved and the depth gate parked
- [x] 1 · C1 wire shape — resolved 2026-09-19 by the user's instruction: the mesh follows the coastline and depth mechanism — packed primitive arrays with CSR offsets under javalite, schema in a `.proto` beside `coastline.proto` and `depth.proto`, hand-built like their serializers — so one set of numbers is both the file and what the search reads while running, and the builder's objects never reach the wire
- [x] 2 · C2 limits — resolved 2026-09-19: a speed limit does not belong in the route data file at all; it belongs to the 300 m band and to the regulated zones, whose values are centralised and built to change; and per plan §15 the band's **shape** is baked while its number stays live — its boundary respected by the triangulation and an in-band mark on each edge
- [x] 3 · C3 lookup at search time — closed with item 2, the two being one subject split in two: the mesh carries geometry only, and the effective limit is resolved from the zone layer while a route is asked for, cached per search, so a changed limit or cruise speed never re-bakes
- [x] 4 · C4 resolution — resolved 2026-09-19: refine near the coast, bounded rather than uniformly fine, with a 30 m minimum channel width held by the connectivity pass — unnecessary in this corridor but assumed as the floor
- [x] 5 · C5 snapping — resolved 2026-09-19: the bake labels connected water, and when either end of a route resolves into a different stretch the end moves to the closest point inside the same stretch rather than failing or reaching across — the same mechanism that answers a land-resolving destination
- [x] 6 · C6 bounds — resolved 2026-09-19: the mesh box is the app's own water definition, derived as the depth envelope derives its own (coastline bbox + the 6 NM band) and reusing `data/model/BoundingBox`; a boat outside the box is reported rather than snapped inland, and whether the cut sits exactly on the boundary or a node spacing inside it is the implementer's call
- [x] 7 · C7 units — resolved 2026-09-19: the four spellings fold into one pure-Kotlin units file beside the spatial helpers, that fold lands first as its own small task, and Route consumes it while declaring no conversion constant of its own
- [x] 8 · C8 naming — resolved 2026-09-19: the route joins the app's own slicing (model in `data/model/`, repository, serializer and spatial adapter in `data/route/`, queries and search in `spatial/`, view model, overlay and host in `ui/map/`, prebake in `src/test/.../data/route/`); the `Route` prefix survives only where a name would be ambiguous outside its package, and `RouteConfig` is dropped
- [x] 9 · C9 two ETAs — resolved 2026-09-19: the zone figure keeps its speed-made-good basis, the trip figure uses a set free-water pace (default 28 kn) then the boat's observed pace outside zones and the band, and the trip figure lives in the dashboard's distance-to-shore cell rather than a panel of its own
- [x] 10 · C10 drawing path — resolved 2026-09-19: the line sits above the tracks and below the markers in the existing order, one file owns the map objects, and the start is the position the dashboard reads — the GPS fix in GPS mode — never the map centre; demo mode's own wrinkle, where the camera is the boat, is under discussion in plan §17
- [x] 11 · C11 depth gate — resolved 2026-09-19 by splitting it (plan §18): the reference half joins the MVP, so a passage the depth data rules out is absent from routing, and the per-boat half became item 12
- [x] 12 · C12 draught gate — resumed and resolved 2026-09-19: the closing depth is 2.5 m, applied at build time so that no route crosses water shallower than it, and a boat's own draught takes that number over later

- Resolutions: ten items closed by decision — the mesh packed like the coastline and depth data; limits left to the zone layer with the band's shape baked and its number live; connected water labelled, one stretch rule for both ends of a route; bounded coastal refinement with a 30 m floor; the box cut to the app's own water; conversions folded into one home first; the route placed in the app's own tree with the `Route` prefix kept only where it disambiguates; the trip figure in the distance-to-shore cell on a set pace that gives way to the observed one; the line above tracks and below markers, with demo mode pausing its sailing while a target is aimed. Then the depth gate, split on 2026-09-19 with its reference half taken into the MVP and its draught half resumed the same day and closed on the 2.5 m closing depth. Dropped: nothing. Parked: nothing — the level is exhausted.

**Level 1 — Date:** 2026-09-19 · **Source:** the pending set — the feature's open todos and the plan's unsettled points, in ship order · **Closed:** 2026-09-19 — exhausted, thirteen items settled and nothing parked
- [x] 1 · The destination entry point — resolved 2026-09-19: a route toggle in the control stack, on aiming a screen-centred target with a live preview, OK confirming, and off ending the route and cancelling an unconfirmed draft
- [x] 2 · JTS and poly2tri — approved 2026-09-19: both, on the test classpath only, versions pinned in the version catalogue, and the first bake fetches them once
- [x] 3 · The units fold — settled: one pure-Kotlin units file beside the spatial helpers, folded before this feature, Route declaring no conversion of its own
- [x] 4 · The zone-layer homes — settled as a prerequisite on RegulatedZones rather than work taken here; Route only consumes `SpeedZoneIndex.query()` and the band's value
- [x] 5 · Models, schema and interfaces — settled: pure domain types in `data/model/`, a packed `.proto` with CSR arrays in `app/src/main/proto/`, `RoutePointQueries` in `spatial/` and `RouteGeometry` test-only
- [x] 6 · The mesh builder — settled: bounded adaptive refinement with the 30 m floor, and a node budget measured by the prebake benchmark rather than fixed on paper
- [x] 7 · The prebake wiring — settled: `tools/bake-route.bat` under `-Dmaro.prebake=true`, a `:do_route` label in `apk-bake.bat` ordered after the depth step, writing `data/app-assets/route/`
- [x] 8 · The repository and serializer — settled: one decode on `Dispatchers.Default` into flat arrays kept by the repository, no other file opening the mesh
- [x] 9 · The search — settled: A* over the CSR arrays with a metric heuristic and `ensureActive()` cancellation, the pace reduction a high quantile over a ten-minute window sampled outside the zones and the band
- [x] 10 · The spatial adapter — settled: a thin Context-based delegator with no logic of its own, the single importer of the spatial and regulation types
- [x] 11 · The pace setting, view model and target mode — settled: a `maro.properties` key with its `AppConfig` accessor and `SettingsManager` field, default 28; one sealed state over `StateFlow`; the toggle already agreed
- [x] 12 · The dashboard cell — settled: the distance-to-shore cell carries the trip figure, as a `Ui_Dashboard` rule with strings in both locales
- [x] 13 · The three shared-file edits — settled: one `RouteHost` call, one menu entry, one Settings row, and read-only route state in an `OverlayLayerParams` bundle rather than a new parameter

- Resolutions: thirteen items settled — the toggle as the single route control, both prebake libraries approved test-only with the bake refusing on a missing input, the units fold landed first, the zone-layer prerequisites routed rather than absorbed, models and a packed schema with CSR arrays, a measured node budget under bounded coastal refinement, the prebake wiring through `apk-bake.bat`, one decode into flat arrays, movement-cancellable A* with a quantile pace, a logic-free adapter, the pace as an ordinary setting, the trip figure in the distance-to-shore cell, and the three app-side seams one line each. Dropped: nothing. Parked: nothing.

**Level 1 — Date:** 2026-09-20 · **Source:** the pending work of the trajectory-quality study — its rounds and their two measurements, in ship order — the two levels above being exhausted with nothing parked · **Closed:** 2026-09-20 — exhausted, five items resolved and nothing parked
- [x] 1 · Fewer vertices, fewer turns — resolved 2026-09-20 by a **turn penalty inside the search's cost** rather than the study's sampled string-pull, a sample test being unable to see a 25 m hole the gate closed, while a path on the mesh's own edges cannot leave covered water at all: 413 → 255 vertices, total turn 7,366° → 6,931°, worst corner 110° → 100°, length and ETA inside a quarter of a percent
- [x] 2 · The passage measurement — resolved: no passage that carries a route today is narrower than 50 m, so the berth's own tripwire never fired
- [x] 3 · The 25 m zone berth — resolved as a **price, not a wall**: a clearance term in the search's own seconds, shipped at twice a leg's time, costing +0.23 % to +0.74 % of length and reducing in-berth kilometres by 2.5 to 6.8 % — with no water deleted, no passage closed, and the hard buffer's 13 % of sounded water not paid; the price row that trips the wire is 3.0, and zone 2's pair is unchanged at every price because no corridor outside the berth exists there
- [x] 4 · The fillet — resolved, and it had **never drawn a curve**: its arc was built on `R / tan(θ/2)` and `R / sin(θ/2)` where `R · tan(θ/2)` and `R / cos(θ/2)` belong, so the sweep it produced was never the turn and the pass refused every corner not within a ten-thousandth of a right angle — and every test case sat at 90°, where the two forms agree, which is why a green suite shipped it. Corrected, with the geometry gathered into one `RouteTurnGeometry` and 30°, 60° and 120° cases added, the fillet now rounds every corner the mesh allows: **0 sharp of 446** at the re-chosen default cap **0.5 m/s²** and 1 of 444 at 1.0, against 425 of 444 refused before
- [x] 5 · The gate's unknown-soundings policy — resolved as **bridging only**: an unsounded triangle is kept only where its own water reconnects two stretches of the strictly gated mesh, one corridor per pair and bounded in length; `nice-menton` reads **21 → 13 stretches** for +19 nodes and +1,249 bytes, the reported bay at 43.5456 N, 7.1252 E merges into the sea beside it, and the number the tripwire names comes out **0.0 km² of unsounded water admitted against 1,228.5 km² that routes**

**Level 1 — Date:** 2026-09-20 · **Source:** this session's scoped decision — the taut-string design's algorithm built **test-only as a tracer**, in place of the incumbent's tracing, with the debts a second engine inherits · **Cursor:** item 2 · **Closed:** — open
- [ ] 1 · The tracer measured against the incumbent — the new line built in `src/test` and run on the same pairs, so the two are compared rather than argued about
- [ ] 2 · The second engine's own readings — its dossier declared, so the columns beside the six metrics are not the mesh's zeros
- [ ] 3 · **Parked 2026-09-20** — the seam's two remaining couplings are derived inside the mesh path, and the tracer derives its own; resumes only if the mesh engine is re-adopted as the design of record
- [ ] 4 · The standoff where the water is narrower than twice it — the design's one open question, and it changes the line
- [ ] 5 · The shallow contour's tolerance against the margin it is measured on
- [ ] 6 · The on-device pass the feature has owed since the engine work — the aim, the fling cancellation, the panel and the saved course
- [ ] 7 · **Parked 2026-09-20** — the draught gate belongs to the mesh's 2.5 m closing depth; the tracer states its own gate, so this resumes only on a mesh re-adoption
- [ ] 8 · **Parked 2026-09-20** — the berth's structural choice and the drawn clock's `inBand` reader are the mesh engine's own; both resume only on a mesh re-adoption

**Level 2 — Date:** 2026-09-20 · **Parent:** 1 · **Source:** the tracer's authority — what the new line is allowed to decide · **Closed:** 2026-09-20 — resolved by decision, the authority answered and the parent left open for its own work
- [x] 1 · Its own way — resolved 2026-09-20: with the mesh engine retired as the design of record, the tracer produces the line on its own terms rather than redrawing the incumbent's way
- [ ] 2 · The old line's decisions — **dropped** with the authority question, the reading that would have needed it having been retired
- [ ] 3 · Both, measured — **dropped** with it: the comparison still runs on the same pairs, but as one line against a baseline rather than two authorities

- Resolutions: the authority settled by the user's decision of 2026-09-20 — the old engine retired as the design of record and its walk items parked, so the tracer produces the line. Dropped: the redraw-only reading and the two-authority comparison. Parent item 1 stays unticked, its own work not yet run.

**Level 2 — Date:** 2026-09-20 · **Parent:** 2 · **Source:** the comparison's columns — what the tracer owes the instrument before a difference between the two lines means anything · **Cursor:** item 1 · **Closed:** — open
- [ ] 1 · The tracer declares its own counts — what the search expanded, how long it took, how big its map was, how much unsounded water it let in and how many corners it refused
- [ ] 2 · The six numbers only — length, time, turning, worst corner, zone clearance and bends, with nothing said about cost
- [ ] 3 · The six plus whatever the tracer reports — a missing figure read as *not reported* rather than as a cheap search

**Level 1 — Date:** 2026-09-20 · **Source:** the user's instruction of 2026-09-20 — deactivate the original engine, and switch to the new one as soon as it is implemented; every other consideration parked until told otherwise · **Cursor:** item 2 · **Closed:** — open
- [x] 1 · The original engine deactivated — **superseded 2026-09-20 by the switch instruction**: the old engine stays wired until the replacement exists, so there is no interim stretch with no line, and the reversal is one wiring expression
- [x] 2 · The replacement built, **and the app switched to it the moment it is implemented** — resolved 2026-09-21: eight files in `spatial/taut/`, the switch one expression at `MapScreen`, `apk-build.bat` and 83 suites green, the independent review returning **revise** with six blockers now planned as the design's §16
- [x] 3 · **Parked by decision 2026-09-20**, at the user's word — the instrument and its columns, the contour's tolerance and the on-device pass, each resuming when the user says so and none of them dropped. The water narrower than twice the margin is **not** in this group: it is a live question in the design (11.7, 15), not a deferred one
- Note: the level opened earlier today for the tracer is unexhausted and therefore parked beneath this one, its own parked items included.

**Level 2 — Date:** 2026-09-20 · **Parent:** 2 · **Source:** the algorithm itself, walked to refine it — its stages in the order they are built, one per exchange · **Closed:** 2026-09-20 — exhausted, eight stages refined and nothing parked; the parent stays unticked because its own work, the build, has not run
- [x] 1 · The corridor — resolved 2026-09-20 by the walk's own choice: a modest box grown **once** and the search re-run when it finds nothing, the second empty answer reported by name rather than returned as nothing
- [x] 2 · The obstacle set — resolved 2026-09-20 by the user's **choice 3**: land plus the 2 m contour wall what the soundings reach, and where they do not, a fixed offset off the coastline supplies the wall; the offset is left as a number, and the bay this study opened may stay sealed under the rule
- [x] 3 · The standoff — resolved 2026-09-20 on the walk's recommendation: two mechanisms, the shore margin **hard** because widening it costs only shallow water and a zone's margin **priced** because widening it deletes the passage, the number being the berth the app already ships; the narrow-water case stays the user's open question, on the retired engine's own measurement (a hard margin severed the water in 83 of 121 cases, a price moved the line by 2.5 to 6.8 %)
- [x] 4 · The graph — resolved 2026-09-20 on the user's own proposal: the obstacles are abstracted into long segments whose tolerance is a stated fraction of the buffer (a quarter, the working figure), with the simplification one-sided outward, the dilation applied after it, the fillet restoring the curve only where an arc fits and is water-tested, and the water the abstraction deletes printed as a reading. **The water it deletes is accepted for the first cut** — the printed area is what would reopen it, not a reason to hold the engine back
- [x] 5 · The edge price — resolved 2026-09-20 at the user's word: **the clock and nothing else** — the length at the speed in force plus the time a corner's slowdown costs, with the entry direction carried so the search can see that cost; no berth price, no aversion factor and no distance term, since speed has priority where no physical constraint intervenes, and a line at the margin is what taut and fastest means; zones stay a rule rather than a price; ties break deterministically
- [x] 6 · The search — resolved 2026-09-20 on the walk's recommendation: A\* over the corner graph with a straight-line estimate, the **arrival direction in the state** so a corner can be priced at all, ties broken by a stated rule (shorter, then fewer turns) and never by insertion order, and **two runs** — priced zones forbidden first, priced only when that finds nothing, with the crossing named
- [x] 7 · The corners and the speeds — resolved 2026-09-20 at the user's word: the curve is an **easing spiral** where the water allows, a circular arc where it does not and a sharp vertex where neither fits, so the turn reads as a trajectory rather than a round turn; the fit test shortens the transition before it reduces the radius, the speed is set by the tightest point, the corner's price is the spiral's own length, and the easing's geometry has one home. **The room the easing costs is accepted** — more corners may come out sharp than under a circle, and the count of them is the printed reading that would reopen it
- [x] 8 · The output — resolved 2026-09-20 on the walk's recommendation, accepted at the user's word: points and per-leg times exactly as the app already reads them, each curve emitted as chords stepping the **shorter of 10 m and 10° of arc** (about nine points a corner at any radius), the leg's limit in force carried so the drawn line is timed at what the boat is bound by, and **two readings printed** — the largest deviation between the chords and the curve, and the seconds sampling cost — with the engine's own slack a stated tolerance rather than a near-zero one

- Resolutions: eight stages settled in one walk — the corridor grown once on an empty answer and refused by name; the obstacle set walled by land, the 2 m contour where the soundings are fine and a **shore offset** where they are not, at the user's own choice; the standoff **hard off the shore and priced around a zone**, the narrow-water case left open on the retired engine's measurement; the graph abstracted into long segments **licensed by the buffer**, one-sided outward and validated by exact segment distance; the price **the clock alone**, carrying the arrival direction so a corner can be charged; the search an A\* with a stated tie-break over **two runs**, zones forbidden first and priced only when nothing else answers; the corners **eased by a spiral** where the water allows, an arc where it does not and a sharp vertex where neither fits, priced at the spiral's own length; and the output unchanged in shape with its precision printed. Dropped at the user's word: the berth price, the zone-aversion factor and the distance term, speed holding priority wherever nothing physical intervenes; and an exact Euler spiral deferred behind the chord approximation. Parked: nothing — the parent's own work, the build, is what remains.

- Resolutions: the replacement built and switched in, the review's revise verdict recorded in `## Implemented`, and its corrections ordered in the design's §16. Parked: the instrument and its columns, the contour's tolerance, the on-device pass, and the level beneath this one.

**Level 1 — Date:** 2026-09-21 · **Source:** the design's §16, the revision pass planned from the review's six blockers and its should-fixes, in dependency order · **Cursor:** item 9 · **Closed:** 2026-09-21 — exhausted, nine items resolved with their readings
- [x] 1 · Measure before fixing — resolved 2026-09-21: the printed cost is **three figures per pair**, with the pair and node counts beside them and no total called "search". The acceptance pair now reads harvest **278 ms** · graph **387 ms** · search **802 ms** over 432 vertices, 3 605 edges and **93 096 pairs**, where the same pair before the correction read 321 / 531 / **3 097 ms** over 466 / 4 062 / 108 345 — the split is what made the rest of this pass judgeable
- [x] 2 · The corridor's margin restored from the obstacles it must contain — resolved: the box is the rough guess (1 NM × 2^growth **plus the berth**) unioned with the union of the extents the harvest was handed, clamped to a 1 NM reach and inflated by the berth, re-harvested once **per growth attempt**, with the growth pass still the single fallback. The two refused pairs are re-run and each is a **named absence**: NO_ROUTE_IN_CORRIDOR at growth 0 and 1 and NO_ROUTE_AFTER_GROWTH, with the split printed beside the refusal (Cap d'Antibes → Golfe-Juan 7.7 s — harvest 187 ms, graph 7 554 ms, search 0 ms; Antibes → Nice 98.1 s — harvest 488 ms, graph 97 653 ms, search 0 ms). The grown-corridor probe on the acceptance pair reads **still only a priced crossing** (38 legs, 430 vertices), so the crossing taken at 1 NM is real at 2 NM rather than hidden by the box
- [x] 3 · The zone margin priced in seconds, run one forbidding only a zone's interior — resolved: the berth is a **price rising as the boundary nears**, read at the leg's own closest approach (not at its middle) at a ceiling of twice a leg's own time, so the courtesy is in the clock's unit and never beside it; run one refuses only a zone's **interior** — outer ring minus holes — through the same predicate the price reads. The collar, its dilation and its corner harvest are deleted, and `route.zoneBerthM`'s documentation is rewritten as the tracer's price rather than the mesh's. Acceptance pair: **crossings 38, in-margin 0.25 km, berth price 515.29 s**, crossing [Zone 5 kn, Zone 10 kn] — run one cannot answer even grown, so the crossing is taken and reported
- [x] 4 · Zone holes navigable through one predicate — resolved: `TautZone.covers` is the single home (outer ring minus holes, a boundary point read as water), read by the rule **and** the price, and hole rings' corners are graph vertices so a line may bend through one. Pinned by a synthetic strip with a slot through it — run one answers with crossings 0 — and by the same strip with the hole moved aside, where the same leg is reported a crossing and the engine answers a way round instead
- [x] 5 · The corner's slowdown in the cost, carried by the longitudinal property — resolved: **`route.turn.longitudinalAccelMps2=1.96`** (0.2 g, §12.3's own figure) written into `maro.properties` with its `AppConfig` accessor and bounds — the last key this feature adds — and read through `RoutePlanTiming.longitudinalSec`, the one home both the search's price and the drawn clock charge, so the two agree on every pair by construction. Acceptance pair: longitudinal **4.34 s**, and the A\* matches the Dijkstra reference that charges the same term
- [x] 6 · The assertion set of 13.3 built — resolved: `TautAssertionsTest`, **8 tests**, one per assertion of 13.3, each with a **negative control** that reads the same predicate where it must say no and whose KDoc names the revert it catches — so every assertion is a reading of the mechanism's own output rather than a restatement of a constant. The Dijkstra reference and the determinism property stay in `TautSearchTest` (5 tests, the determinism one made able to fail with its control). **The set earned its keep on its first run: two assertions failed and both were real defects** — the easing rounded a corner *into* a zone's interior the rule had forbidden (the curve is now held to the run's own rule), and the corridor-sizing counter was shared across growth attempts, so the grown pass never sized its box and the way round was never found
- [x] 7 · The should-fixes in one pass — resolved: one home for the price and the accumulation (`pricedSec` read back from the search's own `best` map instead of summed a second time; the berth's arithmetic in one function); every zone ring and the containment predicate in the frame's **metres** rather than raw degrees; the zone ring's **own first vertex** restored as a corner; the unread members deleted (the collar and its array, `enteredCollar`, `collarOf`, `zoneWaterSign`, `wallSegmentsIn`, the unused `TautObstacles.walls` property and the unused build parameter); refusals named in the tracer's own vocabulary (`TautRefusal`, plus `RouteResult.OutsideWater` for the missing grid) with no mesh, no stretch and no bake, asserted by test; and the determinism test made able to fail, with a second one pinning the drawn-against-accumulated comparison with the berth set aside
- [x] 8 · Performance decided by the split reading — resolved as a **decision, with its target unmet and said so**: the acceptance pair's own split put the **search** on top (3 097 of 3 949 ms), so §16's second branch was taken — the **abstraction tolerance**, 0.25 → 0.5 of the berth, grow-only — together with an exact pre-fit reject (a leg whose own seconds already exceed the incumbent cannot win, so no easing is fitted for it); the search fell to **802 ms** and the wall clock to **1 469 ms**. The 500 ms target is **not met**, and the two refusals are the **build's** case (7.5 s and 97.7 s against 0 ms of search over 780 625 pairs, ~95 µs a pair), so §16's first branch is where their numbers fall: the prune by angle or distance and the wall index's bounding-box cell walk are **named and not taken blind**, a geometry prune being able to delete the only way round — the same reason the corridor itself was not touched
- [x] 9 · Every pair re-measured, and only then the device — resolved: all three pairs re-read on the figures above, the six metrics and the three split figures printed per pair, `apk-build.bat` **green**, **113 focused tests green** (25 in the tracer's own suites, 88 under the `Route` name) and the harness green under `-Dmaro.prebake=true`. The device pass stays the user's, unstarted and unoffered

**Level 1 — Date:** 2026-09-21 · **Source:** the design's §17, the second revision pass planned from the review's three blockers and its nine should-fixes, in its own order · **Cursor:** item 8 · **Closed:** 2026-09-21 — exhausted, eight items resolved with their readings
- [x] 1 · One leg, one limit, and an edge's price is a sum — resolved: a leg's price is its **time summed over its own limit regions**, homed in the build beside `legLimits.limitKn` (`LegLimits.regionsOf` · `priceSec`, read on as `TautGraph.drawnPieces` · `priceSec`), and the splitter lives in that same home so no second computation sits inside the assembly. The **one segment test** replaced the midpoint-plus-parity read: `TautZone.interiorSpans` answers the interval a leg shares with the interior and returns its entry and exit parameters, `covers`' boundary-is-water rule unchanged, and the rule, the price and the splitter all read it — so a leg clipping a zone's corner is charged at the zone's limit **and** counted by the crossing report. Every drawn leg is cut where the limit changes, a chord inside an arc included. Acceptance pair: 70 vertices · 536° turn · worst 52° · 5.36 km · ETA 25.49 min · crossings 38 · in-margin 1.03 km · berth price 515.29 s · crossing [Zone 5 kn, Zone 10 kn] · 2.20 km inside the 5 kn zone
- [x] 2 · A sharp corner pays, and its four causes are counted apart — resolved: the charge is written on `Turn.speedMps`, `√(a · r_floor)` = 4.7 kn, which is an **upper** bound on the corner speed for the water case alone (hence a lower bound on the real slowdown) and a bare number for the other three; both readers consult it, so nothing is exempt. Pair: sharp 8 — **guard 1 · build 0 · cutback 0 · water 7** — charge **0.04 s** of the 4.38 s the brake-and-accelerate pair cost. §17's ≈ 3.5 min does **not** materialise on this pair, and the reason is stated: all seven water-caused corners sit in 5 kn water, where the boat is already at the corner's own floor speed, so the pair is nearly free there — the charge's own size is what the line is judged on, per pair
- [x] 3 · Readiness through both doors — resolved: `TautWorld.readiness` (READY · COASTLINE_MISSING · DEPTH_MISSING) is the one member, answered by `RouteSpatialAdapter`, the harness's `PrebakedTautWorld` and the test fake alike, and read by **both** `prepare()` and `route()`; the adapter now triggers the **coastline's own load** (`CoastlineRepository.loadCoastline`) beside the depth grid's; and the case is named — `RouteUnavailableReason.COASTLINE_NOT_LOADED` with its `@StringRes` id and one string in each locale, so a route armed inside the window is refused with a reason rather than drawn over land
- [x] 4 · The ETA's rise attributed — resolved: the pair reads **25.49 min against 22.79** with the crossing unchanged at 38 legs and 2.20 km inside the 5 kn zone, so the rise is the **zone time the midpoint rule was dropping** — a leg whose midpoint stood outside the zone was charged at cruise over its whole length — item 2 adds 0.04 s on this pair, and item 1's crossing detection took no route away (the grown-corridor probe still reads *only a priced crossing*, 38 legs over 429 vertices). The figure sits above 22.79 as §17 expected
- [x] 5 · The nine should-fixes, each with the reading that shows it closed — resolved: (1) the margin's price and its reading are **one home** — `inMarginM` is Σ leg metres × the very fraction `berthPriceSec` multiplies by the leg's seconds, and a test recomputes it leg by leg; (2) the dead members went **by name**: `TautZoneSet.zoneAt`, `TautZoneSet.size`, `WallIndex.countIn`, `TautGraph.grown` and `TautGraph.inBand` with its `edgeInBand` store — plus `TautGraph.box` found by the same sweep; (3) the in-band question has one home (`inCoastalBand`), read by the price, the drawn clock and the harness; (4) the controls are shown to **move** when the mechanism behind them is reverted — the berth toggles the dilation, the deleted water and the wall segment count together, and the traversal rule's two runs are read on the synthetic corridor; (5) the mislabelled acceptance-pair test now names the pair it runs (a synthetic corridor), the real pair being the harness's own; (6) the band path has its own test, with the band switched off as its control; (7) the tolerance lever's **cost** is printed beside its benefit — 12.5 m (0.5 of the berth) deletes 1.47 km² of water and leaves 8 sharp corners of 20 in a 536° line; (8) the 802 ms standing was replaced by a named outcome, item 7 below; (9) the harness reads the world the way the app does — its `landPolylinesIn` is the index's own bounding-box walk, not the whole region
- [x] 6 · What the corrections may not do — resolved: nothing here adds a setting, a row, a panel or a key; the abstraction tolerance stays **0.5** (the user's, untouched), the ceiling 2.94 m/s², the berth 25 m, the band's price the shipped one, and the corridor's width exactly as §16 left it. The route stays on water, never enters a priced zone while a way around exists, reports a forced crossing by name, and its ETA is read off the drawn line at the limit in force over each leg
- [x] 7 · Performance closed on the levers, against a named target — resolved on the item's **second** outcome and no third: the search reads **2 110 ms** on the acceptance pair against the 500 ms target, so **the target moves to 3 000 ms of wall clock for that pair**, and the reason is written beside it — the search fits an exact, water-tested easing at every corner it considers **and** walks the graph twice (run one cannot answer this pair), while the interval test that closed item 1's corner-clip defect is dearer per chord than the parity read it replaced. The levers were taken with their measurement: the wall index now covers **the cells the line actually crosses** (a DDA walk with a stamp array, no per-pair `HashSet`) — the pair's graph **387 → 298 ms**, and the two refusals **7.5 s → 1.7 s** and **97.7 s → 5.2 s** (4.3× and 19×) over the same order of pair count (**232 903 + 410 871** and **510 555 + 779 376** pairs examined), so nothing was skipped; the turn-price cache was taken and **bought nothing** (1 922 → 2 110 ms, noise) and is reported rather than claimed. **No geometry prune was taken**: it could delete the only way round
- [x] 8 · The feature set stands exactly as the epic describes it — resolved: every correction above is internal, and the honest figures items 1, 2 and 4 produce are the whole of what a user sees differently

- Resolutions: eight items settled in one pass — the price a sum over a leg's own limit regions with one segment test feeding rule, price and splitter; a sharp corner charged at the ladder's floor with its four causes counted apart; readiness through both doors and both layers, the coastline's load triggered and its reason named in both locales; the ETA's rise attributed to the zone time the midpoint rule was dropping; the nine should-fixes closed with a reading each, five dead members named as they went; the tolerance, the ceiling, the berth and the band untouched; and performance closed against a **moved** target with its reason beside the new number and each lever's own measurement. Dropped: the geometry prune, named and not taken. Parked: nothing.

**Level 1 — Date:** 2026-09-21 · **Source:** the independent review of §17's pass — its three blockers and its should-fixes, in the order they close · **Cursor:** item 6 · **Closed:** 2026-09-21 — exhausted, six items resolved with their readings
- [x] 1 · The corner's price is a second quantity beside the clock — resolved: `Turn.priceSec` is handed the **world's own nominal at both stubs** (`∞` … the corner's chords … `∞`), so the splitter resolves what is in force over the metres each stub really covers and only the emitted chords carry a limit of the corner's own; the ≈ 280 s was a leg's dearest limit charged over metres standing in open water. A zone-carrying world now asserts the price and the drawn clock agree within the slack (`TautSearchTest`'s new test — the class's other invariant cannot see it, its world carrying no zone), and the nominals are pinned by a **recording clock** in `TautEasingTest`
- [x] 2 · The corner's reference speed is a per-leg maximum — resolved: **`TautGraph.limitAt(vertex)`** is the one reading — a zone's limit where its interior covers the vertex, the band's where it hugs the shore, `∞` otherwise — taken once per vertex at the build and read by `TautEasing.fit`, by the corner's price and by the assembly's brake-and-accelerate term alike, while the edge's own maximum (`edgeLimitKn`, `LegLimits.limitKn`) is **deleted as unread**. Acceptance pair: **63 vertices · 524° turn** against the 70 · 536° the maximum produced, ETA 25.49 → **25.26 min**
- [x] 3 · The readiness gate latches and its reason reaches no surface — resolved: `RouteSpatialAdapter.loadIfNeeded` **waits out** a coastline load already in flight (`state.first { it !is Loading }`) before the mutex re-reads the layer, so the cold-start order no longer leaves the wall unbuilt for the session; a tap on a not-ready square asks for **one** more preparation (`RouteViewModel.prepareAgain`), arms when it can, and otherwise **shows** the refusal by `RouteUnavailableReason.labelResId` on the map's own transient status line inside the route's slot — no screen, no panel, no key added. The toggle's stale KDoc is corrected and its dead `enabled` knob removed: the square is never without a tap
- [x] 4 · The should-fixes of the same review — resolved, all five: `TautZone.covers` no longer claims the home the rule and the price left (its KDoc names its two point-shaped readers); the margin's reading runs over **the price's own legs** — the chain's edges, not the drawn pieces — so `berthPriceSec` and `inMarginM` are one fraction on one leg set, the acceptance pair's in-margin reading moving 1.03 → **0.66 km** and `TautAssertionsTest` asserting the price is the reading at the pace twice over with the drawn line's own sum asserted **different**; the corner's price's target is read by a recording clock; `RoutePlanTiming`'s orphaned KDoc is re-homed onto `arcPriceSec`; and the harness prices the band from `AppConfig.zoneRegulatorySpeedKn` rather than at a test literal
- [x] 5 · The two other harness pairs are still refused — resolved as a **named absence, not a corridor failure**: the ends' own readings, now printed with every refusal, show **both starts resolving onto land** (Cap d'Antibes → Golfe-Juan: start on water false, coast 470 m, aim on water false, coast 131 m; Antibes → Nice: start on water false, coast 293 m, aim on water true), so the water truly offers none from those coordinates and the engine says so — `TautRefusal.START_OFF_WATER`, refused before the graph is built, which takes the pair from 1 643 ms to **73 ms** and from 5 373 ms to **179 ms**. Snapping the start onto water was not taken: the start is the boat's own position, and moving it silently would break the rule about what the line starts from
- [x] 6 · Every pair and the split re-measured after items 1–3 — resolved: acceptance pair **63 vertices · 524° · worst 52° · 5.35 km · ETA 25.26 min**, split **harvest 280 ms · graph 138 ms · search 1 003 ms** (wall 1 423 ms) over 431 vertices, 3 682 edges and 92 665 pairs; crossings 38, in-margin **0.66 km**, berth price 515.29 s, longitudinal 0.04 s, unsounded 0.29 km, straight-line ratio 1.572; sharp 9 — guard 2 · build 0 · cutback 0 · water 7 — for a 0.04 s charge; the tolerance lever prints 12.5 m deleting 1.47 km² and leaving 9 sharp of 19 in a 524° line; the grown-corridor probe still reads *only a priced crossing* (38 legs, 429 vertices). `apk-build.bat` green, **123 focused tests green** (18 suites under both name filters) and the harness green under `-Dmaro.prebake=true`

- Resolutions: six items settled in one pass — the stubs priced at the world's own nominal with the corner's reference limit the one **in force at its vertex** (and the edge maximum deleted as unread), which brings the acceptance pair's vertices to 63 and its clock to 25.26 min; the readiness gate un-latched through both doors, the coastline's in-flight load waited out and the refusal spoken from the reason's own string on a surface that already exists; the five should-fixes closed with a reading each, the margin's reading moved onto its price's own legs; the two inshore pairs named as ends standing off the water rather than as corridor failures; and every figure re-read. Dropped: none. Parked: none.

**Level 1 — Date:** 2026-09-21 · **Source:** the independent review of the six-item level above — its one blocker and its seven should-fixes, in the order they close · **Cursor:** — · **Closed:** 2026-09-21 — exhausted, six items resolved with their readings; items 7–9 are kept as resume points, two of them the user's questions and one the review's observations
- [x] 1 · The **band's** limit is still the midpoint rule the rest of the pass removed — `LegLimits.regionsOf()` resolves `bandLimit` once per leg from `inCoastalBand()`, a midpoint boolean, and hands it to every piece, so a leg straddling the 300 m line is the one limit change the splitter never cuts; closes by cutting the band at its own crossing, a bisection on `distanceToCoastM` in the same home that already cuts the zone rings, or by stating that the band's limit is legal only where a leg lies wholly inside or outside it. It is an **inherited** limit — the band's shape is not a polygon the tracers inherit and the mesh engine shipped the same per-edge mark — and it stands only because this pass claims the leg cutting complete while the epic's Description calls each leg the limit in force over it. **Decided 2026-09-21, the build still owed:** a point is added at **every** crossing and the leg is split there, for the 300 m band and for any regulated zone alike, because each piece's speed depends on it — the zone's crossing is the segment test that already exists, the band's is a **bisection** on `distanceToCoastM` since the band is a distance and not a ring, the number of pieces is the number of crossings + 1, and a leg's **spans** come from `LegLimits.regionsOf` as the one home the search's price, the drawn splitter and the crossing report all read. The item closes when the code cuts the band and the acceptance pair is re-read, the discussion's own objection — the trip figure will read slower than the boat makes good — being the user's to settle rather than the build's (design of record §18) — **resolved 2026-09-21: the predicate read first, and it decided the geometry the other way.** `distanceToCoastM` is the distance to the nearest coastline **segment** (the index's own `pointToSegmentDistance`), so the crossing is **closed-form and the bisection was not taken**: [`TautBand`](app/src/main/java/ykws/android/maro/spatial/taut/TautBand.kt) gathers the corridor's coastline segments, buckets them in the corridor's own uniform grid and returns per segment the one interval `{t}` inside the width — an affine **slab** plus the two **endpoint circles**, each convex in the leg's parameter — merged into the leg's own inside intervals; `LegLimits.regionsOf` merges those with `TautZoneSet.limitSpans` into one breakpoint list and reads each piece's limit once, so the per-leg boolean is gone, the signature and the one-home role are unchanged, and **no edit was needed in `TautSearch` or the assembly**. A cut point is collinear and **free** (`RouteTurnGeometry.turnPriceSec(…, 0)` is nought, pinned). The straddling test is `TautAssertionsTest` 16 — the boundary **mid-leg** with the leg's own middle outside, two pieces, the limit over the 200 inside metres alone, the price equal to the drawn clock within the slack — with the midpoint rule's own answer (one piece at the cruise) as its control and the middle's own distance printed as the reading that rule used. The build's bill, taken rather than assumed: **harvest 181 · graph 158 · search 799 ms** (wall 1 140 ms) with **694 band segments**, over the same 431 vertices, 3 682 edges and 92 665 pairs against 307 · 221 · 937 ms before (the pass's own last reading being 138 ms of graph), so the spans sit inside the build's run-to-run noise rather than above it. The pair moved as §18 said: **34 vertices · 205° turn · worst 38° · 5.19 km · ETA 25.26 → 33.63 min**, crossings 38 → **26**; the objection stands as the user's, not settled here
- [x] 2 · A rule refusal is counted and charged as the water's — `TautSearch.fits()` returns false both when the water refuses the curve and when run one forbids the zone's interior, so `sharpByWater` counts a refusal the water never made and `sharp()` charges it at the ladder's floor from a cruise reference, ≈ 30 s a corner rather than the lower bound its KDoc claims; closes by counting the rule refusal apart or by exempting it from the floor charge — **resolved 2026-09-21, both halves:** `TautSearch.fits()` now answers with the **cause** of a refusal (`TautEasing.SharpCause?`) instead of a boolean, so a curve the run's own rule forbade is `SharpCause.RULE`, counted apart as `sharpByRule` and printed by the harness; and a rule-refused corner is driven at the water's own reference speed, so §12.3's pair costs it nothing — the ladder's floor is a bound read off a radius the *water* refused, and read at a rule's refusal it was ≈ 30 s a corner of a slowdown nothing forced. Pinned in `TautEasingTest` (the rule's refusal is counted as the rule's, its price does not move with the longitudinal ceiling, and the difference from the water's own corner is the pair's own arithmetic). On the pair the counts read **sharp 4 — guard 1 · build 0 · cutback 0 · water 3 · rule 0**, the nought being run two's own answer, a rule refusal arising only in a run-one answer. **Deferred, named as deferred:** the level's own alternative — drawing the arc and cutting it at the zone's boundary instead of refusing it — changes the drawn line and is the user's to choose
- [x] 3 · The margin's exclusion reads a point while the price reads an interval — `TautZoneSet.berthFraction()` skips a zone only where `covers(midpoint)` holds, so a leg clipping a zone's corner pays the zone's limit over the clip **and** the berth's ceiling together, and both `berthPriceSec` and `inMarginM` are inflated by it; closes by excluding a zone whose `interiorSpans()` the leg shares, the predicate the rule and the price already read — **resolved 2026-09-21:** the exclusion is now `TautZone.entered` — the interval the leg shares with the interior, through the very predicate the rule and the price read — so the clip is charged the zone's limit and the berth's ceiling no longer lands on the same metres. Pinned in `TautAssertionsTest` 10 (the clipping leg's fraction is 0; a leg that only grazes the boundary shares nothing and still pays, the margin being read live in the synthetic berth tests that still pay). Both figures re-read on the pair: **in-margin 0.66 → 0.00 km, berth price 515.29 → 0.00 s** — the line also moved with item 1's cut and its own legs carry no margin at all now
- [x] 4 · Item 2 of the level above has no assertion that fails on its revert — `limitAt(vertex)` is read by the search and by the reference Dijkstra in the same test file, which shares the call and so cannot disagree with it, so reverting it to a leg maximum moves a printed pair figure and no test; closes with one synthetic case, a corner outside a zone whose leg clips it — **resolved 2026-09-21:** `TautAssertionsTest` 18 is that case — a zone whose south-west corner is a graph node **outside** it while the leg leaving it shares the interior — asserting the corner's reference limit is the vertex's one, with a vertex standing inside the zone reading the zone's limit as its control, and with **the value a per-leg maximum would return (5.0) asserted beside it**, so the revert is a number this test refuses rather than a claim in prose
- [x] 5 · The phantom's own quantity is unreadable where it matters, and the named absence reaches no user — the harness prints neither `pricedSec` nor the drawn-against-priced gap, so a recurrence of item 1's phantom on the acceptance pair would show only as the ~14 s a different line moves; `START_OFF_WATER` warns and returns `OutsideWater`, which every surface reads as an ordinary empty draft while the readiness refusal does reach a line; and the aim keeps the misattribution the start lost, `TautGraph.build()` falling back to the raw aim so an aim standing on land reads as a corridor failure — **resolved 2026-09-21 in its two technical halves:** the harness prints **priced · berth · drawn · gap** per pair (the acceptance pair reading **priced 2018.24 s · berth 0.00 s · drawn 2017.66 s · gap 0.58 s of 2018.24 s**), so a recurrence of the phantom would be a printed figure rather than the seconds a different line moves; and the aim's own end is named — `TautRefusal.AIM_OFF_WATER`, raised **before the graph is built** where the fall-back to the raw aim would have reported a corridor failure — the way the start already is. **Deferred, named as deferred:** the **surface** on which a user would read a named absence (`START_OFF_WATER` and its kin read by every surface as an ordinary empty draft) changes what a user sees and is the user's
- [x] 6 · Two seam KDocs deny what ships — `RouteEngine.prepare()` still says the taut-string design is parked and no such engine exists in the tree, while `TautRouteEngine` is switched in at one expression in `MapScreen`; and the adapter's "nothing here fetches" is now false, `loadCoastline()` running the OSM generation pipeline on a cache-and-bundled miss — **resolved 2026-09-21:** the seam's KDoc now names the tracer that ships (built in `spatial/taut/`, switched in at one expression in `MapScreen`) with the mesh engine and its bake kept in the tree wired to nothing, and its readiness paragraph no longer calls the mesh engine the shipped one nor promises that a live-reading engine stays permissive; and the adapter's "nothing here fetches" is replaced by what really happens — each layer is asked for its own load on a miss, the coastline's running the OSM generation pipeline when neither cache nor bundled asset answers, while the regulation alone is never asked for and prices open water when absent
- [ ] 7 · **The user's, deferred rather than closed** — where a named absence is *shown*: `START_OFF_WATER`, `AIM_OFF_WATER` and their kin warn and return, and every surface reads that as an ordinary empty draft, so a name the design's §13.3 itself requires ("every no-route case returns a named reason rather than an empty answer") is spoken only to a log reader. Resumes when the surface that carries it is chosen
- [ ] 8 · **The user's, deferred rather than closed** — the arc-cut alternative: run one's rule refuses to round a corner **into** a zone's interior and leaves it sharp, where the water under it is legal and the crossing splitter could instead draw the arc and cut it at the zone's boundary. It rounds a corner the level left sharp, so it changes the drawn line, and it resumes on the user's word
- [ ] 9 · The review's five code-health observations, none blocking, kept so they are not lost — `TautEasing.SharpCause` is now the fit test's own vocabulary and its name misleads; `TautBand.of` adds a second `landPolylinesIn` per build; the grid walk is now one home serving the wall index and the band; `berthFraction`'s interval exclusion is structurally dearer than the point read it replaced; and `regionsOf` names the leg's far end as itself, so a drawn line ends exactly on its vertex

- Resolutions: six items settled in one pass, and the band stopped being a statement about a leg and became one about a **piece** — `distanceToCoastM` was read first and found to be segment distance, so `TautBand` closes the crossing in **closed form** and the planned bisection was not taken, and `LegLimits.regionsOf` merges the band's spans with the zones' into one breakpoint list read once per piece, with **no edit needed** in the search or the assembly. A rule refusal is counted and charged apart; the margin excludes a zone by the interval the leg shares; the corner's reference limit gained the assertion that fails on its revert; the harness prints priced · berth · drawn · gap and the aim's own end is named before the graph is built; and two seam comments that denied what ships now describe it. Figures: the acceptance pair reads **34 vertices · 205° · worst 38° · 5.19 km · 33.63 min** against 63 · 524° · 52° · 5.35 km · 25.26, the clock's rise being the band's limit now charged over the metres that really stand in it — the objection §18 left standing, and the thing a user sees differently. Dropped: nothing. Kept as resume points: items 7 and 8, the two questions that are the user's, and item 9, the review's observations recorded rather than acted on.

**Level 1 — Date:** 2026-09-21 · **Source:** the independent review of the band pass — **verdict revise**, four blockers and six should-fixes, in the order they close · **Cursor:** — · **Closed:** 2026-09-21 — exhausted, five items resolved with their readings; the pass also found and fixed a hung graph build
- [x] 1 · **The band's closed form is a superset, not the interval it claims** — resolved: the slab is now **intersected with the projection's own range** before it is absorbed and the caps are added after it, so "within the width of the line" is only ever read where the nearest point of the line is a point of the segment; the four unnamed cases exist in [`TautBandTest`](app/src/test/java/ykws/android/maro/spatial/taut/TautBandTest.kt) — parallel-beyond-end, the coincident tangent roots, a cap-entry with a slab-exit, and two disjoint capsules that stay two spans. Running them found **two further defects**: a tangent contact produced a float-epsilon sliver, now refused by a stated millimetre floor (`MIN_SPAN_M`, a touch is not an interval), and the locus cross-check demanded that the leg's **own** ends lie on the width, which is a claim about the caller's geometry rather than the band — interior ends are checked now, which is also why the harness's worst case fell from **274.11 m to 0.02 m**. Acceptance pair: 63 → **54 vertices**, 524° → **307°**, worst 52° → 30°, 5.35 → 5.44 km, 25.26 → **27.40 min**
- [x] 2 · **The berth's only real-water reading is a zero, and the exclusion forgives a whole leg** — resolved: the exclusion is **per piece** — the interior's own span comes out (`outsideSpans`) and the remainder is priced zone by zone, each piece weighted by its share of the leg — and the acceptance pair's figures are re-read, which is the control the zero could not give: **in-margin 0.00 → 0.17 km and berth price 0.00 → 135.90 s**, so the 25 m berth prices real water again rather than being forgiven whole legs. `inMarginM` still runs over the price's own legs, so the reading and the price remain one home
- [x] 3 · **The rule refusal's bucket is pinned nowhere it can be non-zero** — resolved: the refusal is now **produced** where it is read — one synthetic run-one world whose corner's rounded chords enter the zone asserts `sharpByRule == 1` on the search's own answer in [`TautAssertionsTest`](app/src/test/java/ykws/android/maro/spatial/taut/TautAssertionsTest.kt), and the zone-carrying reference pair in [`TautSearchTest`](app/src/test/java/ykws/android/maro/spatial/taut/TautSearchTest.kt) asserts it again where run one is in force — so the lambda-only pin and the duplicated predicate are both gone. The acceptance pair's own count stays **0**, and that is now a reading with a reason rather than a nought: it answers on run two, and the rule belongs to run one
- [x] 4 · **The 8.4 minutes are a claim, not a reading** — resolved, and the reading **refutes the claim the pass made**: the harness prices the same drawn chain twice, under the band's spans and under the removed midpoint rule — **1 644.19 s against 1 644.19 s, the band's own share 0.00 s (0.00 min)** over 53 drawn legs and 45 band spans. So the clock's 25.26 → 33.63 → 27.40 min was the **search choosing a different line** with the band priced honestly, not the band re-pricing a line that was already drawn; the earlier "+8.4 minutes is the band's charge" is withdrawn, and what the band costs on this water is a selection, not a sum
- [x] 5 · **The review's six should-fixes, none of them a behaviour** — resolved: the band's span ends are checked against the world's own `distanceToCoastM` in the harness's own reading (**0.02 m** worst, once only interior ends are read); the shared grid walk indexes with `floor` and walks a **bounded supercover** — `|Δcol| + |Δrow|` cells — and it was the walk's unbounded shape that **wedged every suite run**, one step at zero while the other index never reached its last cell, which is a hung graph build for two hours and every wall still unseen; the zone's intervals are read **once** per candidate pair and handed to the price and the berth; the Dijkstra reference is handed the search's own clock and fit predicate and runs on a zone-carrying world; §18's piece count is corrected to the **merged intervals + 1**; and item 17 asserts the cut's own seconds as well as its angle

- Resolutions: five items settled, and the pass found two defects the review could not see — a **tangent's float sliver** and a **grid walk that could not terminate**, the second being why no suite had completed since the band landed. The band's geometry is the interval now rather than a superset of it, its span ends stand on the world's own locus within 2 cm, the berth prices real water again, and the band's share of the clock is a measured zero at fixed geometry. Acceptance pair: **54 vertices · 307° · worst 30° · 5.44 km · 27.40 min**, split **350 · 258 · 1 181 ms** over 431 vertices and 92 665 pairs, crossings 30 · in-margin 0.17 km · berth 135.90 s · priced-against-drawn gap 0.18 s; the two inshore pairs are still refused by name. **18 suites green, 0 failures.** Dropped: nothing. Parked: nothing — the level is exhausted.

- Resolutions held from the review, so the next pass does not re-argue them: §18.1 step 4 **holds** (the search's step, the corner's price and the drawn clock all reach `regionsOf`, with `TautBand.spansOf` a sanctioned source merged inside it and no second crossing-finder beside it); item 16's control **really does turn red** on the midpoint revert (two pieces demanded, the 5.0 asserted, the midpoint 400 m off a 300 m band); the closure with three resume points is the walk's own documented shape; and item 3's clip exclusion is genuinely pinned. The failures are geometric and evidential, not structural.

**Level 1 — Date:** 2026-09-21 · **Source:** §19.1's plan for the drag and the search's cost, re-ordered by the tolerance experiment and priced as options in **§19.2** — which is where the order, each lever's cost and the agent-versus-user split are read, and re-priced a second time in **§19.3** once items 1–5 were in · **Cursor:** — · **Closed:** 2026-09-21 — exhausted by the cursor, seven items resolved with their readings; items 7 and 8 are kept as resume points rather than resolved
- [x] 1 · **The counters, and the split they produced** — resolved 2026-09-21: the search carries its own price for the work inside a station (`fitsBuilt`, `fitMillis`, `clockCalls`, `clockMillis`, on the dossier and printed by the harness), counted at the two expensive doors only — the easing fit and the drawn clock — so measuring cannot become the cost being measured. On the acceptance pair the search's **792 ms** is **6 274 fits at 593 ms** (75 %) and **6 274 clock evaluations at 145 ms** (18 %), leaving ~54 ms of expansion: **93 % is the per-corner work, ~95 µs a fit, at only 1.6 fits a station**. That decides the plan the way §19.2 said it would — the vertex levers are for the pair loop and memory, and **the per-corner work is the lever that can reach a fast search**. The same run is a caution about the instrument: this configuration read 1 181 ms before and 792 ms now, so a lever's number is a pair of runs
- [x] 1a · **And the fit's own inside, which names the lever** — resolved 2026-09-21: `TautEasing.fit` reports, per candidate the ladder builds, the nanoseconds of drawing the curve against judging it, and the search accumulates them onto the dossier (`candidatesBuilt`, `candidateBuildMillis`, `candidateWaterMillis`). On the acceptance pair: **9 798 candidates, draw 19 ms against water 652 ms** of a 681 ms fit. So the lever is the **judge, not the curve** — `curveOnWater`'s point queries and its per-chord wall walk, on a handful of points repeated across thousands of candidates — and **item 2 is a cheaper judge** (a run-scoped memo of those two queries, exact-keyed so no answer can move), with 652 ms of water tests standing between this search and half a second
- [x] 2 · **A cheaper judge — and the measurement refused the shape the lever was planned in** — resolved 2026-09-21: [`traversable`](app/src/main/java/ykws/android/maro/spatial/taut/TautObstacles.kt:109) and [`curveBlocked`](app/src/main/java/ykws/android/maro/spatial/taut/TautObstacles.kt:140) are memoised **per harvest**, exact-keyed (a rounded key would answer about a *neighbouring* place, and the water's answer is constant over no cell), with the questions asked and the answers the memo gave carried on the dossier and printed by the harness. Its acceptance is **met**: the drawn line is the recorded one to the digit — 54 vertices · 307° · worst 30° · 5.44 km · 27.40 min, crossings 30 · in-margin 0.17 km · berth 135.90 s — and the water figure moved **652 → 601 ms**. But **161 051 questions and 14 418 memo answers is a 9 % hit rate**, so the water's milliseconds are **new work per candidate, not repeated work**, and the plan's target is **re-stated rather than met**: 601 ms is the floor a *cheaper question* — the point query's own cost, or the points the ladder asks about — has to beat
- [x] 3 · Say it on the device — resolved 2026-09-21: the three phases, the stations and the fit's own inside are **one log line per answered route** on the channel the refusals already use (`TautRouteEngine`'s `warn`, tag `MaroTautRoute`), where before a *success* said nothing at all and only a refusal spoke; and the draft's `searching` became the panel's **third state** — `route_searching` ("Searching for a route…" · "Recherche d'une route…") in both locales, so a slow search, an unasked aim and a refusal stand as three sentences rather than two. The counters are what a device run has to carry beside the phases, a JVM split not being the phone's
- [x] 4 · Cancellation inside the loops — resolved 2026-09-21: a `cancelCheck` is threaded into the harvest's per-shape loop, the graph's per-outer-vertex pair scan and the search's per-corner door, each handed the coroutine's own `context.ensureActive()` from the engine's four call sites, so an abandoned drag stops **inside** a phase rather than at its end. It is the abort's latency and nothing else: no counter moves, no geometry moves, and the corner door checks its own cache *before* it asks
- [x] 5 · Coalesce — resolved 2026-09-21: the preview writes its aim into **one slot** whose content the newest aim replaces, and a single drain loop serves it, so a drag starts **one search per completed search** rather than one per frame and no search is killed to start another. The loop ends when the slot is empty — and *that* is where `searching` goes false, so a refusal is never shown on a frame that still has work behind it — or the moment the mode leaves the draft; a newest aim landing while it winds down restarts it. Confirmation and exit clear the slot, so nothing is served after the route is locked. **Pinned by a reading rather than by the reading of the code:** [`RouteEngineSeamTest`](app/src/test/java/ykws/android/maro/ui/map/RouteEngineSeamTest.kt) gained a **gated** foreign engine — one that holds its answer until a test releases it, the one state a synchronous fake cannot show — and with two aims arriving while a search is in flight the assertions are that the engine was asked **once**, that the next ask is the **newest** aim and never the skipped one, and that `searching` clears with the slot
- [x] 6 · The vertex filter §10.3 step 2 already asks for — **closed as built, by reading rather than by measuring**: [`protrudesIntoWater`](app/src/main/java/ykws/android/maro/spatial/taut/TautGeometry.kt:163) **is** the non-convex test and both harvests apply it ([`TautObstacles.cornersOf`](app/src/main/java/ykws/android/maro/spatial/taut/TautObstacles.kt:429) for the walls, [`TautGraph.collectCorners`](app/src/main/java/ykws/android/maro/spatial/taut/TautGraph.kt:890) for the zone and hole rings), the per-obstacle simplification with the one-sided outward tolerance ships beside it, and step 2's **next** clause — "plus every vertex of a non-convex obstacle" — would keep *more* vertices than ship, so the extreme-vertex clause has nothing left to remove on this water. The one thing left is a **stricter** rule the item does not contain — a protruding corner pruned for facing away from the corridor — which can delete a bend; it is re-asked and priced as an option in **§19.3** rather than taken here, the gate having moved once the reading showed the item was already satisfied
- [ ] 7 · The corridor as a measured lever, after item 6, with the two refused pairs as the guard — **kept 2026-09-21 by the user's word: not dropped, not resolved, and read before it is taken.** The reading: [`sizedFromObstacles`](app/src/main/java/ykws/android/maro/spatial/taut/TautGraph.kt:607) unions the clamped extent *into* the guess and inflates by the berth again, so the corridor can only ever **enlarge** and it is the guess that decides which walls the harvest ever *sees*; the guard the item names is **vacuous**, both inshore pairs being refused by `START_OFF_WATER` before the corridor exists (§17 item 5), so a real guard — a pair whose only way round lies beyond the margin — has to be built **before** the lever, which reverses the order the item reads in; and the 512 ms it would buy (harvest 292 · graph 220) is the same 512 ms §19.3's row **b** buys **without** spending the containment guarantee, which is why it is kept as a lever of record with its own price rather than taken
- [ ] 8 · The engine's statefulness decided on the device split rather than before it — **built 2026-09-21, its two blockers closed, and the reading that decides it taken.** C1: one unique stamp per query in the wall index and the band — the two-thread test read **13 960 of 100 000 answers wrong** under the old plain `stamp++` and **0** on the fix, so a leg crossing land was *reachable*; the sharing KDoc corrected with it. C2: the licence is now the **largest box a cold search could build from the rough corridor** — its revert turns the drag reading red (outward reuse 5/6 where cold builds wider), and on the fix it holds **5/6 inward, 0/6 outward**. C4: the generation hole closed by counting each layer's identity, so a depth refresh invalidates like a first load. C5/C6: `over`'s obligation stated, `of` named the tests' cold entrance, the terrain key given one factory, `terrainReused` made the answering attempt's flag, and `cancelCheck` honoured by the zones and the band. **C3, the decisive reading: over 6 aims of one drag the licence held 5 and the warm line differed from the cold one on 5**, never dearer (0.01 s on three aims, cheaper on two, ~6e-6 relative) — so with the cache the route is no longer a function of its start and aim alone but of the drag's history. **The number that decides the rest is missing: how far apart the two lines stand in metres, per aim.** On it the choice is the **slice** (the kept terrain filtered to the new box in memory, exact) or accepting the divergence and re-scoping the guard from *the same line* to *never dearer* — and it is the user's, because it is the line they see

- Resolutions: seven items settled — the counters and the split they produced; the fit's own inside naming the **judge** as the lever; the memoised judge measured, its acceptance met on the pair and its cache gain **withdrawn** on a 9 % hit rate; the three phases and the stations carried as one log line per answered route with `searching` the panel's third state; cancellation reaching inside the harvest's loop, the pair scan and the corner door; the drag coalesced into one slot and one drain loop, pinned by a gated foreign engine; and the vertex filter **closed as already the code**, the stricter prune re-asked as an option rather than taken. Kept as resume points: item 7, the corridor lever, kept at the user's word with its guard found vacuous; and item 8, the engine's statefulness, built with its two blockers closed and its decisive reading still **not taken** — how far apart the warm line and the cold one stand in **metres, per aim** — which is the number the choice between slicing the kept terrain and re-scoping the guard from *the same line* to *never dearer* turns on, and it is the user's. Dropped: nothing.

- Note, 2026-09-21: the list above was renumbered once the measurement re-ordered it, and its old item 6 ("the lever that reaches a fast search") is **deleted rather than kept**, its content now living in items 1a and 2 — a duplicate lever in a walk is worse than a shorter list.

**Level 1 — Date:** 2026-09-21 · **Source:** the fixed plan — the eleven steps the independent review cut to nine and four landings since have carried to twelve, in ship order, running on §19.3's re-priced levers and on the device reading the log line carried · **Cursor:** item 1 · **Closed:** — open
- [ ] 1 · **The goal and its stopping number** — the phone's route latency, and the figure at which the work ends. The user's, and every item below waits on it
- [x] 2 · **The device reading recorded with the re-pricing it forces** — resolved 2026-09-21: the entry is the design's **§19.6**, five answers over two captures on a Pixel 7 with the debug provenance stated beside them, step a closed as measured, §19.3's rows re-priced one at a time, and the two readings that are **not** timings named so a later run cannot misread them — the degenerate previews and the judge's tallies riding the kept terrain
- [x] 3 · **The graph read and decided in one step** — **read and decided 2026-09-21, built 2026-09-21** (the reading kept, the build added): the pair loop's reading stands as it was recorded — it reads **only the pair** (`allowed`, `zones.limitSpans`, `legLimits.priceOf`, `zones.berthFraction`) and never the ends, `cruiseSpeedKn` entering once in `LegLimits` — and the build is [`TautGraphBase`](app/src/main/java/ykws/android/maro/spatial/taut/TautGraphBase.kt), the corner set and its edges with the two ends left out: `of` is the O(N²) corner scan, `over` adds the ends and scans **their rows alone** (O(N)) beside the kept edges, and the ends' rows are scanned in the order a cold build scans them with the kept edges following it, so a reused graph is a cold one field for field — which is why the edge counts and the drawn line come out identical rather than merely close. The licence is `licenses(terrain, cruiseSpeedKn)` — the terrain's **identity**, carrying the box, the zones and the world's own generation, plus the pace the kept edges were priced at — the store is one `@Volatile` entry beside `keptTerrain` on the engine, and wherever the licence fails the graph is rebuilt exactly as today. Guarded in `TautTerrainTest`'s five tests: the same aim twice reads **7 pairs against 10** (`2·5−3` against `5·4/2`) over the same 5 edges; an **aim off the water** — the one place the corner set depends on the ends — reads warm 4 vertex(es) · 3 edge(s) · **5 pairs** against cold 4 · 3 · **8**, the same line; a **pace** the base was not priced at and a **moved world** each rebuild it (a cold build's pairs, and a cold engine's line at that pace); and the licence is read at its own type. The licence's drag rate is unmoved at **5/6 inward, 0/6 outward**. **The harness re-read, as a pair of runs** (§19.6's own caution): the acceptance pair is the recorded line to the digit — **54 · 307° · worst 30° · 5.44 km · 27.40 min**, map **431 vertex(es) · 3 645 edge(s) · 92 665 pair(s)**, crossings 30 · in-margin 0.17 km · berth 135.90 s · grown-corridor probe *only a priced crossing* (30 legs, 429 vertices) · band share 0.00 s · judge 161 051 asked, 14 418 from the memo — the split reading **218 · 224 · 886 ms** (wall 1 378) and **301 · 171 · 701 ms** (wall 1 176), and both inshore pairs still refused **by name** (`START_OFF_WATER`, 72/183 and 76/176 ms). The keeping's own pair on that very pair: graph **85 → 1 ms** over **92 665 → 859 pairs** (`2·431−3`), harvest **58 → 0 ms**, wall **753 → 160 ms**, **the same line, 54 against 54**, the log line reading `terrain reused, graph kept`. **§19.6's desktop `3 682 edge(s)` is corrected rather than matched:** the old loop written out against the same world reads 92 665 / **3 645** / 431 — identical to the split build's own — so nothing moved and the recorded figure was stale; and the split's one difference is named with it: on Cap d'Antibes → Golfe-Juan, an **off-water start**, the cold build examines **233 584** pairs against the old loop's **232 903**, the 681 being the corner an end stands on, which the base scans before the ends can dedupe it away, over the same 10 775 edges and 683 vertices. 68 focused tests green (61 under `spatial.taut` plus 7 in `RouteEngineSeamTest`), the harness green under `-Dmaro.prebake=true`, `apk-build.bat` green with no new warnings
- [x] 4 · **The candidate cheaper water questions named and priced** before any is built — **priced 2026-09-21, and the price refused two of the three while the split moved the lever's own target.** Each candidate is a switch on *what is asked* rather than on what the test means, read **twice** through one instrument (the search's own code shape, with the judge's door counting and timing the two question kinds apart) — **and every figure below is the hop's own console dump, `tmp-hop-*.log` in the repository root: gitignored and therefore outside this record, and no run of the shipped tree reproduces it, the instrument having been the hop's own; the figures are carried here so the numbers survive the logs.** **The shipped pair first: water 480 · 494 ms — points 90 867 asking 444 · 460 ms, ≈ 4.9 µs each, against chords 65 424 asking 24 · 26 ms, ≈ 0.37 µs** — so **92 % of the water's milliseconds are the point query's** oracle and the per-chord wall walk has five percent to give, which is not the shape §19.3's row c was planned on. **The two counted kinds are the search's own doors while the total beside them is the instance's**: `judgeAsked` is `waterAsked + chordAsked` over the obstacles, so it also carries the questions the **build** asks before the search opens — one water question per harvested corner (the filter deciding which corners become vertices) and the **midpoint** question behind every pair that survives the wall test — counted and never timed, the clock sitting in the search, which is why points + chords falls **4 743** short of the judge total in **every configuration**, a shortfall no sampling lever moves. **A · fewer points a candidate** (the ladder's own emission 10 m → 30 m, the arc's geometry untouched): water **245 · 245 ms**, questions 161 034 → 93 752, candidates 9 798 → 9 822, search 629 · 642 → **364 · 359 ms** — the cheapest by far, and **refused as moving the line**: the pair's six metrics hold except its vertices (54 → 44) while the chord deviation rises **0.06 → 0.31 m**, and on the moved aims a bend between two samples goes unseen so the worst turn reads **35° → 30°** (33° → 30° on the third). **B · an early exit on the first blocked chord** (the chord asked with the point it leaves, so a wall refuses at the first chord instead of after every point): water **375 · 420 ms** with **72 048** points asked, search **512 · 573 ms**, dearer on no run — and **the only candidate whose line cannot move**, the six metrics, the drawn map and the moved-aim reading being the shipped ones to the digit; the saving is the **18 819 point questions a refused curve no longer asks**, which is the same lever A pulls, pulled without touching the sampling. **C · a cheaper oracle** (the bucket grid 100 m → 20 m): **refused on the walk it targets** — the chord walk 24 · 26 → **47 · 44 ms**, dearer on **both** runs, and that is the whole of the refusal: a finer grid puts **more** cells beneath the same chord, since a chord crosses a fixed distance through a finer partition, so its walk is dearer by construction rather than by a trade against the segments it saves. The run's **total water is discordant across the two readings** (dearer on one, cheaper on the other), so the aggregate is not what the candidate is refused on, and the graph and the search beside it (216 · 198 → 321 · 393 ms, 629 · 642 → 726 · 664 ms) are context rather than evidence. **Not priceable in this hop, named as such:** a cheaper **point** oracle — the half that owns the 92 % — has no exact variant inside this surface, the point's answer being the coast index's own rule (a nearest-**mainland**-segment side test plus a real-island override) and the depth grid's bilinear sounding, and a local reproduction is a build whose exactness the same reading would have to prove; its ceiling is the measured 444 · 460 ms, and that is the number a build must beat.
- [x] 5 · **The chosen cheaper question built** — the six metrics and the drawn line unchanged as its guard, the acceptance pair read twice. **The guard is the six metrics and the drawn line, never the counters**: fewer point questions mean fewer memo entries, so `judgeAsked` and `judgeMemoHits` move with the sampling and a counter pair cannot stand as the thing a line is accepted on — the counters only pre-screen whether there is anything to measure at all (§19.3's protocol) — **resolved 2026-09-21: the walk ends at the first blocked chord, and the counters read the price's own figure to the digit.** [`curveOnWater`](app/src/main/java/ykws/android/maro/spatial/taut/TautObstacles.kt:252) now asks each point **with the chord it leaves**, so a wall refuses a candidate at that chord instead of after the whole point walk, where before every point was asked and the chords followed. **The answer cannot move, and that is structural rather than measured**: the predicate is an **AND** of questions that are pure functions of the place (or the chord) and the world, memoised exact-keyed, so the order decides only *which* questions a refused curve pays for. **The split is printed apart at last, through the counters that already existed** — `pointsAsked` · `chordsAsked` on the obstacles, carried onto the dossier and printed by the harness and by the engine's own log line beside the water's milliseconds, with `judgeAsked` and `judgeMemoHits` summed beside them. **The pair, twice, in the shipped configuration** (the pricing pass's switches are gone and none returned): run one harvest 335 · graph 212 · search 817 ms (wall 1 428), run two harvest 321 · graph 221 · search 729 ms (wall 1 315) — both reading the same judge, **144 405 asked · points 76 808 · chords 67 597 · 11 416 from the memo**, water 539 · 446 ms, because the counts are deterministic and the clock is not. **What moved**, read against the same pair under the reverted two-loop shape on this machine: points **95 627 → 76 808** — **18 819 questions**, the priced figure to the digit; chords **65 424 → 67 597**, +2 173, the half the price never printed, a chord being asked now before the point that would have ended the walk at a seventh of a point query's cost; judge 161 051 → 144 405; memo 14 418 → 11 416. **What did not move, in all three runs to the digit**: the acceptance pair **54 vertices · 307° · worst 30° · 5.44 km · 27.40 min**, crossings 30 · in-margin 0.17 km · berth 135.90 s · sharp 4 (water 4) · gap 0.18 s · map 431 · 3 645 · 92 665 · 9 798 candidates · draw 20 ms; the two inshore pairs refused **by name** (`OutsideWater`, 83–89 ms and 175–187 ms); the grown-corridor probe still *only a priced crossing* (30 leg(s), 429 vertices). **No wall-clock gain is claimed from one run, and none from these two**: the water reads 542 ms under the reverted shape against 539 · 446, the search 813 against 817 · 729, so the two shipped runs stand 12 % apart on the search within §19.3's own spread and the reading offered is the **questions**. **The control is a test as well as a run**: [`TautObstaclesTest`](app/src/test/java/ykws/android/maro/spatial/taut/TautObstaclesTest.kt:1) pins it on a synthetic corridor — a spit crossing a curve's first chord reads **refused · 1 chord · 1 point**, where the revert to a full point walk reads 3 points — and its second test keeps the predicate able to answer yes with every question asked. **Green**: 20 suites · 154 tests · 0 failures (3 gate-off skips) and the harness green under `-Dmaro.prebake=true`, `apk-build.bat` green with no new warnings. No git write, no device, no new dependency, no setting added; the only non-production edits are the harness's own print and the new test.
- [x] 6 · **Item 8's metres taken in the harness** — **resolved 2026-09-21, and the divergence is real on two aims of the six.** `movedAimReading` now reads each line's own vertices against the **other line's polyline** — both directions, worst and mean — beside the leg times' own gap in milliseconds, so a bend moved a hundred metres cannot hide inside a vertex-to-vertex comparison. **The reading's own limit is stated where it is defined** — [`lineApartM`](app/src/test/java/ykws/android/maro/spatial/taut/TautRouteHarness.kt:551) is a **vertex-sampled two-sided maximum**: it samples each line's **vertices**, so a leg whose own ends sit on the model while its middle bows away reads **0.00 m** — aims 1–3 read 0.00 m apart while carrying **4.4, 4.9 and 14.1 ms** of per-leg price movement — which is why the **metres of line standing past the bound** are now printed beside the worst offset, the quantity a user comparing two drawn lines actually sees rather than the single point that stands furthest. One drag's six aims: aims 0–3 **0.00 m apart at most** (the same curve sampled the same way, though the point lists differ on three of them and their leg times by up to 14 ms), aims 4 and 5 **19.42 m apart** at most (mean 1.22 · 0.95 m) where the warm line rounds the bend the kept graph's own superset offers; the licence holds on 5 of 6 (aim 0 being cold by construction), the lines differ on 5, and the warm line is **dearer on 3 aims by 0.01 s** while being **cheaper on the two it diverges on** (−0.96 s, −0.09 s). **The bound stated with it is 12.5 m — the abstraction's own tolerance, `TOLERANCE_FRACTION` of the berth, read from the one home that owns it — and it is stated as what it is: a policy bound chosen at one tolerance, not the error two lines are already allowed to spend.** `TOLERANCE_FRACTION` bounds **one** answer's offset from the truth, so two answers each inside it can stand **twice** it apart, and the honest analogue — **2 × tolerance = 25 m** — is now named beside it in the harness's own companion and printed on every run. Read against both, the measured **19.42 m sits past the one and inside the other**, so the item says what the number **is** rather than what it produces: the policy stands as the working rule for a single drawn line, the analogue is the real head-room of a comparison, and the honest pair is **2 aim(s) past the policy against 0 past the analogue** — so this reading does not settle the cache and the call between accepting the divergence (re-scoping row b's guard from *the same line* to *never dearer*) and building the **slice** stays §19.5's closing note. `points`, `legTimesSec` with its own gap, `distanceM`, `durationSec`, the six metrics, the **metres of line standing past the bound** and the reuse flag print per aim beside it, so the divergence is judged on the lines rather than on the summary.
- [ ] 7 · **Every pair re-read** after each landed item — the acceptance pair, the two inshore refusals, and the harness under `-Dmaro.prebake=true`
- [ ] 8 · **One drag re-read on the device** — taken twice, its degenerate previews set aside as answers rather than timings, against a twice-read baseline
- [x] 9 · **The user's levers priced last — both levers that project more than three seconds on the phone are refused, on readings** (priced 2026-09-22 in one `#implement` Code hop; the instrument is deleted with the pass and no switch is left in the tree). **Row d, the narrower corridor.** The guard the item names was vacuous, so one was **hunted**: coastal water points either side of a headland, chords over land, ranked deepest-first, each run at the shipped margin and at every narrower one — and **3 of 3 candidates answered at 1.0 NM with 2 LOST by a narrowing** (e.g. `[43.4291, 6.8676] → [43.5129, 6.9389]`, an 11.0 km chord wholly over land, answering at 1.0/0.5/0.25 NM and refused grown at 0.0), so the containment guarantee is **real and spent, not vacuous**. The acceptance pair re-read, warm, as a pair of runs: margin 1.0 → 0.5 → 0.25 → 0.0 NM moves vertices **431 → 401 → 393 → 380** and pairs **92 665 → 80 200 → 77 028 → 72 010** while the drawn line stays **the recorded one to the digit at every margin**; a halved *rough* margin buys **13.5 % of the pairs**, and a halved **effective** corridor (1.0 → 0.0 — the reach is the sizing clamp and the margin lever never touches it) buys **22 % of the pairs and 12 % of the vertices**, which is **≈ 0.7 s projected** on the phone's own ratios (§19.6 — harvest ~10×, graph ~35×): an order of magnitude short of the **−3 to −5 s** it was taken on. **Refused.** **Row f, the invented-vertex prune.** Read as a per-piece support vertex toward the two ends and priced through a temporary harvest argument (`null` again in the shipped tree), it takes vertices **431 → 169**, pairs **92 665 → 14 196** (−84.7 %), edges **3 645 → 697** and the graph **56·58 → 13·11 ms** — but the **line moves** (54 · 307° · worst 30° · 5.44 km · **27.40 min** → 49 · 193° · worst 18° · **6.81 km** · 20.41 min, crossings 30 → 32, in-margin 0.17 → **3.42 km**, berth 135.90 → **724.45 s**), so its own acceptance — **the same six metrics** — **fails**, and the search's fall is the *line* moving rather than a pair-count lever cheapening a per-corner search. Its own price, the pair loop and the memory, is **≈ 46 ms of graph, ≈ 1.7 s projected on the phone — under the order's three**. **Refused on both counts.** Rows **e** and **g** were not priced: the harness's own split refuses **e** without a run (**1.6 fits and 1.6 candidates a station** over 3 871 — the ladder is already lean), and **g** is the trade of optimality for speed §19.1 names as **the user's to make and not this pass's to assume**. Guards re-read on the reverted tree: acceptance pair **54 · 307° · worst 30° · 5.44 km · 27.40 min**, map **431 · 3 645 · 92 665**, crossings 30 · in-margin 0.17 km · berth 135.90 s; both inshore pairs refused **by name**; the grown-corridor probe **only a priced crossing** (30 legs, 429 vertices); the reuse guard **never dearer true** (0.000 s of 1 644.23 s against the 0.010 s band); **8 suites · 66 tests · 0 failures** and the harness green under `-Dmaro.prebake=true`; `apk-build.bat` green with no new warnings. **Carried, not fixed — one reading this hop could not attribute:** the same guard candidate read **1023 vertices in one run and 971 in another** on the same coordinates, while three harvests of one box inside a run agreed to the digit (370 corners twice, 401 v twice, 971 v three times) and the acceptance pair read 431 · 3 645 · 92 665 in every run. The cause is named — [`DepthIsobaths`](app/src/main/java/ykws/android/maro/data/depth/DepthIsobaths.kt:127) breaks a tie in a contour line's dominant source by `HashMap<DepthSource, Int>` iteration order, identity-hashed over an **enum** key and therefore different **between JVM runs**, which moves the 2 m contour's wall set and so the route's own walls — a **wall set that is not reproducible across launches**, contradicting this plan's own claims (§16 step 9, §17 item 5, §19.5's "the graph is the cold graph field for field"). It is a fix in the **depth feature's own file** and is the user's to order
- [x] 10 · **The Ask hop's ten should-fixes closed in one pass, worst first** — the zone-carrying reused graph's assertion (the licence's zone half pinned nowhere, the harness's own reuse reading only **printing** its pair count and its same-line flag); then the documentation that overstates what the code now does (`TautGraph.build` read as the production path while only tests call it, `allowed`'s two readers down to one, `pairKey`'s single spelling duplicated in `edgeBetween`, the two haversine spellings, `over`'s O(N) claim leaving `finish()`'s O(V+E) copy unsaid); the two `EdgeSink` returns nobody reads; `cornerCount` counted before the end-dedupe; `graphReused` reaching only the log line rather than the dossier; and `over`'s `cancelCheck` KDoc promising a check per row it does not make — **resolved 2026-09-21, all ten, the hole first.** **The zone half is read on a world that carries one:** `TautTerrainTest` builds a world carrying a 5 kn harbour the aim stands inside and walks the kept graph against a cold build over the **same terrain**, pair by pair — every shared edge must carry the same zone, berth fraction, price and length — reading **9 vertex(es) · 25 edge(s) · 13 entering the zone · 16 earning a berth fraction**, so a zone-carrying terrain reaches both entrances alike; the harness's reuse line prints the keep as a flag now (`graph kept true`, `859 pair(s)`) rather than leaving it to be inferred. **`over`'s `cancelCheck`** is asked **per row** — the start's, the aim's and the kept edges' copy — as its KDoc promised, with a control that throws on its third ask. **The KDoc drifts are gone:** `allowed` names its **one** reader ([`EdgeSink`](app/src/main/java/ykws/android/maro/spatial/taut/TautGraphBase.kt:339)); `pairKey` is one home both `finish()` and [`edgeBetween`](app/src/main/java/ykws/android/maro/spatial/taut/TautGraph.kt:513) call; `TautGraph`'s private `metres` is **deleted** and `nearestTraversable` reads `metresBetween`; [`TautGraph.build`](app/src/main/java/ykws/android/maro/spatial/taut/TautGraph.kt:763) reads as the **readings' cold entrance** rather than the production path, which builds `of` once and answers through `over`; and the O(N) claim now states the assembly's **O(V + E)** rebuild and copy. **Then the three behaviour fixes:** [`EdgeSink.offer`](app/src/main/java/ykws/android/maro/spatial/taut/TautGraphBase.kt:353) and `append` return `Unit`; `cornerCount` counts the corners **standing as vertices**, the end-dedupe taken off, pinned at `vertexCount − 2` (the off-water aim reads **4 vertex(es) · 3 edge(s) · 5 pair(s)** warm against cold **4 · 3 · 8**); and `graphReused` rides the **dossier** ( [`TautRouteDetails.graphReused`](app/src/main/java/ykws/android/maro/spatial/taut/TautRouteDetails.kt:163) ) with each existing reuse test asserting it. Guards unmoved: the acceptance pair is the recorded line to the digit — **54 vertices · 307° · worst 30° · 5.44 km · 27.40 min**, map 431 vertex(es) · 3 645 edge(s) · 92 665 pair(s), crossings 30 · in-margin 0.17 km · berth 135.90 s; the two inshore pairs still refused **by name** (`START_OFF_WATER`, 79 ms and 212 ms); the grown-corridor probe still *only a priced crossing* (30 legs, 429 vertices); the licence's drag rate unmoved at **5/6 inward, 0/6 outward**. **7 suites · 63 tests · 0 failures** under `--tests ykws.android.maro.spatial.taut.*`, the harness green under `-Dmaro.prebake=true`, `apk-build.bat` green with no new warnings. **Revised 2026-09-21, the Ask pass having returned revise on four findings of this item's own text:** `pairKey` orders the pair itself now — `edgeBetween` orders nothing, so the order-independence is the function's rather than its callers'; the cost paragraph says what each half does rather than one sentence covering both — `of` the kept O(N²) scan, `over` the ends' rows *plus* `finish()`'s copy, pair index and adjacency, an O(V + E) rebuild **every** graph pays on its way out; and the haversine one home became literal rather than named, `metresBetween` the package's only spelling with `TautEasing`'s private `metres` **deleted** and the search's two inline wrappings — the heuristic's and the distance reading's — folded into it, the two imports they left unused going with them. The two tests' revert claims were then **measured rather than asserted**: `the ends' rows are cancellable between them` is **red** with the three checks moved back out of the loops (the build asking once) and green with them in place, and `an aim resolved onto a kept corner is not a vertex of its own` is **red** on the raw-count revert while the end-dedupe's own removal leaves it green, `over` being the one path both entrances walk — the same reason the zone-carrying reading stood **green** on a kept copy carrying `-1` and `0.0`, its KDoc now saying what it reads instead of naming a revert no run can see. Not done: none of the ten

- [x] 11 · **The divergence accepted, and the reuse guard re-scoped from *the same line* to *never dearer*** — **resolved 2026-09-21, at the user's word, on the numbers §19.5 C3 named.** The guard is a **price** one wherever it asserted identity: a reused answer **may differ** from a cold build's for the same aim and must not be **dearer**, the slack being the reading's own band — **0.01 s absolute, ~6e-6 relative** — held in **one home** (`TautReuseGuard` in [`TautRouteHarness`](app/src/test/java/ykws/android/maro/spatial/taut/TautRouteHarness.kt)) and read by the harness's moved-aim verdict and by `TautTerrainTest`'s guard alike, because the quantity compared is a clock whose own seconds move by more than a hair between runs on identical code (§19.3's pair of runs). The KDocs that promised identity are re-scoped in [`TautTerrain`](app/src/main/java/ykws/android/maro/spatial/taut/TautTerrain.kt), [`TautGraphBase`](app/src/main/java/ykws/android/maro/spatial/taut/TautGraphBase.kt) and [`TautRouteEngine`](app/src/main/java/ykws/android/maro/spatial/taut/TautRouteEngine.kt): the *graph* is still the cold graph field for field, the *answer* is judged by price. **Two of the five terrain readings changed shape and the rest kept their claim** — `a kept terrain is reused by the second search and answers no dearer` and `a kept graph serves the second search and answers no dearer` assert the price guard where they asserted the point list's equality (their gap on the island pair reads **0.000 s** against a 0.010 s band, the kept box there being the cold bound to the digit), while the ends' rows test (**warm 4 · 3 · 5 pair(s) against cold 4 · 3 · 8**), the zone-carrying test (**9 vertex(es) · 25 edge(s) · 13 entering the zone · 16 earning a berth fraction**), the pace rebuild, the moved-world rebuild and the licence-at-its-own-type reading are untouched. **The guard is pinned red-then-green:** `the guard refuses a reused answer dearer than a cold build's` forces a gap of the band + 0.25 s, reads the predicate false and the shared assertion thrown, and the real pair green. **And the drag's own reading, at three decimals:** the six aims carry **+0.008 · +0.006 · +0.015 · −0.962 · −0.094 s** against their cold builds (their own bands 0.0102 · 0.0106 · 0.0106 · 0.0105 · 0.0111 s) — so **two of the three +0.01 s aims sit inside the band and the third stands 0.004 s past it**, the harness measuring rather than gating and the band being the reading's own size rather than one chosen to pass. Guards unmoved: the acceptance pair the recorded line to the digit (**54 · 307° · worst 30° · 5.44 km · 27.40 min**, map 431 · 3 645 · 92 665, crossings 30 · in-margin 0.17 km · berth 135.90 s), both inshore pairs refused **by name** (`START_OFF_WATER`), the grown-corridor probe still *only a priced crossing*, and the reuse's own answer **never dearer (0.000 s of 1 644.23 s)**. `TautTerrainTest` **19 tests · 0 failures**, the taut suites green, the harness green under `-Dmaro.prebake=true`, `apk-build.bat` green with no new warnings. **No slice was built and no cache changed:** the licence stays the terrain's identity plus the pace, and the fallback stays today's rebuild
- [x] 12 · **The cheaper *point* question — the half that owns the 92 %** — opened 2026-09-21 by item 4's own reading, which found no exact variant inside the surface it priced: the point's answer is the coast index's own rule (a nearest-**mainland**-segment side test plus a real-island override) and the depth grid's bilinear sounding, so the lever is a cheaper **answer** to the same question rather than fewer questions. Its ceiling is the measured **444 · 460 ms** of the acceptance pair's point queries, and a build must beat it with the drawn line and the six metrics unchanged, its exactness proved by the same reading that prices it — a cheaper question that answers differently is a defect, not a lever. It changes a judge's cost model, so it waits for the user's word like every lever that can move the line — **resolved 2026-09-22**: the point's price is **97 % the coast index's walk** — isWater **441 · 297 ms**, the offset question **236 · 139 ms**, the grid **13 · 9 ms** — with 48 % of the questions walking the same grid a second time and 16 574 of 31 306 offset walks expanding rings to settle 50 m. Three candidates were priced twice each through one instrument: **A**, the offset asked at its own radius, **refused** (236 · 139 → 174 · 147 ms, and dearer where the mass sits); **B**, each grid cell visited once per query, **built** (isWater **327 · 280 → 101 · 90 ms**, the pair's wall 1 312 · 758 → 543 · 540, search 787 · 600 → 403 · 400), its answers compared with the shipped walk on **28 004 points** with **0 differences** and its exactness structural; **C**, B plus a bounding-box prefilter, **refused on both counts** — dearer than the walk it would save, and one answer of 28 004 moved. On the shipped tree the ceiling is beaten: the harness's own pair reads water **539 · 446 → 202 · 216 ms**, search **817 · 729 → 503 · 544**, wall 1 428 · 1 315 → **993 · 1 060 ms**, with the acceptance line the recorded one to the digit and every count unmoved (points 76 808 · chords 67 597 · fits 6 274 · map 431 · 3 645 · 92 665). `CoastlinePointWalkTest` pins the walk against a brute-force oracle (worst gap 0.0e+00 m · 0 water mismatches) and ran red first on the 13 m sliver the index drops by design; the edit is a **shared surface** — `CoastlineSpatialIndex` — and no setting, dependency or switch is left in the tree → design §19.8
- Note: the closed level above keeps two points, and both are carried here — item 8's metres reading as item 6, its terrain-cache guard as item 3 — while its corridor lever stays kept there and is priced here as item 9. Not a step, by the review's second blocker: the release-build measurement, the module shipping no release type and no signing config. And not a step either, named as a limitation: the phone has no automated gate, its readings being taken by hand.
- Note: items 2 and 3 reached their readings in the Architect session of 2026-09-21, and the rest wait on what that mode cannot do — items 4 to 7 need a harness run and item 6 a device drag, and items 5 and 9 need code (item 3's build landed 2026-09-21). Item 1 is the user's alone.

## Implemented

- **Route — the two levers priced last, and both refused (2026-09-22, `feature/route`)** — walk item 9 priced through a **test-only instrument deleted with the pass**, every millisecond read after a warm-up run thrown away. **The corridor (row d):** its guard was vacuous, so one was **hunted** — coastal water points either side of a headland, chords over land ranked deepest-first — and on this water **3 of 3 candidates answered at 1.0 NM with 2 LOST by a narrowing** (e.g. `[43.4291, 6.8676] → [43.5129, 6.9389]`, an 11.0 km chord wholly over land, answering at 1.0/0.5/0.25 NM and refused *grown* at 0.0), so the containment guarantee is **real and spent**. The acceptance pair re-read as a **pair of runs**: margin 1.0 → 0.5 → 0.25 → 0.0 NM moves vertices **431 → 401 → 393 → 380** and pairs **92 665 → 80 200 → 77 028 → 72 010** with the drawn line **the recorded one to the digit at every margin**; halving the *rough* margin buys **13.5 % of the pairs**, halving the **effective** corridor **22 % of the pairs and 12 % of the vertices** — the width being `margin + reach`, and the reach the **sizing clamp the margin lever never touches** — which projects **≲ 0.7 s**, **three to seven times short** of the **−3 to −5 s** it was taken on. **Refused.** **The vertex prune (row f):** vertices **431 → 169**, pairs **92 665 → 14 196** (−84.7 %), edges **3 645 → 697**, graph **56 · 58 → 13 · 11 ms** — but the **line moves** (5.44 → 6.81 km, 27.40 → 20.41 min, in-margin 0.17 → 3.42 km, berth 135.90 → 724.45 s), so its own acceptance — **the same six metrics** — **fails**, and the search's fall (296 · 315 → 90 · 79 ms) is the *line* moving: §19.3's row f claim that the search does not move holds **only while the line is held**. Its own price, the pair loop and the memory, is **≈ 45 ms, ≈ 1.6 s projected** — under the order's three. **Refused on both counts.** Rows **e** and **g** were not priced (e refused by the harness's own **1.6 fits and 1.6 candidates a station**, g named as §19.1's trade **for the user**). Guards re-read on the reverted tree: acceptance **54 · 307° · worst 30° · 5.44 km · 27.40 min**, map **431 · 3 645 · 92 665**, crossings 30 · in-margin 0.17 km · berth 135.90 s; both inshore pairs refused **by name**; the probe **a priced crossing** (30 legs, 429 vertices); the reuse guard **never dearer true**; **8 suites · 66 tests · 0 failures** and the harness under `-Dmaro.prebake=true`; `apk-build.bat` green with no new warnings. **Carried, not fixed:** [`DepthIsobaths`](app/src/main/java/ykws/android/maro/data/depth/DepthIsobaths.kt:127) breaks a contour line's dominant-source tie by `HashMap<DepthSource, Int>` iteration order — identity-hashed over an enum, so it differs **between JVM runs** — moving the 2 m contour's wall set and with it a route's own walls: a wall set **not reproducible across launches**. → `xTrack/Route/260920_FEAT_PLN_Route_taunt_algorithm-design.md` §19.9

- **Route — the point question made cheaper, and the candidate that moved an answer refused (2026-09-22, `feature/route`)** — walk item 12 built in one `#implement` Code hop, the pricing done on a **test-only instrument deleted with the pass** (a counting wrapper around the harness's own world: nothing under `main/` was touched to take the reading, and no switch is left in the tree). **The reading first:** the point's price is **97 % the coast index's walk** — `isWater` **441 · 297 ms**, the offset question **236 · 139 ms**, the depth grid **13 · 9 ms** — with the untrusted branch, **48 %** of the questions, walking the same grid a *second* time, and **16 574 of 31 306** offset walks expanding rings to settle a 50 m threshold. **Three candidates, each priced twice:** the **offset question asked at its own radius refused** (236 · 139 → 174 · 147 ms — inside the spread, and dearer where the mass sits at 13.4 µs against 3.9 in the 50–200 m band; its exactness was confirmed and its apparatus reverted whole), **each grid cell visited once per query built** (`isWater` **327 · 280 → 101 · 90 ms**; the pair's wall **1 312 · 758 → 543 · 540**, search **787 · 600 → 403 · 400**), and the **bounding-box prefilter refused on both counts** (dearer than the walk it would save, and one of 28 004 answers moved). The winner's exactness is **structural** — the cells a ring adds are exactly the clipped square's perimeter, and the old dedupe's row-major order over it is the order the new loop keeps, the stop conditions and the clip unchanged — and **measured**: 28 004 points (corridor spread, ±30 m coastline offsets, segment vertices, the three pairs' ends) with **0 distance and 0 water differences**. **On the shipped tree the ceiling is beaten:** the harness's own pair reads water **539 · 446 → 202 · 216 ms**, search **817 · 729 → 503 · 544 ms**, wall 1 428 · 1 315 → **993 · 1 060 ms** against the item's **444 · 460 ms**, with the acceptance line the recorded one to the digit (54 · 307° · worst 30° · 5.44 km · 27.40 min), the map (431 · 3 645 · 92 665), the fits (6 274), the stations (3 871) and the question counts (points 76 808 · chords 67 597 · 11 416 memo) all unmoved — the sampling never moved, which is the shape a cheaper *answer* has. Guards re-read: both inshore pairs refused **by name** (`START_OFF_WATER`), the grown-corridor probe *only a priced crossing*, the reuse guard's price verdict unchanged (never dearer, gap 0.000 s against the 0.010 s band). `CoastlinePointWalkTest` is the permanent guard — the walk's distance against a **brute-force oracle over the index's own cleaned segments** (worst gap 0.0e+00 m, 0 water mismatches, each branch counted) — and it ran **red first**, its oracle demanding a distance to the discarded 13 m sliver. **The edit is a shared surface:** `CoastlineSpatialIndex` belongs to the coastline feature and every reader of the index pays that walk, which is why the guard is drawn over the region's coastline rather than the corridor alone. **9 suites · 67 tests · 0 failures** under `-Dmaro.prebake=true`, `apk-build.bat` green with no new warnings; no setting added, no dependency, no device. → `xTrack/Route/260920_FEAT_PLN_Route_taunt_algorithm-design.md` §19.8

- **Route — the reuse guard became a price (2026-09-21, `feature/route`)** — walk item 11, the user's decision of 2026-09-21 built: the cache **may** keep a warm answer that differs from a cold one, provided it is **never dearer** for the same aim, and the **slice is not built**. The guard moved from identity to price wherever it asserted identity — `TautTerrain`'s, `TautGraphBase`'s and the engine's KDocs, and the harness's reuse line, which prints the price verdict rather than "the same line" — with the slack held in one home (`TautReuseGuard`, **0.01 s / ~6e-6**) and its reason stated: the quantity compared is a **clock**, whose seconds move by more than the band between runs on identical code. Two of `TautTerrainTest`'s five readings changed shape to carry it (`… answers no dearer`, a gap of **0.000 s** of 1 644.23 s against the 0.010 s band); the ends' rows, the zone-carrying reading, the pace and moved-world rebuilds and the licence-at-its-own-type reading kept their claims; and the guard is pinned **red** on a forced gap of the band + 0.25 s and green on the real path. **The pass also found a contradiction in its own record:** at three decimals the drag's aims carry **+0.008 · +0.006 · +0.015 · −0.962 · −0.094 s** against bands of 0.0102–0.0111 s, so the third stands **0.004 s past** the band and the earlier "+0.01 s on three aims" was two-decimal rounding — recorded rather than smoothed, the harness measuring and not gating. Guards unmoved: the acceptance pair the recorded line to the digit, both inshore pairs refused **by name**, the grown-corridor probe *only a priced crossing*; **8 taut suites · 66 tests · 0 failures** (TautTerrainTest 19), the harness green under `-Dmaro.prebake=true`, `apk-build.bat` green with no new warnings; no setting added, no dependency, no device. → `xTrack/Route/260920_FEAT_PLN_Route_taunt_algorithm-design.md` §19.7

- **Route — the judge stops at the first blocked chord (2026-09-21, `feature/route`)** — walk item 5, the build of the candidate the pricing pass named. [`curveOnWater`](app/src/main/java/ykws/android/maro/spatial/taut/TautObstacles.kt:252) now asks each point **with the chord it leaves**, so a refused curve ends its walk at the first blocked chord instead of finishing every question — the fit predicate being an AND, the answer cannot change and the saving is exactly the questions a refusal never needed. The judge's two kinds are exposed apart ([`pointsAsked`](app/src/main/java/ykws/android/maro/spatial/taut/TautObstacles.kt:163) · [`chordsAsked`](app/src/main/java/ykws/android/maro/spatial/taut/TautObstacles.kt:164)), carried onto `TautRouteDetails` and printed by the engine and the harness, so the split is a first-class reading rather than a console dump. **Pinned by a control run rather than asserted:** [`TautObstaclesTest`](app/src/test/java/ykws/android/maro/spatial/taut/TautObstaclesTest.kt:1) reads a refractured curve at **refused · 1 chord · 1 point** where the reverted two-loop shape asks three points, and on the acceptance pair the same pair of runs reads **−18 819 point questions** — the priced figure to the digit — against **+2 173 chords**, the half the price never printed, a chord being asked before the point that would have ended the walk at a seventh of its cost. **No wall-clock gain is claimed:** the search read 813 ms reverted against 817 · 729 ms shipped, inside the spread §19.3 warns about, so the counters are the only deterministic witness. The six metrics, the drawn line and the map are identical to the digit in all three runs — 54 · 307° · worst 30° · 5.44 km · 27.40 min, crossings 30 · in-margin 0.17 km · berth 135.90 s, map 431 · 3 645 · 92 665 — both inshore pairs refused **by name** and the probe still *only a priced crossing*. **20 suites · 154 tests green**, the harness green under `-Dmaro.prebake=true`, `apk-build.bat` green with no new warnings; no setting added, no dependency, no device. → `xTrack/Route/260920_FEAT_PLN_Route_taunt_algorithm-design.md` §19.6

- **Route — the water question priced, and the cache's own divergence measured (2026-09-21, `feature/route`)** — walk items 4 and 6 of the fixed plan, a measurement pass with **no production change**. The water's milliseconds were split by question kind at last: the **point** queries are **92 %** of them (444 · 460 of 480 · 494 ms on the acceptance pair), so the lever is the question rather than the ladder. Three candidates were priced twice through one instrument — **A**, the ladder's emission 10 m → 30 m (water halved, search −42 %), **refused because it moves the line** (54 → 44 vertices, chord deviation 0.06 → 0.31 m, worst turn 35° → 30° on the moved aims); **B**, an early exit on the first blocked chord (water −22 % · −15 %, search −19 % · −11 %, the line identical to the digit and structurally unable to move it, the fit predicate being an AND, save the counters the memo rides on), **named as the candidate to build**; **C**, a finer bucket grid 100 m → 20 m, **refused as dearer on the walk it targeted** (24 · 26 → 47 · 44 ms, a finer grid putting more cells beneath the same chord). The cache's divergence is a measured length now: the warm line stands **0.00 m** from the cold one on four of six aims and **19.42 m** on two — past the **12.5 m** policy bound and inside the honest **25 m** analogue — with **340 m** of line standing past the bound, the quantity a comparison of two drawn lines actually shows. The Ask hop sent the pass back once, and the revision corrected the record rather than the code: the bound is stated as a policy with its twice named beside it (the old rationale read one answer's tolerance as bounding the difference between two answers), item 4's evidence is named as the gitignored `tmp-hop-*.log` with its figures carried into the item, the questions the instrument excludes are said (the build's own probes, a constant **4 743**) so its counts reconcile with `judgeAsked`, C's mechanism note is corrected, and walk item 5's guard reads the **six metrics and the drawn line**, never the counters. Guards unmoved: the acceptance line to the digit, both inshore pairs refused **by name**, the grown-corridor probe *only a priced crossing*; **70 focused tests · 0 failures**, the harness green under `-Dmaro.prebake=true`, `apk-build.bat` green; the only source edits are the test-only harness's printing and two helpers. → `xTrack/Route/260920_FEAT_PLN_Route_taunt_algorithm-design.md` §19.6

- **Route — the measurement pass's record corrected: the bound stated as what it is, and the divergence's metres read beside it (2026-09-21, `feature/route`)** — the review of `## Walk` items 4 and 6 returned **revise**, both blockers about what the record said rather than about what runs, and this pass closed them and the seven should-fixes. **The bound is no longer argued backwards:** `DIVERGENCE_THRESHOLD_M` is gone, the tolerance product has **one home** ([`ABSTRACTION_TOLERANCE_M`](app/src/test/java/ykws/android/maro/spatial/taut/TautRouteHarness.kt:770)) and the harness reads two bounds off it — [`DIVERGENCE_POLICY_M`](app/src/test/java/ykws/android/maro/spatial/taut/TautRouteHarness.kt:787), a **policy chosen at one tolerance** and no longer sold as the error every corner already spends, and [`DIVERGENCE_ANALOGUE_M`](app/src/test/java/ykws/android/maro/spatial/taut/TautRouteHarness.kt:793), the honest twice — so item 6 now says the measured **19.42 m sits past the 12.5 m policy and inside the 25 m analogue** (2 aims past the one, 0 past the other) instead of reading a single number as a verdict. **And the divergence gained the quantity a user actually sees:** [`lineBeyondM`](app/src/test/java/ykws/android/maro/spatial/taut/TautRouteHarness.kt:646) prints the **metres of line standing past the bound** — **340 m** on the two diverging aims — beside [`lineApartM`](app/src/test/java/ykws/android/maro/spatial/taut/TautRouteHarness.kt:595), whose KDoc now states what it is, a **vertex-sampled two-sided maximum**, and its limit: the aims that read **0.00 m** apart while carrying **4.4, 4.9 and 14.1 ms** of per-leg movement, `offsetToM` collapsing that question to one home. **Item 4's evidence is named and its counts reconciled:** the candidate pricing's figures come from the hop's own console dump, `tmp-hop-*.log` in the root, **gitignored and therefore outside the record**, with the figures carried in the item so they survive the logs; `judgeAsked` is the **obstacles instance's** total, so it carries the build's own corner, midpoint and end questions — counted and never timed — which is why points + chords falls a constant **4 743** short in every configuration; and **C's refusal is put back on the walk it targeted** — a finer grid puts *more* cells beneath the same chord, so the walk is dearer by construction (24 · 26 → 47 · 44 ms, dearer on both runs) — with the run's discordant total water named as aggregate rather than evidence. **The guard moved off the counters:** walk item 5 and §19.3's protocol both now read **the six metrics and the drawn line**, the counters being a pre-screen only, since fewer point questions cut the memo entries with them. Guards unmoved and re-read on the run — the acceptance pair the recorded line to the digit (**54 · 307° · worst 30° · 5.44 km · 27.40 min**, 431 vertices · 3 645 edges · 92 665 pairs, crossings 30 · in-margin 0.17 km · berth 135.90 s), both inshore pairs refused **by name** with `START_OFF_WATER` on the warn line, the grown-corridor probe still *only a priced crossing* (30 legs, 429 vertices); **70 focused tests · 0 failures** (7 suites under `spatial.taut` plus `RouteEngineSeamTest`), the harness green under `-Dmaro.prebake=true`, `apk-build.bat` green. No production file is touched: the only source edit is the test-only harness → `xTrack/Route/260920_FEAT_PLN_Route_taunt_algorithm-design.md` §19.3

- **Route — the Ask hop's ten should-fixes closed, and the four revisions after them (2026-09-21, `feature/route`)** — walk item 10, built in two Code hops. The real hole first: a **zone-carrying** world whose aim stands inside a 5 kn harbour, the kept graph walked against a cold build over the same terrain pair by pair, every shared edge asserted to carry the same zone, berth fraction, price and length — **9 vertices · 25 edges · 13 entering the zone · 16 earning a berth fraction** — where before no focused test carried a zone at all and the harness's own reuse reading only printed its figures. Then, in order: `over` asks its `cancelCheck` **per row** as its KDoc promised, a control throwing on the third ask; `allowed` names its one reader; `pairKey` orders the pair itself so its KDoc's order-independence is the function's and `edgeBetween` orders nothing; `TautGraph`'s duplicate `metres` deleted with `metresBetween` left the package's only spelling, `TautEasing`'s private twin and the search's two inline wrappings folded in; `TautGraph.build` reads as the **readings' cold entrance** rather than the production path; the O(N) claim carries `finish()`'s O(V+E) with each half's work stated; `cornerCount` counts the corners that stand as vertices; `graphReused` rides the dossier; and `EdgeSink`'s two unread returns are gone. **The review returned revise on four, all in this pass's own text, and the reverts were run rather than asserted:** moving `over`'s three checks out of the loops turns *the ends' rows are cancellable between them* red, and the raw-`cornerCount` revert turns *an aim resolved onto a kept corner is not a vertex of its own* red. Guards unmoved throughout — the acceptance pair the recorded line to the digit (54 · 307° · worst 30° · 5.44 km · 27.40 min, 431 vertices · **3 645** edges · 92 665 pairs, crossings 30 · in-margin 0.17 km · berth 135.90 s), the two inshore pairs refused **by name** with `START_OFF_WATER` on the warn line, the grown-corridor probe still *only a priced crossing* (30 legs, 429 vertices); **7 suites · 63 tests green**, the harness green under `-Dmaro.prebake=true`, `apk-build.bat` green with no new warnings. **Left open with its reason:** the zone-carrying fields are **read** on a zone-carrying terrain rather than revert-pinned, because one code path serves both entrances — `TautGraph.build` being `of` plus `over` — so no revert inside the kept-edge copy can move a comparison between the two. → `xTrack/Route/260920_FEAT_PLN_Route_taunt_algorithm-design.md` §19.6

- **Route — the graph kept beside the terrain (2026-09-21, `feature/route`)** — walk item 3 of the fixed plan, built in one `#implement` Code hop. The graph's **corner-to-corner edge set** is a kept base now ([`TautGraphBase`](app/src/main/java/ykws/android/maro/spatial/taut/TautGraphBase.kt:1)): `of` is the corner harvest and its quadratic scan, `over` adds the two ends and scans **their rows alone** beside the kept edges, and `TautGraph.build` is those two halves — so a cold build and a reused one are one code path differing only in `examined`, which is why they cannot diverge by construction. The **licence** is the terrain's identity plus the pace, held as one `@Volatile` entry on [`TautRouteEngine`](app/src/main/java/ykws/android/maro/spatial/taut/TautRouteEngine.kt:174) beside `keptTerrain` — the box, the zones and the world's generation riding inside the terrain's identity — with a rebuild exactly as today wherever it fails, and the log line now naming `terrain reused, graph kept`. Readings on the shipped world, each a **pair of runs**: the acceptance pair is the recorded line to the digit (54 · 307° · worst 30° · 5.44 km · 27.40 min, 431 vertices · 3 645 edges · 92 665 pairs, crossings 30 · in-margin 0.17 km · berth 135.90 s) with the split 218 · 224 · 886 ms and 301 · 171 · 701 ms; the keeping's own pair reads graph **85 → 1 ms** over 92 665 → 859 pairs, harvest 58 → 0 ms and wall 753 → 160 ms, drawing the same 54-point line; the two inshore pairs are still refused **by name** and the grown-corridor probe still *only a priced crossing*. Five tests in [`TautTerrainTest`](app/src/test/java/ykws/android/maro/spatial/taut/TautTerrainTest.kt:107) pin the licence, the ends' rows (7 pairs against 10 over the same 5 edges) and the same line off the water. `apk-build.bat` green with no new warnings, 68 focused tests green, the harness green under `-Dmaro.prebake=true`. **The Ask hop returned no blocker and ten should-fixes**, the first real: no focused test builds a **zone-carrying** reused graph, the harness's reuse reading only printing its figures, so the licence's zone half is asserted nowhere — a small test, the fixture already taking zones. The rest are documentation that overstates the code (the build function read as the production path while only tests call it, `allowed`'s two readers down to one, `pairKey`'s one spelling duplicated in `edgeBetween`, the two haversine spellings, the O(N) claim leaving `finish()`'s O(V+E) copy unsaid), two `EdgeSink` returns nobody reads, `cornerCount` counted before the end-dedupe, `graphReused` reaching only the log line rather than the dossier, and a `cancelCheck` KDoc promising a per-row check it does not make; the reviewer could not check any build or test claim, and the equality against the **pre-change** order rests on the recorded line alone, no git history being available to that pass. Planned as item 10 → `xTrack/Route/260920_FEAT_PLN_Route_taunt_algorithm-design.md` §19.6

- **Route — the corridor's terrain kept for a box, and the review that found a land-crossing race in it (2026-09-21, `feature/route`)** — walk item 8's first cut: a search's **terrain** — the obstacles, the zone set and the band, the band built off the obstacles' own frame — is a function of the **box**, so the engine keeps **one** entry and reuses it while the next corridor lies inside it, and the harvest's 292 ms and the band's own coastline read leave every repeat search (a second search on one engine: wall clock **739 → 204 ms**, the harvest **55 → 0 ms**). Two rules came with it: invalidation by a **generation** that `TautWorld` now carries, counted per layer so a depth refresh invalidates like a first load, and read-only-to-whoever-is-running, which the judge's memos needed before they could be shared at all. **The review returned revise, and both blockers were real.** The **scratch**: `WallIndex.crossedBy` and `TautBand.spansOf` marked their walk with a plain `stamp++` over a shared array, and b1 is what made those instances shared with a **cancelled, unjoined predecessor** — a two-thread test read **13 960 of 100 000 answers wrong** before the fix and **0** after, so a leg crossing land was *reachable*, not theoretical. The **licence**: it tested the rough corridor while a cold search builds from a box its own clamp widens, so a reuse could refuse, or answer over a narrower corridor, where cold finds a way; it now holds the **largest box cold could build** — reverting it turns the drag reading red (outward reuse 5/6 where cold builds wider; on the fix, **5/6 inward and 0/6 outward**). **And the reading that decides the rest:** over 6 aims of one drag the warm line **differed from the cold one on 5**, never dearer (0.01 s on three aims, cheaper on two, ~6e-6 relative) — so with the cache the route is no longer a function of its start and aim alone but of the drag's history. The number that choice rests on — **how far apart the two lines stand in metres** — is named as missing rather than assumed: the **slice** is not built, and accepting the divergence would re-scope the guard from *the same line* to *never dearer*. Acceptance pair unchanged throughout (54 vertices · 307° · worst 30° · 5.44 km · 27.40 min, crossings 30 · in-margin 0.17 km · berth 135.90 s; both inshore pairs refused by name; the grown-corridor probe still *only a priced crossing*); **145 tests green**, the harness green under `-Dmaro.prebake=true`, `apk-build.bat` green → `xTrack/Route/260920_FEAT_PLN_Route_taunt_algorithm-design.md` §19.4 and §19.5

- **Route — the drag coalesced, the loops cancellable, and the judge memoised and refused (2026-09-21, `feature/route`)** — the walk's items 2–5 were built in one pass, and the measurement decided the one whose shape the plan had guessed wrong. **The drag is coalesced:** `preview` writes its aim into one slot the newest aim replaces, and a single drain loop serves it, so a gesture starts **one search per completed search** rather than one per frame and no search is killed to start another — the loop ends when the slot is empty (*that* is where `searching` goes false, so a refusal is never shown on a frame with work still behind it) or the moment the mode leaves the draft, and a confirmation or an exit clears the slot rather than serving it. **Cancellation reaches inside the loops** — the harvest's per-shape loop, the graph's per-outer-vertex pair scan and the search's per-corner door each ask the coroutine's own `ensureActive()`, handed down from the engine's four call sites — so an abandoned gesture stops *inside* a phase rather than at its end, moving no counter and no geometry. **The judge's memo** (`traversable` · `curveBlocked`, memoised per harvest and exact-keyed, because a rounded key would answer about a *neighbouring* place and the water's answer is constant over no cell) reproduced the drawn line **to the digit** — 54 vertices · 307° · worst 30° · 5.44 km · 27.40 min, crossings 30 · in-margin 0.17 km · berth 135.90 s — and moved the water figure 652 → **601 ms**; but **161 051 questions answered 14 418 times from the memo is a 9 % hit rate**, so the water's milliseconds are *new* work per candidate rather than repeated work, and §19.2's target is **re-stated rather than met**: 601 ms is the floor a cheaper question has to beat, and the cache gain is withdrawn. **And the device half:** one log line per answered route on the channel the refusals already use (tag `MaroTautRoute`), where a success used to say nothing at all and only a refusal spoke, plus `searching` as the panel's third state with its string in both locales. `apk-build.bat` green, the Taut suite green, the harness green under `-Dmaro.prebake=true`; the acceptance pair's split reads harvest 292 · graph 220 · **search 864 ms** over 431 vertices and 92 665 pairs, inside the spread §19.2 records → `xTrack/Route/260920_FEAT_PLN_Route_taunt_algorithm-design.md` §19.2

- **Route — the tolerance tried for the drag target, and the measurement that refused it (2026-09-21, `feature/route`)** — the user asked for a value that would bring a search to half a second, so the abstraction tolerance was doubled twice and measured on the acceptance pair: **12.5 m → 25 m → 50 m** took the vertices 431 → 391 → 366, the pairs 92 665 → 76 245 → 66 795 and the search 1 181 → 967 → 814 ms, while the water the abstraction deletes rose 1.47 → 1.52 → 1.71 km² and the drawn line gained a sharp corner and twenty degrees of turning. **No value reaches it**, and the reason is not only the flattening curve: the tolerance turned out to **spend the berth's own guarantee** — a harvested corner stands `berth − tolerance` off the undilated obstacle it came from, which `TautAssertionsTest` asserts, so a tolerance of one berth makes that assertion vacuous and the suite went red when it was tried. `TOLERANCE_FRACTION` is back at **0.5** with that ceiling written into its KDoc. What the same run established is where the search's time is: vertices fell 15 % while the search fell 31 %, and 2 966 stations in 814 ms is **~275 µs a station** — the per-corner easing fit and its drawn-clock price, not the vertex count. §19.1 is corrected accordingly: its "limit-aware heuristic" is **withdrawn as inadmissible** — a path may leave the band and come back, so the straight line's own limit does not bound the remaining time, and §7a protects the admissible bound explicitly — and the item that can reach a fast search is now the **per-corner work**. 133 tests green, `apk-build.bat` green → `xTrack/Route/260920_FEAT_PLN_Route_taunt_algorithm-design.md` §19.1

- **Route — the band's spans made exact, a hung build found, and the Gradle settings that hid it (2026-09-21, `feature/route`)** — the walk's five-item level was closed in one pass, and two of its findings came from **running** rather than reading. The band's geometry is the interval now and not a superset of it: `spanWithin` intersects the slab with the projection's own range before absorbing it and adds the caps after, so "within the width of the line" is read only where the nearest point of the line is a point of the segment. The four unnamed cases exist as `TautBandTest` — parallel-beyond-end, the coincident tangent roots, a cap-entry with a slab-exit, two disjoint capsules — and running them exposed **a tangent's float-epsilon sliver**, now refused by a stated millimetre floor because a touch is not an interval, and **a locus check that read the leg's own ends as crossings**, which corrected took the harness's worst case from **274.11 m to 0.02 m**. **And the reason no suite had completed in hours:** the shared grid walk's unbounded shape could circle one cell forever — one step at zero while the other index never reached its last cell — so the graph build hung and every wall the walk had still to see went unseen; it is now bounded by `|Δcol| + |Δrow|` cells, `floor`-indexed, keeping a supercover's coverage. The margin's exclusion went **per piece** and the pair's figures came back to life: **in-margin 0.17 km and berth 135.90 s** where both read 0.00. A rule refusal is now **produced** where it is read, in two tests, instead of pinned by a lambda and a duplicated predicate. The band's own share is a **measured 0.00 s** at fixed geometry — the same drawn chain priced under both rules, 1 644.19 s against 1 644.19 s, 53 drawn legs and 45 band spans — so the pass's earlier "+8.4 minutes is the band's charge" is **withdrawn**: the clock moved because the search chose a different line, not because the band re-priced one. Acceptance pair: **54 vertices · 307° · worst 30° · 5.44 km · 27.40 min**, split **350 · 258 · 1 181 ms** over 431 vertices and 92 665 pairs, crossings 30 · in-margin 0.17 km · berth 135.90 s · priced-against-drawn gap 0.18 s; the two inshore pairs are still refused by name. **18 suites green, 0 failures**, the harness green under `-Dmaro.prebake=true`, `apk-build.bat` green. The build itself was repaired in the same pass: [`gradle.properties`](gradle.properties:1) gained the build cache, the configuration cache, a sane daemon (`-Xmx3g -Xss2m`, `-XX:-UseCompressedOops` dropped) and a Kotlin-daemon heap, and [`app/build.gradle.kts`](app/build.gradle.kts:34) reads `maro.properties` through a **provider** rather than an eager `readLines()` — a repeat build now reuses the configuration cache in **1 s** → `xTrack/Route/260920_FEAT_PLN_Route_taunt_algorithm-design.md` §18

- **Route — the band's limit became a span source, and the review's six items closed (2026-09-21, `feature/route`)** — the walk's six-item level was built in one `#implement` run. **The band is a span source now, not a midpoint boolean:** `distanceToCoastM` was read first and it is the distance to the nearest coastline **segment**, so the crossing is closed-form and the plan's bisection was **not** taken — [`TautBand`](app/src/main/java/ykws/android/maro/spatial/taut/TautBand.kt) gathers the corridor's coastline segments into the corridor's own uniform grid and returns, per segment, the one interval inside the width (an affine **slab** plus the two **endpoint circles**, both convex in the leg's parameter), and `LegLimits.regionsOf` merges them with `TautZoneSet.limitSpans` into one breakpoint list where each piece's limit is read once — so the per-leg boolean is gone, the signature and the one-home role are unchanged, and **no edit was needed in the search or the assembly**, which is the plan's own test that the change sits in the right place. A cut point is collinear and free, pinned by `turnPriceSec(…, 0)` and by the straddling test (`TautAssertionsTest` 16 — the boundary mid-leg with the leg's own middle outside, two pieces, the limit over the 200 inside metres alone, the price equal to the drawn clock within the slack — whose control is the midpoint rule's own answer). A rule refusal is **counted and charged apart**: `TautSearch.fits` answers with the cause, `SharpCause.RULE` gets its own count printed by the harness, and a rule-refused corner is driven at the water's own reference speed so §12.3's pair costs it nothing. The margin excludes a zone by the **interval the leg shares** (`TautZone.entered`) rather than by `covers(midpoint)`, so a corner clip no longer pays the limit and the berth's ceiling on the same metres; the corner's reference limit gained the assertion it lacked, with the value a per-leg maximum would return (5.0) asserted beside it; the harness prints **priced · berth · drawn · gap** per pair and the aim's own end is named (`TautRefusal.AIM_OFF_WATER`, raised before the graph is built, where the fall-back to the raw aim had reported a corridor failure); and the two stale seam KDocs are corrected, the adapter's "nothing here fetches" replaced by what `loadCoastline()` really does on a cache-and-bundled miss. The acceptance pair moved as §18 predicted: **34 vertices · 205° turn · worst 38° · 5.19 km · ETA 25.26 → 33.63 min**, crossings 38 → **26**, in-margin 0.66 → **0.00 km** and berth price 515.29 → **0.00 s** — the line no longer grazes a boundary at all — with the split at **harvest 181 · graph 158 · search 799 ms** (wall 1 140 ms) and **694 band segments** over the same 431 vertices, 3 682 edges and 92 665 pairs, so the spans sit inside the build's own run-to-run noise. `apk-build.bat` green, **126 focused tests green** over 17 suites, the harness green under `-Dmaro.prebake=true`. **Deferred, named not closed:** the level's own arc-cut alternative, and the surface on which a user would read a named absence — both change what a user sees. → `xTrack/Route/260920_FEAT_PLN_Route_taunt_algorithm-design.md`

- **Route — the review's blockers closed, and the band's limit found as the one midpoint left (2026-09-21, `feature/route`)** — the walk's six-item level was built in one `#implement` Code hop and closed. The corner's price stopped being a second quantity: `Turn.priceSec` now hands the clock the **world's own nominal at both stubs** and only the emitted chords carry the corner's own limit, so the splitter resolves what is in force over the metres each stub really covers. The review settled what the ≈ 280 s had been and the answer is on the record: it lived in the corner's **admission price** (`pricedSec`, the number the search ranks candidate lines by) and never in the ETA, so the acceptance pair's 25.49 → 25.26 min is the corrected price choosing a different line (70 → 63 vertices, 536° → 524°) rather than a residue of the phantom — a zone-carrying world now asserts the phantom in the accumulation, which a world with no zone could not see, and a recording clock pins the nominals. The corner's reference limit became `TautGraph.limitAt(vertex)` — the limit in force at the vertex — and `edgeLimitKn`, `limitKn(edge)` and `LegLimits.limitKn` were deleted as unread. **The user's own defect is fixed:** a layer load already in flight is awaited rather than skipped, the toggle always carries its tap, a tap asks exactly one more preparation, and a refusal is shown through `RouteUnavailableReason.labelResId` on the existing `MapStatusBanner` inside the route's slot, with the string in both locales and no new screen, panel, composable or key. The five should-fixes closed — `covers`' claim, the margin's reading now over the price's own legs (the acceptance pair's in-margin figure 1.03 → 0.66 km), both targets asserted, the orphaned KDoc re-homed onto `arcPriceSec`, the harness's band priced from `AppConfig` — and the split re-read at **280 · 138 · 1 003 ms** against §17's 2 110 ms search. The two inshore pairs are a **named absence**, `TautRefusal.START_OFF_WATER` refused before the graph is built, with snapping the start onto water explicitly declined: the start is the boat's own position by the epic's rule. `apk-build.bat` green, 123 focused tests over 18 suites, the harness green under `-Dmaro.prebake=true`. The review then returned **revise** on one blocker: the **band's** limit is still resolved once per leg from a midpoint boolean and handed to every piece, so a leg straddling the 300 m line is the one limit change the splitter never cuts — an inherited limit, the band being no polygon the tracers own, but it stands against this pass's own claim that every leg is cut where the limit changes. Seven should-fixes ride with it, and four of the pass's printed figures rest on the hop's own run rather than on an independent one. Open work is the walk's level above → `xTrack/Route/260920_FEAT_PLN_Route_taunt_algorithm-design.md`

- **Route — §17's corrections built, and the review found a maximum still pricing the corners (2026-09-21, `feature/route`)** — §17's eight items landed in one `#implement` Code hop. A leg is now priced as its **time summed over its own limit regions**, homed in the build, with one interval test (`TautZone.interiorSpans`) returning the entry and exit of a leg's overlap with a zone interior so the rule, the price and the splitter read one answer — which closes a corner clip run one used to allow and the crossing report counted zero. The corner's charge is written on `Turn.speedMps` at the ladder's floor (4.7 kn), an upper bound on the corner speed for the water case alone, with the **four refusal causes counted apart** (guard 1 · build 0 · cutback 0 · water 7). Readiness is one member (`TautWorld.readiness`, READY · COASTLINE_MISSING · DEPTH_MISSING) answered by the adapter, the harness's world and the test fake alike, read by **both** `prepare()` and `route()`, with the coastline's own load triggered and `RouteUnavailableReason.COASTLINE_NOT_LOADED` carrying a string in each locale. The nine should-fixes closed with a reading each and the five dead members named as they went; the tolerance, the ceiling, the berth and the band's price were not touched. Performance closed on item 7's **second** outcome with its reason beside the new number — the wall index now walks only the cells the line crosses (the pair's graph 387 → 298 ms, the two refusals 7.5 s → 1.7 s and 97.7 s → 5.2 s over the same order of pair count), while the turn-price cache bought nothing (1 922 → 2 110 ms, noise) and is reported rather than claimed; no geometry prune was taken, it being able to delete the only way round. `apk-build.bat` green, 122 focused tests green, the harness green under `-Dmaro.prebake=true`; the acceptance pair reads 70 vertices · 536° · worst 52° · 5.36 km · **25.49 min** against 63 · 448° · 52° · 5.32 km · 22.79, the rise attributed to the zone time the midpoint rule was dropping, at an unchanged 38 crossings and 2.20 km inside the 5 kn zone. The review then returned **revise**: a corner's price is a **second quantity beside the clock** — a zone-clipping leg's stub priced at the leg's most restrictive limit while the clock charges its non-zone metres at cruise, ≈ 280 s on the ordinary case — the corner's **reference speed is a per-leg maximum**, against item 1's own rule, which is where the 448° → 536° and the seven extra vertices come from, and the readiness gate **latches** with its reason reaching no surface, so the toggle stays shut for the session and both new strings are unreachable. Five should-fixes stand beside them, and both other harness pairs are still refused. Open work is the walk's level above → `xTrack/Route/260920_FEAT_PLN_Route_taunt_algorithm-design.md`

- **Route — the revision pass built, and the review sent it back again (2026-09-21, `feature/route`)** — §16's nine steps landed in one `#implement` Code hop. The printed cost is now **three figures per pair** — harvest · graph · search, with the pair and node counts beside them — instead of one number called "search"; the zone margin became a **price in seconds** read at the leg's closest approach with run one forbidding only a zone's **interior**, the hard collar deleted and `route.zoneBerthM`'s documentation rewritten; zone **holes are navigable through one predicate** ([`TautZone.covers`](app/src/main/java/ykws/android/maro/spatial/taut/TautZone.kt:1), pinned by a synthetic strip with a slot); the corridor's box is sized from the obstacles it must contain and its counter is no longer shared across growth attempts; §12.3's brake-and-accelerate pair entered the cost behind **`route.turn.longitudinalAccelMps2`**, the second and last key this feature adds; and §13.3's assertion set exists as `TautAssertionsTest`. `apk-build.bat` green, 113 focused tests green, the harness green under `-Dmaro.prebake=true`; the split moved 321 · 531 · 3 097 ms to 278 · 387 · 802 ms, and the acceptance pair now reads 63 vertices · 448° of turn · worst 52° · 5.32 km · 22.79 min against 50 · 218° · 22° · 5.58 km · 14.96 min. The new assertions earned their keep on their first run, catching the easing rounding a corner **into** a zone interior the rule had forbidden and a corridor-sizing counter shared across growth attempts. The review then returned **revise** a second time: three blockers — a leg's limit resolved at its **midpoint alone**, so a leg clipping a zone is charged at cruise; a **sharp corner carrying no slowdown at all**, `sharp()` zeroing its speed and both guards skipping the term, so §12.3 is only half answered; and readiness gating on the **depth grid alone** while the coastline still answers permissively — plus nine should-fixes; the 500 ms target is missed at 802 ms and the tolerance lever's own price is visible in the line, 218° → 448° of turn and 7 sharp corners against 2. The corrections are ordered in the design's §17 → `xTrack/Route/260920_FEAT_PLN_Route_taunt_algorithm-design.md`

- **Route — the taut-string engine built, switched in, and sent back to revise (2026-09-21, `feature/route`)** — the design of record became code through one `#implement` run: eight files in [`spatial/taut/`](app/src/main/java/ykws/android/maro/spatial/taut) — the world seam, the geometry home, the corridor harvest, the graph, the easing, the search, the engine and its own dossier — carrying the corridor grown once on an empty answer, the one-sided abstraction validated by **exact segment distance**, the **clock-only price with the arrival direction in the search state**, the two runs with the crossing named off the drawn line, the easing ladder with the speed from the tightest radius, chords at the shorter of 10 m and 10°, the limit in force per leg and the engine's **own dossier** rather than the shared result. `route.shoreOffsetM=50` was added with its `AppConfig` accessor and the ceiling set to `2.94` (0.3 g); the app switched to it in **one expression** at [`MapScreen`](app/src/main/java/ykws/android/maro/ui/map/MapScreen.kt), with `MeshRouteEngine`, its repository, serializer, search and bake left in the tree and wired to nothing. `apk-build.bat` green, **83 suites** green, three new test classes, and the harness green under `-Dmaro.prebake=true`; the acceptance pair (Baie des Milliardaires → Port de la Salis) answers at 50 vertices, 218° of turn, worst corner 22°, 5.58 km, 15.0 min. The review returned **revise** with six blocking findings — the zone berth enforced as a **hard collar** where the plan prices it, zone **holes walled** though §10.2 calls them navigable, the corridor's margin dropped to 1 NM so an obstacle between the ends can fall outside the box, §12.3's **brake-and-accelerate term absent** with no longitudinal property to carry it, the **3 653 ms** acceptance run against the 500 ms target folding harvest, graph and search into one figure whose cost is the eager all-pairs visibility loop, and §13.3's **assertion set unbuilt**, two refused pairs passing the suite. The contradiction it exposed is resolved in the design — the zone margin stays a price, **expressed in seconds** — and its remaining findings, the corridor margin and the assertion set first among them, are owed → `xTrack/Route/260920_FEAT_PLN_Route_taunt_algorithm-design.md`

- **Route engine seam — the engine became a parameter, and the review's six should-fixes closed (2026-09-20, `feature/route`)** — the seam's review returned **ship** with six should-fixes, and this pass closed all six. The one that mattered: [`RouteViewModel`](app/src/main/java/ykws/android/maro/ui/map/RouteViewModel.kt:177) no longer builds an engine, it **takes one**, and [`MapScreen`](app/src/main/java/ykws/android/maro/ui/map/MapScreen.kt:573) builds the shipped `MeshRouteEngine.overBundle(...)` once at its own composition point from the coastline and regulation instances it already holds — a swap is one expression at one site, and a test can hand in a foreign engine. [`RouteEngineSeamTest`](app/src/test/java/ykws/android/maro/ui/map/RouteEngineSeamTest.kt:1) is a **real reading** now: a straight-line engine that knows no mesh is driven through `beginDraft` → `preview` → `confirm` → `recompute` and through the readiness retry, and the plan, the trip figure and the save are asserted against numbers the test derives itself rather than against the object under test (6 tests, none true by construction). [`RouteEngine.prepare()`](app/src/main/java/ykws/android/maro/spatial/RouteEngine.kt:43) now states what `Ready` does **not** promise — the mesh engine loads the mesh alone, so the live layers arrive per search and a first route may be priced permissively — and both loads are **single-flight** (`Mutex` with a re-read under it, in `RouteMeshRepository` and `RouteSpatialAdapter`). Three KDocs that claimed a joined cancel were corrected **rather than the scheduler**: one search is *started* at a time, never one *running* at a time. The tests gained one home for "a route with no dossier reads as all zeros", with a strict reader beside it so a counting test can no longer pass on a missing dossier. Left standing on purpose: the closed-set `Unavailable` reason and its string, and `details`' marker shape. **Reported, not fixed:** the band mark and the forced-crossing report are still derived inside the mesh path — the seam plan's §3, couplings 5–6 — and `inBand` reaches `RoutePlan` and stops, nothing under `ui/` reading it. `assembleDebug` green, **572 tests** green, and the probe re-read unchanged with its fallback line at **0 of 8** → `xTrack/Route/260920_FEAT_PLN_Route_engine-seam-and-parking.md`

- **Route — the pipeline's review of the crossing pass, and the crash it caught (2026-09-20, `feature/route`)** — the review's verdict was **revise**, and its blocking finding was a crash path rather than a wrong number: the merge and the shortcut compared the legs they replaced and never the turn at the corner they rotated, so a legitimate route could breach the drawn-against-accumulated invariant — one degree at a 90° corner on a 28 kn leg is 0.15 s against a slack of 1e-6 s — and the `check` that threw was caught by neither the preview nor the recompute. Both admissions now charge every turn they change, through one home, so admission compares the quantity the drawn clock charges; [`arcPriceSec()`](app/src/main/java/ykws/android/maro/spatial/RoutePlanTiming.kt:105) replaced the pass's last duplicated arithmetic; and a breach returns the **raw node chain** with a warning and a flag instead of throwing, losing smoothing being survivable where losing the route is not. The crossing report walked a 50 m step, so a clip shorter than the step could go unreported and a forced crossing would have read as an ordinary route; every leg is now walked in at least two halved intervals. The fillet's refusals were re-ordered to geometry, water, then price so that no count names a constraint it never consulted, four dead members were removed, three tests that would have passed on a revert were strengthened, and three readings pinned nowhere were added. The probe re-read unchanged on all five pairs with the fallback firing **0 of 8** times; 566 tests green → `xTrack/Route/260920_FEAT_PLN_Route_trajectory-quality.md`

- **Route — the zone crossing became an invariant, and the clock followed the drawn line (2026-09-20, `feature/route`)** — the device reported the straightened line now cutting across zones the search had rounded, and the cause was the cost model being bypassed: the mesh's water includes a zone's interior by design, a limit being a restriction and not a barrier, so the search was always free to cross and paid the limit, while the shortcut pass tested containment and stretch and asked no price at all. Three parts, in the order an independent review's verdict demanded. **P3** — one home, [`RoutePlanTiming.drawnLegSeconds()`](app/src/main/java/ykws/android/maro/spatial/RoutePlanTiming.kt:57), reads a plan's seconds off its own drawn polyline, each leg at the limit inherited from the chain edge it was cut from and the most restrictive one where a leg spans several, so the trip cell, the confirm panel and the saved course report one clock instead of two; the ~15 % overstatement is closed, the ETA now describing the line on the screen. **P0** — every pass re-prices what it changes, and the invariant *the drawn line's own cost must never exceed the seconds the search accumulated* is asserted in `buildSuccess()`, where every run reaches it. It fired on its first run and named the pass the task had not: the **fillet**, whose whole arc at a corner between a fast and a slow leg pays the slow limit and is therefore dearer than the corner it replaces by `R·θ/2·(1/v_slow − v_fast)`. The plan's own rule was applied rather than the invariant loosened — [`arcNoWorse()`](app/src/main/java/ykws/android/maro/spatial/RouteSearch.kt:686) refuses an arc dearer than its corner, counted as `sharpNoPrice` at 1 to 2 per pair — and P0 now covers the merge and the fillet too. **P2 with its fallback** — a priced zone's interior returns the reserved sentinel and is not traversable, the 300 m band is never forbidden, and the exception is per zone against the nodes the search resolved, covering **both ends** — the start as well as the destination — so a boat already inside a zone can still leave it and one aimed into a zone can still arrive; the fallback is a **second priced run**, not a second objective, the heap being keyed on one `Double`; and the result carries the crossing it took with the zone names read off the drawn line, which is a report and never a rule. The invariant stays the guard on all three parts — *the drawn line must never cost more than the seconds the search accumulated*, asserted where every run reaches it, so a pass that changes the line without re-pricing it is caught rather than shipped. The adoption gate held: 8 of 8 metric pairs routed, none lost and none forced, so P2 stands and the factor P1 swept drops to a tuning alternative. 563 tests pass, 11 of them new → `xTrack/Route/260920_FEAT_PLN_Route_trajectory-quality.md`

- **Route — the line straightened on the boat's own water, and the berth answered by geometry (2026-09-20, `feature/route`)** — the substitution the study made — a turn *price* in place of a string-pull — bought six percent and was reported as "the line straightened"; the device said otherwise, so the pass was built properly: a **shortcut walk over the search's own node chain**, every candidate tested by the bake's kept triangles **and** the boat's own stretch, which is exactly the test the first sampled string-pull could not make. On four inshore pairs in the Antibes / Golfe-Juan / Cap d'Antibes water it drops **87–94 % of the drawn vertices and 84–99 % of the turning** and shortens the line by **6–23 %** (inshore 1: 218 → 17 vertices, 932° → 56°, 4.77 → 4.01 km), and the pairs are derived from the mesh by a rule rather than typed in, each printing its own share of the 300 m band. The berth then went to the **hybrid** the user chose: 25 m cut hard **only where cutting severs nothing** — 38 of 121 zones, of which 21 cut, removing 0.11 % of the mesh's water, while 83 stay priced because their strip *is* the way through; not one of the four inshore pairs moved, which settles the question structurally: on this coast the passages beside a zone are a few tens of metres wide, so a 25 m berth there is water that does not exist, and the choice is the close pass or no coastal passage at all. Two deviations are on the record — the shortcut skips the coastline oracle for speed (10,893 ms → 171 ms, the containment test being the mesh's own water assertion) and the **ETA still describes the priced path rather than the drawn line**, overstating by ~15 % on a long route, which waits on the user → `xTrack/Route/260920_FEAT_PLN_Route_trajectory-quality.md`

- **Route — trajectory quality: the line straightened, the corners rounded, the bay reconnected (2026-09-20, `feature/route`)** — the study's sampled string-pull was retired for a **turn penalty in the search's cost** (a sample test cannot see a small hole the gate closed; a path on the mesh's own edges cannot leave covered water at all), with a collinear merge: 413 → 255 vertices, 7,366° → 6,931° of total turn, worst corner 110° → 100°, length and ETA inside a quarter of a percent. The bake ships its **90,242 triangles** (+699 KiB) so inside-the-mesh is an exact test, and on that footing a **fillet** rounds each retained corner at `R = v² / a` from `route.turn.lateralAccelMps2` — which the pipeline's review found had **never drawn a curve**: its arc was built on `R / tan(θ/2)` and `R / sin(θ/2)` where `R · tan(θ/2)` and `R / cos(θ/2)` belong, so it refused every corner not nearly a right angle while every test case sat at 90°, where the two forms agree. Corrected, with the geometry gathered into one `RouteTurnGeometry` and 30°, 60° and 120° cases added, the fillet rounds every corner the mesh allows — **0 sharp of 446** at the default **0.5 m/s²** the sweep over the property's true range chose. The zone berth became a **price rather than a wall**: +0.23 % to +0.74 % of length, in-berth kilometres down 2.5 % to 6.8 %, nothing deleted and no passage closed. The depth gate gained a **bridging pass** keeping an unsounded triangle only where it reconnects two stretches, one bounded corridor per pair: 21 → 13 stretches for +19 nodes and +1,249 bytes, the reported bay merged, and the tripwire's own number **0.0 km² of unsounded water admitted against 1,228.5 km² routed**. The bake is bit-stable across runs and the suite is green at 76 suites; the device pass stays owed → `xTrack/Route/260920_FEAT_PLN_Route_trajectory-quality.md`

- **Route — the confirmation is the dashboard slot's panel (2026-09-20, `feature/route`)** — the floating ladder dialog is gone: while a route is aimed the shell composes [`RouteConfirmationPanel`](app/src/main/java/ykws/android/maro/ui/map/RouteConfirmPanel.kt:1) into the dashboard slot, in both orientations, so the details of the aimed route, the pin the save would start with and the four outcomes sit where the eye already is and nothing floats over the map being dragged. It is built from what exists rather than re-drawn — `ConfirmActionButton`, made `internal` with a `modifier` so a weighted row can place it, and `RoutePinOption` — while the aim ring lost its hint bubble and the draft gained an `asked` flag so "nothing asked yet" and "no route to this aim" stay apart. With the ladder no longer carrying the route, `ConfirmRequest.modal` and its scrim clause are removed and the composition-local wrapper around the route slot goes with them; the back key now ends the mode from the host. `apk-build.bat` green and `testDebugUnitTest` green, with the panel's on-screen behaviour left to the device pass → `xTrack/Route/260919_FEAT_PLN_Route_registration-and-drift.md`

- **Route — the confirmation panel had no host (2026-09-20, `feature/route`)** — the panel the correction pass made ride the aim could never appear on device, and never had: `LocalConfirmDialogHost` is provided only around `OverlayLayer`, while the route is composed through `MapContent`, a sibling composed before that provider, so [`RouteHost`](app/src/main/java/ykws/android/maro/ui/map/RouteHost.kt:117) read the ambient `null` and every raise returned early — which is also why the first, tap-to-confirm version failed the same way. The route slot now carries the provider at its own composition point, the same instance the ladder receives, and a `MaroRoute` warning records any raise that still finds no host, since a silent early return had already cost a device cycle. `apk-build.bat` green and 518 tests green with the route suites at 33 of them; no test covers the wiring, the project carrying no instrumentation harness, so the panel's appearance remains device evidence → `xTrack/Route/260919_FEAT_PLN_Route_registration-and-drift.md`

- **Route device correction — the start frozen at the toggle, the dialog riding the aim (2026-09-20, `feature/route`)** — the user's on-device report: in demo mode the route's start was re-read on every preview and the dashboard's seam resolves to the map centre there, so both ends of the route moved with the finger and the search answered a degenerate green wiggle between the last centre and the new one. The start is now captured at the mode's own `Idle → Draft` edge and carried by the Draft, with `preview(aim)` taking no start at all, so a re-read is impossible by construction in either mode; the confirmation dialog rises with the mode instead of from a tap on the aim, its details tracking the aim live, which needed a scrim-free variant of the shared confirm (`ConfirmRequest.modal`, default `true`, the route passing `false`) and one line in the shell keeping the ladder's scrim truthful; Confirm locks the route, while Cancel, the back key and the toggle all end the mode; the aim target lost its tap, the zoom-to-fit was dropped for fighting the drag, and the missing `formatted="false"` on `settings_zone300_opacity_desc` — which had stopped resource compilation outright — was fixed in both locales. `apk-build.bat` green and 32 focused tests green; the device behaviours themselves remain unobserved → `xTrack/Route/260919_FEAT_PLN_Route_registration-and-drift.md`

- **Route prerequisites — one home for the conversions and the two zone values (2026-09-20, `feature/route`)** — the knot and nautical-mile conversions folded into `spatial/Units.kt` with every local spelling deleted and fifteen call sites thinned, and the 300 m band's value and the zone-to-effective-limit mapping each given one home (`AppConfig.zoneRegulatorySpeedKn`, `RegulatedZone.effectiveSpeedLimitKn()`) whose three former copies now read them → `xTrack/Route/260919_FEAT_PLN_Route_registration-and-drift.md`
- **Route engine — the packed mesh, its A* and its bake (2026-09-20, `feature/route`)** — javalite packed primitive arrays with CSR offsets beside `coastline.proto` and `depth.proto`, filling the same flat arrays the search reads; a stream-deserializing repository; a cancellable A* whose cost is recomputed per search from the zone layer and whose heuristic is admissible by construction; the JTS and poly2tri prebake builder — noded overlay, per-face constrained Delaunay, bounded graded refinement, the 2.5 m depth gate held on a triangle's vertices, the 30 m channel floor and a union-find stretch label — baked for `nice-menton` at 46,621 nodes, 136,853 edges, 21 stretches and 3.1 MB, with a worst-case search of 127.9 ms inside the ≤ 500 ms target; `tools/bake-route.bat` refusing by name on a missing input and wired into `apk-bake.bat` as `:do_route` after the depth and zone steps → `xTrack/Route/260919_FEAT_PLN_Route_registration-and-drift.md`
- **Route app side — the toggle, the aim, the trip figure and the save (2026-09-20, `feature/route`)** — `RouteViewModel` on `StateFlow` over Idle → Draft → Confirmed with no arrival state, arrival being the trip cell reading zero; the route toggle gated on the mesh being ready, mutually exclusive with inspect and off ending the route; the screen-centred aim with a movement-driven, cancellation-backed preview; `RouteHost` owning the polyline and the pin between tracks and markers with the start taken from the dashboard's own position seam and demo mode's sailing suspended; one `ConfirmDialog` with `Route`, `Save and route` and `Save only` stacked above Cancel and the pin checkbox default off; the trip figure in the dashboard's distance-to-shore cell with its age and a tap-to-recompute; `TrackFromCourse` writing an ordinary track through `plannedCourse` at `@ProtoNumber(19)`; the three seams and the line's appearance as `maro.properties` keys behind `AppConfig`; 518 tests green and the build green, with one review blocking finding — the aim resolved under a radius cap instead of against the boat's own stretch — raised and closed; the on-device pass over the aim, the fling cancellation, the zoom-to-fit and the dialog remains owed → `xTrack/Route/260919_FEAT_PLN_Route_registration-and-drift.md`
