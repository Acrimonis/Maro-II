# Context Hydration — Route — 2026-09-26

**Last Bake:** 2026-09-26 11:28 UTC — written by `#bake`

**Directive trace:** three covered action classes ran this session, each on the user's own word — the branch was cut on the `#new menu-route` invocation (a fetch and `checkout --no-track -b`, in Code mode, carrying one modified state file across), the record was moved on the explicit `#archive` invocation (a plain filesystem `move`), and the commit ran on the `#commit` invocation after its scope gate was answered (`572444a`, nine paths, nothing else staged). Both builds that ran were the pipeline's and the polish hop's own (`apk-build.bat`, in Code mode); no dependency was added, no machine-shaped data file was opened, the device was never touched and nothing was deployed. Every claim about the code came from a file read or a tool's own output.

## State

The session designed the drawer's route surface and shipped it in the same pass: the side menu's Route action group left whole — `RoutePill`, the `route` glyph import, the title-and-comment block, the three pills and the seven route parameters — and a read-only summary of the mode took its place in the Navigation card, carrying the status word with the engine's stage, the plan's own distance and ETA and, while following, the boat-relative pair under its own sub-title. [`RoutePlan.remainingFrom`](../../app/src/main/java/ykws/android/maro/ui/map/RouteViewModel.kt:85) now measures what is left from the nearest **point** of the line rather than its nearest vertex, `RouteOverlayData` became `RouteSummaryData` with its visibility in `OverlayChrome`, the ETA split gained one home in `routeEtaText()`, and the five `menu_route_*` keys left both locales for one new key. `apk-build.bat` SUCCESSFUL with `app-debug.apk` produced and the route-filtered suites green at **126 tests in 18 suites, 0 failures**; the Ask hop returned **ship**, and its three code-side should-fixes were closed after it — the pair printing only when both halves are present, the block's gate narrowed to stand only while a search runs or a plan stands, and `RoutePlanTest`'s doc naming the halfway case as the fractional pin. The record: the 2026-09-22 build order retired through `#archive`, the parked duplicate-save point closed by the user's word, the epic re-baked, and `572444a` committed before the implementation ran.

## Target Files

- `xTrack/Route/260926_FEAT_PLN_Route_menu-mode-summary.md` — the design of record and its `## Outcome`: the removal inventory, the summary's rows and gate, the bundle swap, the geometry change, and the polish pass
- `app/src/main/java/ykws/android/maro/ui/map/RouteViewModel.kt` — `remainingFrom` projected onto the nearest leg, its KDoc rewritten, `nearestVertexIndex` retired into it
- `app/src/main/java/ykws/android/maro/ui/map/MenuDrawerOverlay.kt` — the Route group and `RoutePill` gone, `RouteSummaryBlock` in their place with the pair gated on both halves
- `app/src/main/java/ykws/android/maro/ui/map/OverlayLayerParams.kt` · `OverlayLayer.kt` · `MapScreen.kt` — `RouteSummaryData`, the chrome flag and its narrowed predicate, and the three menu-only aliases deleted
- `app/src/main/java/ykws/android/maro/ui/map/RouteOverlay.kt` · `RouteConfirmPanel.kt` — `routeEtaText()` and the panel moved onto it
- `app/src/main/res/values/strings.xml` · `values-fr/strings.xml` — five `menu_route_*` keys out, `route_trip_remaining` in
- `app/src/test/java/ykws/android/maro/ui/map/RoutePlanTest.kt` — the halfway case added, the leg-times pin restated, its doc naming the fractional pin
- `xTrack/Route/FEAT_DSC_Route.md` · `xTrack/Ui_Menu/FEAT_DSC_Ui_Menu.md` · `260924_FEAT_PLN_Route_acquisition-and-route-workflow.md` — the record: `## Implemented`, the `## Docs` pointer, the seam sentences, the pointer line and the snap sentence

## Next Step

Nothing of this session's work is owed — the plan shipped whole, the review's three code-side should-fixes are closed, and the record is level with both. What is left is the water and two words: the drawer's look with the trip card's new reading is a device pass, which is the user's, and the sub-title's wording beside the `route` glyph's deletion stay theirs to give. Then the feature's own standing work: the fine band's mechanism and its width against the inert `route.avoid.fine.cellRatio=0.40`, and the progressive-draw plan's seven findings unfolded.
