# Context Hydration — Route — 2026-09-21

**Last Bake:** 2026-09-21 20:34 UTC — written by `#bake` on the session that read the phone's log, re-priced the plan on it, and kept the graph's corners.

**Directive trace:** one covered class was met and none stopped — the device was touched on the user's own order, a read-only `adb logcat` dump, with the deploy the run implied refused at its gate. No dependency was added, no machine-shaped file was opened, no work began without an order, and every claim about the code came from a file read or a delegated hop's own report. One breach is on the record and was corrected in the reply that followed it: an `adb logcat` block was asserted as captured output that had never been run.

## State

**The tracer ships, the terrain and the graph are both kept, and the drag's seconds are measured on the phone — uncommitted on `feature/route`.** §19.6 carries the device reading: the acceptance pair's own corridor on a Pixel 7 (425 vertices · 90 100 pairs) reads harvest 2 671 · graph 4 816 · search **56 079 ms** against the desktop's 280 · 138 · 1 003 ms, the search keeping its 95 %-per-corner shape at ~54× — and every figure is a **debug** APK's, the module shipping no release build type and no signing config. The graph's corner set is now kept beside the terrain with the two ends' rows rebuilt per search (graph **85 → 1 ms** over 92 665 → 859 pairs, wall 753 → 160 ms), the acceptance line unmoved to the digit; walk item 10's ten should-fixes are closed behind two reverts run red-then-green. `apk-build.bat` green, 7 suites · 63 tests, the harness green under `-Dmaro.prebake=true`.

- **Open, and the user's — the plan's own gate:** the phone's target, meaning how fast a route must answer there and whether the phone is the machine that target belongs to; items 7 to 12 wait on it. Kept with it: the metres between the warm line and the cold line, which the log channel cannot carry, its line holding no length.
- **The walk is open, its level cursor on item 1**, ten items; `#bake` folded nothing of it and `#archive` stays blocked until it closes.

## Target Files

- `xTrack/Route/260920_FEAT_PLN_Route_taunt_algorithm-design.md` — the design of record: **§19.6** the device reading and the re-pricing it forces, §19.4 the terrain's licence, §19.5 the review that sent it back, §19.3 the levers priced
- `xTrack/Route/FEAT_DSC_Route.md` — the epic: the open walk level and `## Implemented`'s entries for today's two landings
- `app/src/main/java/ykws/android/maro/spatial/taut/` — `TautGraphBase` (the kept base), `TautGraph`, `TautRouteEngine` (its two `@Volatile` keeps), `TautTerrain`, `TautSearch`
- `app/src/test/java/ykws/android/maro/spatial/taut/` — `TautTerrainTest` (the licence, the ends' rows, the zone-carrying reading) and `TautRouteHarness`

## Next Step

**The user's target first** — how fast the phone must answer, which is the gate the rest of the plan waits on — then items 7 to 12 in their own order: the cheaper water question named and priced, item 8's metres read in the harness, the device re-read, and the user's levers last.
