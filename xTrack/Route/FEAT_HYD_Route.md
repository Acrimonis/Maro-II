# Context Hydration — Route — 2026-09-22

**Last Bake:** 2026-09-22 19:27 UTC — written by `#bake`

**Directive trace:** every class that carries a verdict line was met and none stopped. The git writes were the user's own words — `#commit`, `#push`, then `#merge` executed on the gate's yes — and the mode switch to Code was asked for before any of them. The session's one code change was the `@Transient` on `TrackSummary.isLive`, and its root cause was read out of the failing test's own exception rather than guessed. The archive moved one plan on the user's own pick of the multi-select, after the recipe's qualifying question was answered from the epic's text. No dependency was added, no machine-shaped data file was opened, no device was touched, and every claim about the tree came from a file read or a command's own output.

## State

The branch carries `origin/develop` since the same day's merge — one conflict, in `xTrack/GLOBAL_CONTEXT.md`, resolved by hand with both sides' routing rows kept and the focus stack pruned to ten. **The merge's validation earned its keep:** five tests arrived red from develop, all in [`TrackSummaryPositionTest`](../../app/src/test/java/ykws/android/maro/data/track/TrackSummaryPositionTest.kt), because [`TrackSummary.isLive`](../../app/src/main/java/ykws/android/maro/data/track/Track.kt:131) — a body property with no `@ProtoNumber` — was numbered 18, the number [`landPointCount`](../../app/src/main/java/ykws/android/maro/data/track/Track.kt:116) already held, so a land count on the wire decoded as a boolean and was thrown away on reload, reading land as water. It is `@Transient` now (`82f6be2`), and the suite is 535 tests with no failure. **The delivery itself is still the two build orders and no implementation:** the interaction spec is the master requirement book — R1–R28 for the mode, R29–R42 for the trace — both plans reference it rather than restating it, nothing in it is open, and the order is stated: the Route work lands first, its save writing the flag the trace work renames. Both features' walks stand settled, and the Tracks 2026-09-15 level still parks the eye's own value as a resume point, named rather than resumed. The removal plan retired into `xTrack/Route/xxArchive/` on the user's pick — its `## Outcome` written, its index row added, its links re-based, its `## Docs` bullet and its `## Implemented` pointer detached — so the epic's `## Docs` now lists five live entries and names five archived plans.

## Target Files

- `xTrack/Route/260922_FEAT_PLN_Route_ask-policy-and-target-validity.md` — the master requirement book: R1–R42, the saving and naming rules, the trace's own detail and the technical values
- `xTrack/Route/260922_FEAT_PLN_Route_interaction-build.md` — the mode's build order, eleven steps each naming its files
- `xTrack/Tracks/260922_FEAT_PLN_Tracks_trace-flag-and-display.md` — the trace work's build order, eight steps, with its file inventory and verification
- `xTrack/Route/FEAT_DSC_Route.md` — the epic: its rules, its `## Docs` and the closed walk level
- `xTrack/Tracks/FEAT_DSC_Tracks.md` — the Tracks epic: the trace plan's pointer and the closed level it opened for the trace work

## Next Step

The two builds in the stated order: the Route plan's eleven steps first — the ask keys and the arming-frame silence, the single worker, the seam as a session with the placeholder's fixed 15 kn, validity and the crosshair, both phases' slot content, the one exit dialog, the refresh cycle, the ladder, the couplings and the save — then the trace plan's eight, each with the files and the strings its own steps name.
