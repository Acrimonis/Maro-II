# Context Hydration — Route — 2026-09-22

**Last Bake:** 2026-09-22 19:06 UTC — written by `#bake`

**Directive trace:** every class that carries a verdict line was met and none stopped — the day's file changes were docs the user asked for, named before they ran; no dependency was added, no machine-shaped data file was opened, no device was touched, and no git write happened in this mode, the `#commit` and `#push` the user invoked needing a mode with command access. Every claim about code came from a file read.

## State

The branch carries two complete build orders and no implementation. The interaction spec is now the delivery's **master requirement book** — R1–R28 for the route mode, R29–R42 for the trace a saved route becomes — and both plans reference it rather than restating it: the mode's eleven steps in [`260922_FEAT_PLN_Route_interaction-build.md`](260922_FEAT_PLN_Route_interaction-build.md) and the trace work's eight in [`../Tracks/260922_FEAT_PLN_Tracks_trace-flag-and-display.md`](../Tracks/260922_FEAT_PLN_Tracks_trace-flag-and-display.md). Nothing in the book is open; the two "Follow again" suggestions are tracked and not built. The order between the builds is stated: the Route work lands first, its save writing the flag the trace work renames. Both features' walks stand settled — this feature's level closed with ten items and one dropped (the draught gate), and the Tracks level for the trace's four points opened and closed — and a parked 2026-09-15 level in Tracks still holds the eye's own value as a resume point, named rather than resumed.

## Target Files

- `xTrack/Route/260922_FEAT_PLN_Route_ask-policy-and-target-validity.md` — the master requirement book: R1–R42, the saving and naming rules, the trace's own detail and the technical values
- `xTrack/Route/260922_FEAT_PLN_Route_interaction-build.md` — the mode's build order, eleven steps each naming its files
- `xTrack/Tracks/260922_FEAT_PLN_Tracks_trace-flag-and-display.md` — the trace work's build order, eight steps, with its file inventory and verification
- `xTrack/Route/FEAT_DSC_Route.md` — the epic: its rules, its `## Docs` and the closed walk level
- `xTrack/Tracks/FEAT_DSC_Tracks.md` — the Tracks epic: the trace plan's pointer and the closed level it opened for the trace work

## Next Step

The two builds in the stated order: the Route plan's eleven steps first — the ask keys and the arming-frame silence, the single worker, the seam as a session with the placeholder's fixed 15 kn, validity and the crosshair, both phases' slot content, the one exit dialog, the refresh cycle, the ladder, the couplings and the save — then the trace plan's eight, each with the files and the strings its own steps name.
