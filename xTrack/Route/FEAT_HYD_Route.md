# Context Hydration — Route — 2026-09-25

**Last Bake:** 2026-09-25 19:58 UTC — written by `#bake`

**Directive trace:** one covered action class ran this session and none stopped — the merge wrote the tree and the index, `git add` staging the resolutions under the ordered merge, and no other git write issued; no dependency was added, no machine-shaped data file was opened, the device was never touched and nothing was deployed. Every claim about the code came from a file read or a command's own output.

## State

The branch now carries both generations of the route work in one tree: `feature/route-avoid-workflow` merged `origin/feature/route-avoid` as `8107a12`, taking the remote's phases 2–4 engine — one HARD/SOFT cost field, the 3 m depth gate, the priced 300 m band chorded by its tangent look-ahead, and the priced, excludable speed-limit zones with a zone-aware ETA — and porting the branch's own stage channel back onto it, so the engine publishes the boundary it is at through `_stage`, cleared in one `finally`. Five files conflicted and were resolved by policy: the engine from the remote, the seam test and this hydration from the branch, the Focus History unioned newest-first, and the epic keeping both sides with its duplicated doc bullet and front-matter date deduped. Two test fakes predated the world interface's `depthReady`, `bandWidthM` and `depthAt` and were given them. `gradlew :app:assembleDebug :app:testDebugUnitTest` is green over 93 suites with 0 failures and `app-debug.apk` is produced; nothing is pushed, and the branch stands 11 commits ahead of the upstream name it still records. The mode itself ships unchanged, the progressive-draw plan stays **in design, reviewed revise with its seven findings unfolded**, and the incoming session's `route.avoid.fine.cellRatio=0.40` ships inert.

## Target Files

- `app/src/main/java/ykws/android/maro/spatial/RouteAvoidEngine.kt` — the merged phases 2–4 engine, and the stage channel ported back onto it
- `app/src/main/java/ykws/android/maro/spatial/RouteEngine.kt` · `RouteDummyEngine.kt` · `ui/map/RouteViewModel.kt` — the seam's nullable `stage` member, the dummy's null channel, and the ViewModel proxy that carries it to the panel
- `app/src/test/java/ykws/android/maro/ui/map/RouteEngineSeamTest.kt` · `RouteAcquisitionTest.kt` — the two fakes widened for the world interface
- `app/src/main/assets/maro.properties` — the merged keys: `route.anchor.leadSec` kept, `route.ask.*` gone, the `route.avoid.*` dot taxonomy in
- `xTrack/Route/FEAT_DSC_Route.md` · `xTrack/GLOBAL_CONTEXT.md` — the bake's rewrite of the epic, the Feature Summaries row and the focus stack
- `xTrack/Route/260925_FEAT_PLN_Route_progressive-draw.md` — the design of record for the drawing pass, awaiting its seven findings

## Next Step

Fold the progressive-draw review's seven findings into its plan, worst first, and put its one reopened question back to the user — nothing is drawn during the A\*, where the time goes, so an emission inside the A\* is the only lever that would meet the brief as read. Standing from the 2026-09-24 review, untouched: the discarded engine search a re-entry's first `Acquire route` pays, the lead's freshness gate, the session's twin link table, and the two session members only tests call. Owed from the merged side: the fine band's mechanism and its width (Change 4), against a ratio nothing reads yet.
