# Context Hydration — Route — 2026-09-21

**Last Bake:** 2026-09-21 18:43 UTC — written by `#bake` on the session that measured the search's cost, built the drag's four items, and kept a corridor's terrain for a box.

**Directive trace:** no covered action stopped in this session — no dependency was added, no machine-shaped file was opened (the `.bin` assets were only ever reached through the app's own readers), no work began without an order, and every claim about the code came from a file read or from a delegated hop's own report. **The device was touched, on the user's explicit order:** `adb install -r` of the built APK, and the crash buffer read only after the user reported the crash. Two verdicts were recorded *against* the day's own work rather than for it: the judge's memo was **refused by its own reading** (a 9 % hit rate), and the review's two blockers on the terrain were closed as blockers rather than argued down.

## State

**The tracer ships, the drag is coalesced and cancellable, and a corridor's terrain is kept for a box — uncommitted on `feature/route`.** The search's seconds are measured rather than estimated: 93 % is per-corner work, the water judge 601 of the fit's 634 ms, the clock 167 ms, and the ladder only 1.6 candidates a fit — so the easing ladder is cleared as a target and the judge named as the lever. Four agent-side items landed (the one log line per answered route and the panel's third state, cancellation inside the harvest, the pair scan and the corner door, and the drag coalesced, pinned by a gated foreign engine), the judge's memo was built and **refused by its own reading**, and item 8's first cut keeps a corridor's **terrain** — obstacles, zones, band — for the box it was harvested for, reusing it while the next corridor lies inside it: a repeat search reads **739 → 204 ms** with the harvest 55 → 0. The review returned **revise** with two real blockers, both closed: the reuse **licence** (now the largest box a cold search could build from the rough corridor) and the **scratch** in the wall index and the band, where a plain `stamp++` over a shared array read **13 960 of 100 000 answers wrong** under two threads and could accept a leg crossing land. The acceptance pair is unchanged throughout — 54 vertices · 307° · worst 30° · 5.44 km · 27.40 min, the two inshore pairs refused by name, the grown-corridor probe still *only a priced crossing*; **145 tests green**, the harness green under `-Dmaro.prebake=true`, `apk-build.bat` green.

- **Open, and the user's — the reading that decides item 8:** over 6 aims of one drag the warm line differs from the cold one on 5, never dearer (~6e-6), and **how far apart the two lines stand in metres** decides between the **slice** (the kept terrain filtered to the new box, exact) and accepting the divergence with the guard re-scoped from *the same line* to *never dearer*. §19.3's d–g — the corridor, candidate turns pruned by angle, the invented vertex prune, the weighted heuristic — stay priced and the user's.
- **The walk's level is open, its cursor on item 8:** items 1, 1a and 2–6 closed with their readings, item 7 **kept by the user's word** (its guard found vacuous — the two refused pairs are refused before the corridor is ever built), item 8 in build with the reading above.
- **The device pass is blocked, and not by Route:** the app dies at launch with an `OutOfMemoryError` decoding the bundled depth grid — **thirteen** occurrences in the crash buffer from 09-15 to today, 09:21 11:03 → 20:37 — the decode holding the whole grid twice over on a 256 MB heap while `readBundled` catches `Exception` and an `OutOfMemoryError` is an `Error`; a self-contained prompt for a **DepthMapping** session was handed to the user.

## Target Files

- `xTrack/Route/260920_FEAT_PLN_Route_taunt_algorithm-design.md` — **the design of record**: §16–§19, with §19.2's levers priced, §19.3's re-pricing, §19.4 the terrain's step and §19.5 the review that sent it back
- `xTrack/Route/FEAT_DSC_Route.md` — the epic: the walk's open level with every item's reading, and `## Implemented`'s entries for this session
- `app/src/main/java/ykws/android/maro/spatial/taut/` — `TautTerrain` (the kept terrain), `TautObstacles` (the judge's memo and its unique stamps), `TautGraph`, `TautSearch`, `TautRouteEngine`
- `app/src/main/java/ykws/android/maro/ui/map/RouteViewModel.kt` — the coalescing slot and its drain loop; `RouteConfirmPanel.kt` — the panel's third state
- `app/src/test/java/ykws/android/maro/spatial/taut/` — `TautTerrainTest` and the harness's reuse reading; `app/src/test/java/ykws/android/maro/ui/map/RouteEngineSeamTest.kt` — the gated drag test

## Next Step

**The metres reading for item 8** — per aim of one drag, how far the warm line stands from the cold one — because it is what decides between slicing the kept terrain and accepting the divergence; then the user's word on that choice, and the device pass, which waits on the depth cache's launch OOM being fixed in its own feature.
