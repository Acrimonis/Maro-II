# Context Hydration — Route — 2026-09-22

**Last Bake:** 2026-09-22 09:22 UTC — written by `#bake`

**Directive trace:** one covered class was met and none stopped — a branch was cut and the two engines' files were deleted on the user's own word of 2026-09-22, the action named before it ran. No dependency was added (two were removed), no machine-shaped data file was opened, no device was touched, no git write beyond that authorised branch and the `#commit` the user invoked, and every claim about the code came from a file read.

## State

The two engines are gone, each documented in its own file, and `RouteDummyEngine` is what the app draws with: one straight segment from the frozen start to the aim, `Ready` on construction, built at **one expression** in `MapScreen` behind the unchanged `RouteEngine` seam. Everything around the line is whole — the toggle, the aim, the preview, the confirmation panel with its four outcomes, the pin, the trip figure and the save as an ordinary track. Compile, `apk-build.bat` and `:app:testDebugUnitTest` are green (74 result files, no failure) on `feature/route-dummy`. **What a route should be is unanswered** — the water it may use, what it owes a zone and a depth, what it costs in time — and the epic carries that as the `routing-engine` section's first todo.

## Target Files

- `xTrack/Route/260922_FEAT_DOC_Route_mesh-engine.md` — the first engine as it was: its flow, its limits, its figures, why it went
- `xTrack/Route/260922_FEAT_DOC_Route_taut-tracer.md` — the second, the same shape, with the desktop and Pixel 7 readings
- `xTrack/Route/260922_FEAT_PLN_Route_dummy-engine-and-engine-removal.md` — the removal: the kept framework, the deletion inventory, the verification
- `app/src/main/java/ykws/android/maro/spatial/RouteDummyEngine.kt` — what ships
- `app/src/main/java/ykws/android/maro/spatial/RouteEngine.kt` — the seam a real engine fills, and what it promises
- `app/src/main/java/ykws/android/maro/ui/map/MapScreen.kt` — the one expression that builds the engine and hands it to the view model

## Next Step

Nothing is outstanding on the branch — the delivery is complete and the next move is the user's: whether the search is designed again, and on what the two documents say about the first two attempts.
